# Corpus 2 accuracy instrument

Freeze 240 statements. Stability gate 0 mismatched slots. Spans 125 of 125 mapped. Seed 20260909. Stage 1 spend USD 0.1730 across two passes.

## Scoring rules not yet implemented

- S18 table-cell rows have no C/P/X target. They must be excluded from BOTH the catch denominator and the leave-alone denominator, and reported as their own line: how many returned confirmed, which is the failure, against how many did not.
- S17 draft-internal pair rows are labelled and scored as ordinary statements, because each half is individually supported. The PAIR relation is recorded and is not scoreable by the statement scorer. Do not let a pair row's statement label be read as evidence about the pair.
- Group A in the manifest is 48 and is the catch denominator. The six S17 statements and three S18 statements are in Group B by construction and must be routed out of leave-alone before any rate is quoted.
