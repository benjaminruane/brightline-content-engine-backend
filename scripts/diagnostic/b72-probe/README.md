# B72 probe

Draft asserts gross margin of 45 per cent. The only source percent is EBITDA margin of 19 per cent. Difference is 26 points (over the 15-point percent threshold). No other percent figures.

On current main the bags intersect on `margin`, so the magnitude backstop forces a false conflict. After B72 the ids are `gross_margin` vs `ebitda_margin` and the force is suppressed.
