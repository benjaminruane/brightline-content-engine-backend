Re-bless this file only on purpose.

  UPDATE_EXPORT_GOLDEN=meridian-2026-09-18 npx vitest run tests/b245-export-and-payloads.test.mjs -t "canonical export"

Setting the env var to 1 does nothing. Then commit the new file as its own decision.
