# Brightline Content Engine - Launch rows

> **Generated file. Do not edit by hand.**
> Derived view of BACKLOG.md rows where Launch = LAUNCH.
> Source of truth: `docs/BACKLOG.md`.
>
> **To regenerate:** `npm run launch:summary`
>
> Last generated: 2026-09-26

LAUNCH means needed before a first client uses the product. See the Launch plan in `docs/ROADMAP.md`.

## LAUNCH rows (21 total)


### 2. Backend / Pipeline (17)

| ID | Item | Priority |
|----|------|----------|
| B79 | **OPEN. GUARDED. Vercel 4.5 MB request-body ceiling limits source documents to about 3 MB.** Vercel rejects any function request body over 4.5 MB at the edge. Because the function never runs, it never sets CORS headers, so the browser reports a CORS failure and the app receives only `TypeError: Load failed`. The real 413 never reaches application code. Confirmed in production 2026-08-21 with an 11.3 MB PDF. After base64 inflation the practical ceiling is roughly 3 MB of source documents in total across all sources in one Review, not per file. Many real client documents will exceed this. The client-side guard (**F20**, **F21**) makes the failure honest; the tool still cannot review a document over about 3 MB. Pre-existing since first deployment. Unrelated to persistence. Found while discussing a different limit. Distinct from the 4 MB review-state autosave limit (raw bytes dropped, `v8.73.0-review-state-no-blobs`). Real fix is **Pr14** (deferred). Local checks are invalid (**P14**). | H |
| B89 | **`supportSpans[].classification` can disagree with the card / Stage 2 verdict. OPEN. One contract with B149, this face.** Originally: `supportSpans` reads `conflicting` on spans the card treats as confirming, with `hasConflict` false, on two statements in both Meridian passes (drawer conflict styling on confirming evidence). **2026-08-27 cross-link (R10 production verify `baed6ed`):** on the mark card (eval-ablation EA_E3, `meridian_source.txt`) the reverse split appears: Stage 2 and the card are `conflicting` / displayVerdict `conflict` / concernLevel `high`, while multipassage `supportSpans[0].classification` is still `confirmed` on the mark passage. That mark card is now the most prominent product change from the false-green arc, so the mislabel is the first thing a viewer of Sources would see. Widen pass does not feed aggregation (existing product split). Distinct from **B39** (confirmed verdict + `hasConflict` true). Priority M. **Confirmed 2026-08-29 (author-versus-third-party sweep, `b55ab00`).** The framing in this row is right and should not be re-attributed. This is **not** a coordinate-frame or span-authority fault: the sweep read every consumer and each one reads its own frame correctly. It is a **classification disagreement between two deliberately decoupled selection paths**, exactly as written above. Do not re-diagnose it as an offset problem. The other face was **B149** (silence reading those spans). Silence face shipped 2026-09-03. This row is the drawer / Sources mislabel. | M |
| B96 | **`AUTHORING_ORGANISATION` is set to "Halden Group" in Production and Preview.** That is a fixture name and a test value, not a decision. It is a per-deployment global. The presence check makes the failure mode for a real draft the safe one (name not in the draft, fallback, no substitution). This is the argument for the request field (**B95**) carrying it properly. | M |
| B100 | **Observability gaps. OPEN on (a) and (b).** (a) What the authoring organisation resolved to. Only failure is reported, and the message describes a detection step that does not exist. (b) Whether an editorial concern was produced. Editorial logs "starting" but never a completed count; compliance logs a count. **(c) CLOSED as not a defect.** The Stage 2 fingerprint is OpenAI's `system_fingerprint` copied off the completion, not a hash of prompt or source. It is not in the LLM cache key; that key includes `sourceText` and `inputText` directly, so two documents cannot collide. `QC_LLM_CACHE_DISK` is safe on this count. Identical `fp_*` values across documents are expected (OpenAI serving snapshot). See Closed **B100c**. | M |
| B124 | **Per-request author identity. Every tenant on a deployment shares one author identity.** `isAuthoringOrganisationName` / `resolveAuthoringOrganisationName` (`lib/qc/first-person-actor.mjs`) resolve from the **`AUTHORING_ORGANISATION` environment variable**, which is a per-deployment global, rather than from the per-request `authoringOrganisation` that `api/analyse-statements.js` already parses and threads. Invisible today because there is one client. Wrong the moment a second client shares a deployment. Cross-ref **B95** (request supply route exists and has never executed) and **B96** (the deployed value is a fixture name). | L |
| B156 | **OPEN. Scoped 2026-09-03. Not built.** Fifth evidence **display** state: a source spoke and gave a different period or value. Not a conflict. Do not present as one. Stage 2 classification stays `no_support`. Do not move `applyPeriodGateBackstop`. Do not change the Stage 2 pin. Copy `preBackstopClassification` and `periodAssessment` onto the public card (today they die at `lib/qc/pipeline-v4/index.mjs` L364-377). Stage 4 should still emit the competing excerpt. Stage 5 must not say no source addresses. Unlock of Implement Changes is a **later slice** and needs an exact-quote locator (29 August rails: only the source's stated value, only the addressed element, never a surrounding rewrite, no proposal if it cannot be quoted exactly). Wording is Ben's; fifth freeze carve-out on **B153**, not invented here. Hash-pinned R10 blast scores `classification`, not `displayVerdict`. | H |
| B158 | **SECOND EXCERPT GAP. OPEN.** 11 conflict cards on `runs/evidence-pass-lift-1` carry their quote in `primaryExcerpt` with `conflictExcerpt` empty. Span-only empty excerpt is shipped (`3214646`). This remaining hole is the dedicated conflict field empty while the competing passage sits in the primary slot. Fix before the sources drawer is built. Distinct from **B147** (`conflictValues` still null). **Annotation 2026-09-10:** live F18 Review (`live-2026-09-10-f18-review.json`) has `primaryExcerpt` as a string on 10/10 cards and `conflictExcerpt` as an object on 1/10 (null on 9). Stringify `conflictExcerpt` to the same wire type Design 2 already uses for `primaryExcerpt` (passage string or null) so a card stops carrying two excerpt fields in two shapes. Shape fix is not the empty-slot hole; do both before the drawer. | H |
| B163 | **INGESTION. OPEN.** 7 Sep 11-file check is superseded as a planning number by this pass. Report `scripts/diagnostic/delivery-check/b163-ingestion-reality.md`. Of 20 real in-scope PDFs, 13 produced a Review a reader could trust. 19/20 extract on production inside 300 s; d13 JPM letter `fetch failed` at 301388 ms. The 389 KB 3i PR (d01) that timed out at 60 s now finishes in 207727 ms. Original four that never arrived: three HTTP 413 `FUNCTION_PAYLOAD_TOO_LARGE`, one (`pg-annual-results-2024.pdf`) under 4.5 MB and blocked only by the 4.2 MB client guard. Extract wall tracks page count / Workiva-InDesign, not bytes. Distinct from **B79**. **B164** does not speak on 413, timeout, or `PDF_INLINE_TEXT_NOT_ALLOWED`. **Annotation 2026-09-26 (B334):** extraction time is solved by **B321** and **B327**. What remains of B163 is the upload cap (**B79**) and tables. | H |
| B165 | **SPAN-ONLY EMPTY PRIMARY. OPEN. Out of scope for the Design 2 card-face quote.** 23 of 261 lift-1 cards (`runs/evidence-pass-lift-1`: 13 `supported_full`, 10 `supported_partial`) carry a nonempty `supportSpans` passage and an empty `primaryExcerpt`. The drawer can highlight the span; the card face still has no quote. Distinct from **B158** (dedicated `conflictExcerpt` empty on 11 conflict cards) and from **B147** (`conflictValues` still null). Fixing it means putting `supportSpans` on the card face, which is a different contract. Do not build it in the primaryExcerpt stringify/paint slice. | M |
| B166 | **COMPETING PROPOSALS. OPEN. Filing only.** Evidence and editorial on one sentence can propose incompatible actions. Live F18 Review: **4** of 10 cards (S0, S2, S5, S7) are `conflict` plus a nonempty editorial `suggestedDirection`. S5 is update-the-figure versus delete-the-sentence. Apply already refuses a double write (`plannedWrites` drops the statement if `list.length !== 1`); bulk accept leaves colliding rows unset. Harm is confusion on the card, not a silent merge. Duplication judge keeps voice/delete as INDEPENDENT. **Annotation 2026-09-11:** later live F18-S3 now also has this shape (evidence 380→412 versus editorial 240'000→240,000). That second pair is **B176**; do not close this row into that one. Do not fold into Design A or B. | M |
| B171 | **USER-SUPPLIED AUTHORING ORGANISATION. OPEN. Filing only.** Ben 2026-09-11: the house name should be supplied by the user, not by a server setting. Account settings (do not exist yet) or a pre-populated input field. The request-body path already exists and has never been exercised (**B95**). Environment value is one identity per deployment (**B124**). After `202b357` a wrong or missing value is no longer dangerous but silently does nothing. When built, draft-presence changes job from gate to sanity check: if the user states an organisation the draft never mentions, tell them rather than acting silently. Do not close **B95**, **B96**, **B124**, or **B170**. | M |
| B257 | **OPEN. Filing only.** On recovered PDF text the card statement keeps mid-sentence hard line breaks; the quoted passage flattens them to spaces. Offsets still cover the region via repair-normalise. Typed fixtures and both accuracy corpora never contain this class. CONFIRMED recovered Shopify slice: A1 17 exact, B1 2 exact / 30 whitespace. | H |
| B258 | **OPEN. Filing only.** Extractor turned curly apostrophes into ` n s` (`Shopify n s`, `That n s`). Stage 1 copied that onto the card as the statement. Exact to the extract, not to the PDF. | H |
| B264 | **OPEN. Filing only.** 26 cards quote the B1 stub sentence `The firm is evaluating an investment of up to $7,000,000.` as `primaryExcerpt`, including `Date: October 12, 2010` marked Conflicting. 20 of those have no locatable `supportSpans`. Drawer cannot highlight. 158 cards have no quote. Related **B259**. **Annotation 2026-09-20:** **B259** now hides a quote with no locatable span, so those 20 cards should show a hole rather than the B1 sentence. The 6 with spans can still quote B1. Classification unchanged. Do not close this row into B259. **Annotation 2026-09-21 (B305 3.7):** this is the B259 residue. The unrelated-document quote fell from 26 cards to 6. The six remaining show an honest hole rather than another document's words only where the span was unlocatable. The cure for the six spanned B1 quotes is the span work and needs real documents. No new row. | H |
| B314 | **TWO CONVERTS CAN OVERRUN THE FUNCTION. OPEN. Filing only.** `extractTextFromSource` runs `OfficeConverter.convert` "text" then "chunks", each with `EXTRACTION_TIMEOUT_MS` 300_000. Confirmed live: `3i-audited-fs-2025.pdf` 232 s + 232 s = 464 s. Production would kill the request with no partial result. Do not fix in a drive-by. | H |
| B315 | **EXTRACTION SPENDS THE REVIEW'S FUNCTION BUDGET. OPEN. Filing only.** `api/analyse-statements.js` calls `prepareUploadedSourcesForPipeline` on the Review request. Confirmed L172. Combined with B312 times, one real PDF can consume the 300 s before Stage 1. Do not fix in a drive-by. | H |
| B316 | **SOURCES EXTRACT SEQUENTIALLY. OPEN. Filing only.** `prepareUploadedSourcesForPipeline` awaits each source in a for loop. Three slowest B79-passing files from B312 sum 830 s. Do not fix in a drive-by. | H |

### 3. Process & governance (2)

| ID | Item | Priority |
|----|------|----------|
| P6 | Business backlog gap | M |
| B310 | **WE CANNOT SEE OUR OWN SPENDING. OPEN. Filing only.** The API key the product uses cannot read the provider's usage or billing endpoints (403, missing `api.usage.read`). Nobody could see the credit balance running down before it stopped production on 20 September. Needs a separate admin key with usage read, and a balance alert. | H |

### 4. Product (1)

| ID | Item | Priority |
|----|------|----------|
| Pr16 | **Internal consistency in Review — the draft is never compared against itself. Ben has called this a must-have. Still not built.** Every check the product runs is draft-against-source. Nothing reads the draft as a whole, so a memo can state that a commitment was made in June and then recommend approving that same commitment, and **pass clean**. The live Meridian fixture does exactly this (**P30**). Splits cleanly in two: a **deterministic half** — the same quantity stated two ways, a date that contradicts a tense, a figure that disagrees with itself — and a **judgement half** needing a model. **The deterministic half is worth doing first**: it is cheap, it has no verdict-layer exposure, and it catches the shape above. Distinct from `narrative_coherence`, which is per-statement and, per **B99**, not blind. **Cross-link B131:** Review also does not check two `suggestedDirection` strings on the same statement against each other. That is the same class of defect (output unchecked against itself), scoped to one card rather than the whole draft. **Cross-link B172:** when a source contradicts a base figure, dependents in that sentence hang and the product currently says nothing; surface as a finding, do not duplicate this row. **Pr17 shipped 2026-09-01–02** (no action-list tag; residual **B151**). Sequenced after the Review group and the wording pass (operator 2026-09-02). | M |

### Parked (1)

| ID | Item | Priority |
|----|------|----------|
| Pr14 | **DEFERRED. Blob storage for source files (removes the 3 MB ceiling).** The real fix for **B79**. The browser uploads directly to object storage and sends only a reference; the backend fetches the file server-side where the 4.5 MB rule does not apply. Also removes the re-upload-after-refresh limitation (**F16**). Scoped in full on 2026-08-21 and deliberately deferred the same day. Reasoning: the ceiling does not bite during development; building it means storing client source documents, which the persistence chapter deliberately avoided; the client deployment model is undecided, so it may be superseded by a client's own storage; **B9** is the higher-value next step. Full scoping (recommended provider, permanent choices, three decisions already taken) sits in the Claude project note `claude/blob-storage-scoping-note.md` (Claude project doc, not in this repo). Trigger to revisit: a real source document over 3 MB needs reviewing, or the client deployment model resolves in favour of the hosted app. | L |
