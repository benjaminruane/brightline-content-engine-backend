# Brightline Content Engine - Backlog

This document is the single source of truth for known issues, deferred work, future improvements, and product enhancements that have been identified but not yet scheduled into a sprint.

It complements:
- `docs/ROADMAP.md`: the active sprint plan
- `docs/ARCHITECTURE.md`: the system design
- `ai/AI_OPERATING_MANUAL.md`: how Cursor works on this codebase
- `docs/LAUNCH_SUMMARY.md`: derived list of rows marked LAUNCH (regenerate; do not edit by hand)

---

## How to use this file

**When something gets deferred** during a working session (a UI bug, a code-hygiene item, a future optimisation, a copy change, an architecture nuance) it gets a row in the right Open work table, or in Parked if it is explicitly parked, deferred, or watch-only.

**When something is ready to schedule**, it moves from Open work into a spec (and eventually into the roadmap).

**When something is shipped**, the row is moved to Closed with the spec ID and tag that resolved it.

**Findings log** holds accepted costs, recorded observations, abandoned or rejected attempts, and standing measurement constraints. These are not tasks.

**Standing rules** holds process lessons. Do not schedule them.

**Adding new items**: see "How to update this file" at the bottom.

The Launch column has three values: **LAUNCH** (needed before a first client uses the product), **AFTER** (worth doing, not needed for a first client), **RECORD** (not a task).

---

## Tables

Work still to do sits under Open work, split by character:

1. **Frontend / UI**: user-visible bugs, copy, layout, polish
2. **Backend / Pipeline**: non-blocking pipeline issues, optimisations, architecture nuances
3. **Process & governance**: operational items that are actual work
4. **Product**: net-new features, capability gaps

Parked, Findings log, Standing rules, Closed, and Rejected follow, in that order.

---

## Open work

## 1. Frontend / UI

| ID  | Item | Source / context | Priority | Launch | Notes |
|-----|------|------------------|----------|--------|-------|
| F5  | UI display labels misaligned with architecture rubric | R2.3 user observation | M | AFTER | "Supported" should be "Confirmed". Align all four labels: confirmed / partially confirmed / conflicting / no support. Decision: more definitive language preferred for an audit-safe product. |
| F6  | "1 claim have" grammar bug in Reviewer Assessment | userMemories | L | AFTER | Pre-existing item from product backlog. |
| F10 | Single-concern Editorial cards may render the lone concern as a bullet — check rendering consistency across single-concern Editorial cards (bulleted vs inline). | R2.7.2 Run 1 testing | L | AFTER | |
| F11 | **Author-feedback UI:** top-right controls (Hide assessment summary + View/Generate author feedback) look cluttered stacked together. Try ghost/text buttons or relocating the collapse toggle to the container edge. | B26.2.2 testing 2026-06-30 | L | AFTER | |
| F23 | **Filenames now break with `break-all` (B192), which can split a name at an awkward point.** Cosmetic. | B192 2026-09-15 | L | AFTER | Cosmetic. |

**Suggested grouping**: F1–F5 share the Assess module surface and could be bundled into a single frontend polish sprint, e.g. `v8.43-assess-polish`.

---

## 2. Backend / Pipeline

| ID  | Item | Source / context | Priority | Launch | Notes |
|-----|------|------------------|----------|--------|-------|
| B1  | Stage 2 prompt nuance: "all X, therefore not Y" patterns | R1.2.5.3 reproducibility findings (P02, P25) | L | AFTER | When source says "all employees in Ottawa" and draft claims "offices in Berlin," current rubric drifts between `no_support` and `conflicting` at temp 1. Worth a prompt-tightening spec eventually, but not blocking. |
| B2  | "Excerpt could not be retrieved" on conflict cards | R1.2.3, R2.3 testing | L | AFTER | v3 Stage 4 artifact. Architecture says Stage 2 should already return the passage; v3's separate retrieval step is unnecessary. Should resolve when R2.5 (Stage 4 in v4) lands. Substantially mitigated by r2.5.3 for bracket-abridgement cases. Residual placeholder appears only on silent-splice cases — see new backlog item B-next on splice handling. |
| B4  | LLM call consolidation: merge editorial + compliance + style | userMemories | M | AFTER | Architecture-level optimisation. Three review calls per statement could potentially become one. Cost model projects ~19% saving on production runs. Defer until v4 is fully live. |
| B5  | Stage 2 chunking cost ceiling for long sources | userMemories | L | AFTER | Pre-existing. Long sources can blow up Stage 2 token cost. Defer. |
| B7  | Document "internal source contradiction" handling | R1.2.5.2 finding | L | AFTER | Stage 2 already handles this correctly (returns `conflicting` when a source contradicts itself), but the architecture document doesn't acknowledge this case. Add a paragraph to architecture section 5.2. |
| B8  | E2 deterministic reimplementation | userMemories | L | AFTER | Pre-existing. Defer. |
| B9  | Implement-changes sprint (writer applies QC suggestions) | userMemories | L | AFTER | Pre-existing. Defer. Interim lighter version: **Pr9** (shipped). **Unblocked-but-not-built (2026-08-21):** a store exists (`persist-review-state`), but `review_state` is an overwrite autosave buffer. B9 needs append-only finding-decision event rows in a separate table (ARCHITECTURE §9). **Reframe (2026-08-23):** acting on a finding and acting on an edit are two different workflows. B9 as scoped covers only the first. Per-change accept / reject / modify on Review shipped as **Pr17** (2026-09-01–02; no action-list tag; residual **B151**). That does not close this row. **Annotation 2026-09-14 (B186):** `reviewer_decisions` is the beginning of that table. First kinds are source-governance rulings and leave-as-written overrides. Per-finding accept and reject decisions are still not recorded. Retrieval beyond the simple GET waits for accounts. **Verified live on production 2026-09-14** by reading a stored row back. Draft hash on each row is sent from **B187**. Narrative detail for the Pr9 rewrite-correctness arc also lives in Claude project docs outside this GitHub tree (the sync does not carry `claude/*` into the repo); in-repo record is ROADMAP Pr9 rewrite-correctness and Closed **Pr9-correctness**. |
| B10 | Spring clean / codebase hygiene | userMemories | L | AFTER | Pre-existing. Defer to after v4 is live and v3 is deletable. |
| B11 | Public version prompt | userMemories | L | AFTER | Pre-existing. Defer. |
| B12 | Add canary score for non-schema Stage 2 failures (network errors, timeouts) so they're tracked in Langfuse alongside schema failures. | R2.3 implementation review | L | AFTER | |
| B14 | Model non-compliance with explicit prompt rules: gpt-4o does not reliably honour negative constraints (e.g. 'do not abridge passages') on dense compound statements, even at temp 0. Future stages should design defensively — validation layers that handle expected non-compliance modes rather than rely on the model's adherence. | R2.5.2.1 diagnostic finding | M | AFTER | |
| B15 | Stage 2 occasionally returns silently spliced passages (multiple non-adjacent source spans presented as contiguous, with no ellipsis marker). Validator correctly rejects these, but the resulting placeholder appears on conflict cards. Possible mitigations: (a) two-call retry asking for a single contiguous span when first call's passage fails validation, (b) accept and surface explicitly as 'spliced excerpt - confirm against source'. To consider after pipeline-v4 fully live. | R2.5.3 testing | M | AFTER | |
| B17 | **R2.7.2.1 — Relative-source-period resolution.** Stage 2 period matching only works explicit-vs-explicit; fails when the source period is a relative reference requiring document-date inference. Proposed fix: deterministic date-resolution pass pre-Stage-2. See ROADMAP **R2.7.2.1**. **Note (2026-08-18):** the supersession work (`review-supersession`) added date-aware machinery at the VERDICT layer (`extractSourceAsOfDate` fallback in `resolveSupersession`), but B17's Stage-2 relative-period MATCHING remains unaddressed. Cross-ref `review-supersession`. | R2.7.2 testing | M | AFTER | |
| B41 | **Stage-2 multi-passage widening. PARTIAL.** Intra-source case CLOSED 2026-09-08 where the widened matcher returns a contradicting span: reducer (`ba57db7`) takes most-serious of single-pick and locatable spans, then Stage 3 conflict-wins. Remaining: the matcher still misses some contradicting lines (**B161** F13 EBITDA 11.1%), and the 400-character one-quote format is **B123**. Do not treat this row as fully closed. | R7 Option-2 depth 2026-08; accuracy arc 2026-09-08 | M | AFTER | Intra-source closed where the span is returned. Matcher miss is B161. |
| B42 | **includeFiles bundle rework (real fix).** `excludeFiles` alone cannot reliably defeat load-bearing `includeFiles: "node_modules/**"` (see `docs/R7_BUNDLE_ANALYZE_FINDINGS.md`; esbuild/@napi-rs excludes are interim). Narrow/replace the blanket include; let NFT trace per-function; **explicitly re-include** OpenAI transitive deps + dynamic `import("officeparser")` that NFT historically missed (original reason for the blanket + 256-char limit). Fold **`tests/**` trim** into this pass (test files currently ship into every function via the same include string — do not edit include alone without that careful rework). **PREVIEW-deploy verify** before production. Currently **masked** by `VERCEL_SUPPORT_LARGE_FUNCTIONS` beta flag (crutch). | Vercel bundle / R7 2026-08 | M | AFTER | Drop beta flag after |
| B43 | **Compressed 50MB function limit — unmeasured.** Re-check after **B42** includeFiles rework (uncompressed 250MB and compressed 50MB are separate gates). | Vercel bundle 2026-08 | L | AFTER | After **B42** |
| B44 | **alp/crf authority-assertion harness quirk.** `qc:test` raw harness 7/9 vs 9/9 first-card evidence-pass: `alp_multisource` / `crf_multisource` fail only on `assertAuthority` (`meta.qcEvidenceAuthorities` not attached by v4 `analyse-statements`). Set `assertAuthority: false` on those fixtures or attach authorities. Cosmetic; keeps extractor/future baselines clean. | extractor baseline 2026-08 | L | AFTER | Harness only |
| B61 | **Stage 2 run-to-run non-determinism at temperature 0 (residual `hasConflict`).** Diagnosed 2026-08-19 (`48d8483`, `b81929b`). B53a removed **verdict** instability on the measured subset (1/38 → 0/38 with the flag on). Residual is confined to `hasConflict` on two F18 statements (5.26%), unchanged by B53a because the upgrade must not touch `hasConflict`. OpenAI `seed=1` does not pin it: three distinct `system_fingerprint` values appeared in one diagnostic. **B63 does not close this.** B63 is a process-local cost cache; it does not survive a cold start or an instance change. **Unblocked-but-not-built (2026-08-21):** Neon exists (`persist-review-state`), but that table is the frontend autosave blob, not a durable Stage 2 LLM-result cache. Cross-session `hasConflict` repeatability still needs a store of a different shape. **Annotation (2026-08-24):** do not treat B61 as a blanket prerequisite. See **B83**. Two Review runs on an identical draft (2026-08-23) held evidence verdicts 10 of 10; instability was concentrated in the editorial layer (4 of 10 differed). Verdict-level work can be measured without first solving instability. Editorial-rule work needs repeat runs. Residual `hasConflict` on F18 remains this item's original scope. **Boundary (Meridian re-run 2026-08-24):** that finding is "10 of 10 held on two runs of one draft", not "the evidence layer is stable". Do not quote the second version. On the re-run, a byte-identical statement moved from `not_supported` / high to `supported_partial` / moderate. The second answer is the better one, but it is a flip on unchanged text and it took a card carrying an unsupported eight-figure commitment from high concern to moderate. Editorial instability reproduced independently on the same run: a byte-identical sentence picked up a `sentence_length` concern in one pass and not the other. Planning consequence unchanged. **Annotation (2026-08-25):** part of the 4 of 10 under **B83** is rule taxonomy, not determinism. Separate **B93** before scoping this row as an editorial-determinism problem. **Annotation (chapter five 2026-08-25):** revision-layer instability is a separate and worse problem than editorial instability. See **B98**. | B53a shadows + Stage 2 determinism harness 2026-08-19; B63 gate 2026-08-20; persist-review-state 2026-08-21; **B83** 2026-08-24; Meridian re-run 2026-08-24; **B93** 2026-08-25; **B98** 2026-08-25 | M | AFTER | Still open. Cross-ref **B83**, **B93**, **B98**. Store exists; wrong shape. The 10 of 10 is bounded. |
| B53b | **Supersession no-ops on a compound.** **VERDICT-ADJACENT.** "14 facilities / three markets / 720 people" keeps `hasConflict=true` with the 2023 memo (12/640) instead of demoting to a `supersededSourceNote`. Atomic supersession fixture (headcount/money) passed; compound does not. Hybrid (**B53a**, shipped) helps via per-claim matching / passage pick, but "14 facilities" is not a `collectBackstopFigures` kind (percent / money / headcount only) — decomposition will not invent a facilities extractor. Split from **B53**. | Nordholt expanded-fixture / compound diagnose 2026-08-19 | M | AFTER | Verdict-adjacent. |
| B55 | **`first_person_plural` under-fire on "We expect".** First-person plural is a codified output-type rule (reporting commentary = third person). The deterministic backstop correctly kept all three first-person statements ("We now run", "our acquisition", "We expect"), but the LLM raised `first_person_plural` / `voice_consistency` on the first two and missed "We expect" in the same dirty-draft run. Genuine under-fire — the rule should fire on every first-person pronoun in reporting commentary. | Nordholt dirty-draft run 2026-08-19 | M | AFTER | |
| B58 | **Stage 5 evidence commentary double-states the draft claim.** Stage 5 commentary restates the draft's own claim before describing the source evidence (e.g. "The statement says X. The source confirms X."). The first sentence adds no information — the reviewer already sees the statement on the card. Calibrate Stage 5 prompt to lead with the source finding, not a restatement of the claim. Cross-ref **B18** (editorialises) / **B22** (commentary register). | Nordholt expanded-fixture runs 2026-08-19 | M | AFTER | |
| B74 | **Stage 1 statement text varies on bullet lines.** S. One run in five on F15 (identical input, cache off) dropped the leading `- ` from five list items. Words unchanged; statement text and highlight offsets changed, so the LLM cache key busts. Fix: strip leading list markers deterministically and adjust the offset so our layer absorbs the variance. **Supersedes** the earlier claim that unchanged drafts always produce byte-identical statements (measured on too small a sample; 4/5 identical is not always). | Stage 1 stability harness F15 2026-08-21 (5 repeats, cache off) | S | AFTER | Small. Deterministic strip, not a prompt change. |
| B79 | **OPEN. GUARDED. Vercel 4.5 MB request-body ceiling limits source documents to about 3 MB.** Vercel rejects any function request body over 4.5 MB at the edge. Because the function never runs, it never sets CORS headers, so the browser reports a CORS failure and the app receives only `TypeError: Load failed`. The real 413 never reaches application code. Confirmed in production 2026-08-21 with an 11.3 MB PDF. After base64 inflation the practical ceiling is roughly 3 MB of source documents in total across all sources in one Review, not per file. Many real client documents will exceed this. The client-side guard (**F20**, **F21**) makes the failure honest; the tool still cannot review a document over about 3 MB. Pre-existing since first deployment. Unrelated to persistence. Found while discussing a different limit. Distinct from the 4 MB review-state autosave limit (raw bytes dropped, `v8.73.0-review-state-no-blobs`). Real fix is **Pr14** (deferred). Local checks are invalid (**P14**). | production 11.3 MB PDF 2026-08-21; `v8.75.0-source-size-guard`, `v8.76.0-size-message-wording` | H | LAUNCH | OPEN. GUARDED. Honest, not fixed. |
| B80 | **Pr9 residual: a clause-cut can be labelled CHANGED rather than CUT.** The marker wrapped the surviving sentence rather than a remnant (logged on F10 at `pr9-marker-intent`). Honesty passes either way. Intent does not reliably distinguish a rewrite from a removal if the UI wants to show those two differently. Do not change the honesty check to paper this over. **Still open, verified against the code 2026-08-29.** The marker-honesty arc (`7399333`, `bf9d9e8`, `fc25060`, `a5be4f0`) rebuilt the **what clause** so it is generated from the real diff rather than from the model's prose (`applyNoteWhatFromDiff`, live in `finalizeSuggestRevisionText`). That closes the false-note class and one instance of **P20**. It does **not** close this row: **intent is still parsed from the model's own declaration** (`pr9-marker-honesty.mjs` reads `CHANGED`/`KEPT`/`CUT` from the marker text) and is only checked against span status afterwards. A clause-cut can therefore still be labelled CHANGED. **Seen live on all three Production Meridian runs (2026-08-31), in both directions.** NOT the same defect as **B140**: B80 is a labelling residual; B140 was a comparator lie. Keep the standing instruction: do not change the honesty check to paper B80 over. **Watch (B138, 2026-08-30):** the Production `[pr9-marker-honesty]` intent=KEPT on a replaced span (coverage-gap S5) should stop firing on that cell once the sentence is not replaced. That is a watch, not a gate. This row stays open regardless. | `pr9-marker-intent` 2026-08-23; `pr9-cut-punctuation` log; code read 2026-08-29; B138 ship 2026-08-30; live Meridian x3 2026-08-31 | M | AFTER | Logged, not repaired. Labelling residual, not B140. Intent still model-authored. |
| B81 | **Pr9 residual: punctuation pass does not repair stranded leftovers.** `pr9-cut-punctuation` tidies joiners left behind by a clause cut (comma/semicolon/colon before terminal punct, doubled punct, space before punct, doubled spaces, dangling lowercase `and`/`or`/`but`/`nor`/`with`). It deliberately does not repair stranded prepositions, articles, verbs, or other connectives (`at.`, `the.`, `The company trades at.`, `The team now numbers.`, `including.`). A wrong repair is worse than a visible fault. | `pr9-cut-punctuation` 2026-08-23 | L | AFTER | Deliberate non-fix. |
| B82 | **Revision routing keys off a model-authored verb. LATENT.** For most editorial rules, `classifyEditorialKind` (`lib/build-revision-prompt.mjs`) reads the leading verb of the reviewer's suggested direction against `DELETION_VERB_RE` (`Remove`, `Cut`, `Delete`, `Drop`, `Strip`, `Omit`). Order of the function: `style_guide` routes to `"craft"` first, then `marketing_language_excess` to `"soften"`, then materiality or a leading deletion verb to `"deletion"`. `"soften"` and `"deletion"` are different kinds. The marketing special case PREVENTS a Delete direction on `marketing_language_excess` from matching `DELETION_VERB_RE` and becoming keep-and-flag. It is a guard, not a trap. B82 as originally logged remains LATENT: if that special case were ever removed, `review-hype-delete`'s deterministic `Delete` prefix would match `DELETION_VERB_RE`, route to keep-and-flag, and silently stop the deletion the rule exists to perform. What was biting before `5ff7ced` was the soften branch's rewrite-needed line, which is **B87** and is closed. Durable fix remains a structured action field (`delete` or `keep`) set by the reviewer schema. See **P20**. | review-layer chapter 2026-08-24; `review-hype-delete` (`63c359f`); chapter five 2026-08-25; code read 2026-08-25 | M | AFTER | Latent. Guard, not a trap. Closed **B87** was the live bite. |
| B83 | **Instability is concentrated in the editorial layer. OPEN. Refines B61; do not duplicate.** Two full Review runs on an identical draft, 2026-08-23: evidence layer, 10 of 10 statements returned identical verdicts (conflicts, partials, and not-supporteds all stable); editorial layer, 4 of 10 statements differed. A 344-character sentence was flagged for length in one run and came back clean in the other. A filler phrase was flagged once. The same first-person problem was reported under `first_person_plural` in one run and `voice_consistency` in the other. An extra `underreach_hedging` appeared in one run only. Planning: verdict-level work can be measured without first solving instability, because the evidence layer holds. Editorial-rule work needs repeat runs to verify, because it does not. **B61** should be scoped to editorial instability specifically rather than treated as a blanket prerequisite. Residual Stage 2 `hasConflict` drift on F18 remains **B61**'s original scope. **Boundary (Meridian re-run 2026-08-24):** the finding is "10 of 10 held on two runs of one draft", not "the evidence layer is stable". See **B61**. **Annotation (2026-08-25):** part of the 4 of 10 is rule taxonomy, not determinism. `first_person_plural` and `voice_consistency` share one prompt block; the model picks arbitrarily. Separate that out (**B93**) before **B61** is scoped. **Annotation (chapter five 2026-08-25):** revision-layer instability (**B98**) is a separate and worse problem than editorial instability. Any measurement of revision behaviour needs repeat runs. | two Review runs 2026-08-23; refines **B61**; Meridian re-run 2026-08-24; named-actor reconcile 2026-08-25; chapter five 2026-08-25 | M | AFTER | Cross-ref **B61**, **B93**, **B98**. The 10 of 10 is bounded. |
| B85 | **Fixture 01 is a temporal hybrid, and B48's exhibit sits on it. OPEN. Check before treating that exhibit as settled.** Audit finding: `01_bvp_shopify_memo` is the real 2010 Bessemer memo with 2019 merchant and GMV material spliced in, almost certainly deliberately, since fixtures 91 and 92 are the temporal-gap and stale-present-tense cases built from it. That source is therefore internally inconsistent across two eras by construction. The **B48** review-calibration exhibit, where the same figure was judged differently depending on the length of the sentence containing it, lives on this fixture. Some of what reads as review inconsistency there may be the source genuinely carrying both a 2010 and a 2019 view of the same business. **Reconciliation note:** the instruction was "confirm before B48 is scoped", but **B48** already shipped 2026-08-16 (`review-B48`, `b4bc974`). This row is a check on that shipped exhibit's source, not a pre-scope gate. Confirm before treating B48 observations on this fixture as review inconsistency. | fixture audit 2026-08-24; Closed **B48** | M | AFTER | Check before scoping further B48 calibration on F01. |
| B89 | **`supportSpans[].classification` can disagree with the card / Stage 2 verdict. OPEN. One contract with B149, this face.** Originally: `supportSpans` reads `conflicting` on spans the card treats as confirming, with `hasConflict` false, on two statements in both Meridian passes (drawer conflict styling on confirming evidence). **2026-08-27 cross-link (R10 production verify `baed6ed`):** on the mark card (eval-ablation EA_E3, `meridian_source.txt`) the reverse split appears: Stage 2 and the card are `conflicting` / displayVerdict `conflict` / concernLevel `high`, while multipassage `supportSpans[0].classification` is still `confirmed` on the mark passage. That mark card is now the most prominent product change from the false-green arc, so the mislabel is the first thing a viewer of Sources would see. Widen pass does not feed aggregation (existing product split). Distinct from **B39** (confirmed verdict + `hasConflict` true). Priority M. **Confirmed 2026-08-29 (author-versus-third-party sweep, `b55ab00`).** The framing in this row is right and should not be re-attributed. This is **not** a coordinate-frame or span-authority fault: the sweep read every consumer and each one reads its own frame correctly. It is a **classification disagreement between two deliberately decoupled selection paths**, exactly as written above. Do not re-diagnose it as an offset problem. The other face was **B149** (silence reading those spans). Silence face shipped 2026-09-03. This row is the drawer / Sources mislabel. | Meridian re-run 2026-08-24; R10 production verify `baed6ed`; sweep `b55ab00`; B154 exhibit 2026-09-03 | M | LAUNCH | Drawer / supportSpans mislabel. Same contract as **B149**, opposite face. |
| B90 | **Framing fidelity false positive on "record".** It flagged that a statement uses "record" where the matched source does not, when the source line begins "Track record:". In the first Meridian pass the same rule correctly caught a 23 versus 21 percent discrepancy, so this is a precision problem, not a broken rule. Cross-ref Closed **B53c** (`review-superlative`). | Meridian re-run 2026-08-24 | L | AFTER | Precision, not a broken rule. |
| B91 | **Compound `marketing_language_excess` concern on the team sentence. INTERMITTENT.** One concern carried two unrelated findings as "(i)" and "(ii)", with concernText duplicated. Finding (ii) wrongly asserted that "is a genuine differentiator" uses first-person plural. It produced an ungrammatical output clause. It did not recur on the following run; the two findings arrived as two separate concern objects. Separate module. | Meridian re-run 2026-08-24; named-actor re-run 2026-08-25 | M | AFTER | Intermittent. Did not recur on the next run. |
| B92 | **A card can carry contradictory findings on one span. OPEN. Downgraded. Trust problem, not text corruption.** On the revised draft, one closing sentence came back with both: compliance `hard_concern` `promissory_or_guaranteed_language` ("should deliver returns broadly in line with its predecessor" implies a promise of future performance; soften to "aims to deliver") and editorial `underreach_hedging` ("broadly in line with its predecessor" is overly hedged; REMOVE the hedge). Same clause, same card, opposite instructions, on a forward-looking return statement in a compliance document. `collectEditorialConcerns` in `lib/build-revision-prompt.mjs` skips `underreach_hedging`, so D2 sits on the reviser and the reviser is safe. No revision will strip that hedge. The exposure is that the human still sees the suggestion. **B84** is closed with the false-green arc; this remains a trust/UI problem, not the lead Stage 2 defect. The UI contradiction is the symptom. D2 is a RULE-ID SKIP, not a property check. That is exactly why `first_person_plural` reached over-confidence by a different road in the review-layer chapter and D2 did not stop it. Durable fix: a check on the OUTPUT (did the revision remove a hedge or qualifier present in the source text) rather than a filter on the input. | named-actor re-run 2026-08-25; chapter five 2026-08-25 | M | AFTER | Trust, not text corruption. Durable fix is an output check. |
| B93 | **Rule taxonomy overlap. OPEN. Cheap, and it removes part of what B61 measures.** `first_person_plural` and `voice_consistency` share one prompt block and both can catch the same fault, so the model picks arbitrarily. That is one of the four editorial differences recorded under **B83** as instability, and it is not model noise. Separately, `hyperbole_vs_qualitative` fired where `marketing_language_excess` had fired on identical text the run before. Evaluative language is reachable by at least two rules; voice by at least three. Nothing decides which rule owns a case. Treatment paths also diverge: `hyperbole_vs_qualitative` routes to CRAFT (`isStyleGuideConcern` runs first) while `marketing_language_excess` routes to SOFTEN. Two rules that both catch evaluative language take two different treatment paths. Separate this from **B61** before B61 is scoped. **Annotation 2026-09-18 (B219):** on investor letter, press release, and LinkedIn, `voice_consistency` no longer carries `FIRST_PERSON_ACTOR_INSTRUCTION`, so the first-person-removal versus first-person-house-voice fight is gone on those three types. Reporting commentary still shares the removal instruction between the two rule ids. Hyperbole versus marketing is untouched. Does not close this row. | named-actor re-run 2026-08-25; cross-ref **B83** / **B61**; code read 2026-08-25; B219 2026-09-18 | M | AFTER | Taxonomy, not determinism. Craft vs soften. Narrowed on three output types. |
| B94 | **The first-person-actor harness score is soft.** 18/18 then 17/18 on unchanged code. The miss was the scorer wrongly reporting dropped hedges (`should`, `broadly in line with`) on a rewrite that kept them. A false positive on the one property that harness exists to protect. Do not treat the fraction as a gate until it is looked at. See **P19**, **P22**. | first-person harness 2026-08-24 (17/18 then 18/18 after view-marker bound; 18/18 then 17/18 on 2026-08-24 evening) | M | AFTER | Scorer false positive. Not a gate. |
| B95 | **The request-body supply route added by `review-actor-supply` has never executed.** The harness reaches the house name through the env var, as production does. Code that exists and has never run, which is **B86**'s own shape. Fix when the frontend has something to send. | `review-actor-supply` (`9285e95`); named-actor reconcile 2026-08-25 | M | AFTER | Env path only. Request path unexercised. |
| B96 | **`AUTHORING_ORGANISATION` is set to "Halden Group" in Production and Preview.** That is a fixture name and a test value, not a decision. It is a per-deployment global. The presence check makes the failure mode for a real draft the safe one (name not in the draft, fallback, no substitution). This is the argument for the request field (**B95**) carrying it properly. | named-actor reconcile 2026-08-25 | M | LAUNCH | Fixture name in deployed env. |
| B97 | **The fallback direction contaminates unrelated edits. OPEN.** Cleanest experiment of the arc. Same commit, same actionable refusal form. Only variable: whether the authoring organisation resolved. Local, actor unresolved: "genuinely exceptional" survived, twice. Production, actor resolved: deleted correctly. When the organisation cannot be named, five cards emit some form of "keep this unchanged", and the reviser generalises that into conservatism about edits with nothing to do with first person. A config miss silently degrades the whole revision. Fix: scope the fallback direction so it names the phrase it applies to rather than emitting language a model reads as a general instruction to leave things alone. | chapter five 2026-08-25 (six Meridian runs, local vs production) | H | AFTER | Config miss degrades the whole revision. |
| B98 | **The revision layer is unstable run to run. OPEN.** Two identical local runs, same code, same config, twenty minutes apart. First: five edits attempted, zero applied. Second: four substantive edits landed. Worse than editorial instability, because it changes the document rather than the findings. Any measurement of revision behaviour needs repeat runs, which nothing currently accounts for. Cross-ref **B61** / **B83**. **Measured three times and quantified (2026-08-29, `noise-floor-recheck.md`).** `gpt-5.1-2025-11-13`, temperature 0, seed 1, three runs against **one unchanged Review**. **8 of 10 cards vary.** That design settles the attribution: the Review is held fixed, so this instability is **generated in the revision layer and is not inherited from Review**. Do not describe it as downstream noise. Composition `identical=2 intent=1 note-only=5 prose=2` — the variance is almost entirely **note wording**; the prose is comparatively stable. It is the **same eight cards** every time, and the two stable ones (`fund_desc`, `hold_period`) are the two sentences the Review raises no concern about: every sentence carrying a concern is unstable. Prior measurements `45db80d` (no seed) 7 of 10 and `18ac825` (seed 1) 8 of 10. The `a5be4f0` prompt changes did not move it. **Annotation (2026-08-30). The "almost entirely note wording" framing is no longer supported. Contradicted by one cell, not disproved.** r10-review1 S7 (`recommend` closer): same prompt byte-for-byte, same seeds, same dated model, 78 minutes apart, the DECISION about whether to edit the sentence moved (b122-rescore 05:48Z 0 of 3; b134 reference arm 07:06Z 3 of 3). That remainder is load-bearing. Measurement of this class is superseded by **B135**. Do not treat B98 as disproved. | chapter five 2026-08-25; `noise-floor-recheck.md` 2026-08-29; `reference-arm-contradiction.md` `117cfeb` | H | AFTER | Generated in the revision layer, not inherited. Note-wording framing contradicted by one cell; superseded by **B135**. |
| B99 | **The editorial reviewer is called per statement with `statementCount` 1, but receives the surrounding draft, and files concerns against the wrong statement.** Seen twice in one afternoon, both first-person. `FIDELITY_DROP` catches it only when the quoted phrase happens to be absent from the target statement; a misattribution quoting text the target does contain survives silently. Also means `narrative_coherence` is not blind after all. | chapter five 2026-08-25 backend logs | M | AFTER | Cross-statement leak. FIDELITY_DROP is partial. |
| B100 | **Observability gaps. OPEN on (a) and (b).** (a) What the authoring organisation resolved to. Only failure is reported, and the message describes a detection step that does not exist. (b) Whether an editorial concern was produced. Editorial logs "starting" but never a completed count; compliance logs a count. **(c) CLOSED as not a defect.** The Stage 2 fingerprint is OpenAI's `system_fingerprint` copied off the completion, not a hash of prompt or source. It is not in the LLM cache key; that key includes `sourceText` and `inputText` directly, so two documents cannot collide. `QC_LLM_CACHE_DISK` is safe on this count. Identical `fp_*` values across documents are expected (OpenAI serving snapshot). See Closed **B100c**. | chapter five 2026-08-25; code read 2026-08-25 | M | LAUNCH | (a) and (b) still missing log lines. See **P25**. |
| B101 | **The database failure path is not graceful.** `db=configured` plus a failing credential produces an unhandled rejection that kills the function process. The guard covers `DATABASE_URL` absent, not present-and-failing. Invisible today only because the frontend is not wired to persistence. Small try/catch. | chapter five 2026-08-25 backend logs | S | LAUNCH | Present-and-failing is unhandled. |
| B102 | **`em_dash` false positive.** Fired on statement 0 claiming an em-dash in "fifth-generation flagship fund", which contains a plain hyphen. Survived one run and was dropped the next by `FIDELITY_DROP` purely because the model quoted a different string. Given the standing no-em-dash convention this rule matters. | chapter five 2026-08-25 | M | AFTER | Hyphen misread as em-dash. |
| B106 | **The unsupported region is not always contiguous. The data model assumes one span per pair. Observed floor 2 of 57.** Two of 57 eligible two-step elicit pairs failed validation because the model returned a non-contiguous or non-substring span (ellipsis; omitted wrapper). A single `[start,end)` per pair cannot represent a multi-gap unsupported region. | `review-span-two-step` 2026-08-25 | M | AFTER | Data-model floor. Not a product defect until a multi-gap case is required. |
| B112 | **Worked example 3c is carried on no evidence.** R2 rewrite-ladder measured it moving nothing. Consider dropping it and measuring. Lives in live `stage2_v4.md` (now R10). | rewrite ladder `86d7e2d` | L | AFTER | Drop-and-measure candidate. |
| B114 | **Regenerate claim-spans `.baseline.json` under current live Stage 2 (R10).** File still STALE at promptHash `c718c190` (pre-R3a). Estimated ~$4. Defer until a shadow gate needs a fresh snapshot. Current Stage 2 corpus reference for this arc: `scripts/diagnostic/eval-ablation/r10-corpus-blast-rows.json` (`ce3d85e`). **Annotation (2026-08-29): STILL OPEN. Do not close this against the corpus re-baseline.** The two are different artefacts and it is easy to conflate them. What was promoted at `3122708` is the **Stage 2 corpus baseline**, recorded as `corpusBaseline` in `scripts/diagnostic/fingerprint-manifest.json` and backed by `scripts/diagnostic/anchor-fix-corpus.json` (29 cases, 296 statements, 360 pairs, `gpt-4o-2024-08-06`, prompt sha `44847c61b07b`). This row is the **claim-spans** baseline, `scripts/diagnostic/claim-spans/.baseline.json`, which is gitignored, is untouched, and still carries its own `_staleNote` at promptHash `c718c190` (pre-R3a). Verified by reading the file 2026-08-29. Its `_currentStage2Reference` also still points at `r3a-corpus-blast-rows.json` and is now two references out of date. | R10 ship; `scripts/diagnostic/README.md`; blast rows; corpus re-baseline `3122708` | L | AFTER | STILL OPEN. Distinct from the corpus baseline. ~$4. |
| B115 | **Rich-mark / non-corresponding passage-selection false red. OPEN. RESIZED 2026-08-27. Priority M.** Absolute attention failures are ~1.5% to 4.5% of figure-bearing pairs, so this is not next. **Client-visible sharp end (keep visible at this priority):** rich-mark false red. Trigger (CONFIRMED, `mark-richness-probe.md` `c29bf5a`): a mark or valuation sentence that also carries realised-count or carrying-value content in the same sentence or the one following pulls selection away from a corresponding returned line. RICH mark (fixture 93 / RICH01): `Fund IV (2020 vintage, EUR 750 million) is currently marked at 1.9x gross MOIC and 24% gross IRR.` + following `Four of twelve platform investments are fully realised; the remainder remain in the portfolio at carrying value.` THIN mark (MF01 / THIN93): `Fund IV is currently marked at 1.9x gross MOIC and 24% gross IRR.` Result: RICH01 confl x3, THIN93 conf x3, same draft `Fund IV has returned 2.6 times gross MOIC.` That false red would embarrass in front of a client. Corpus exposure: 3 of 60 scanned sources (`richness-exposure-count.md`); almost none in F01-F23; real LP updates are full of this shape. The ten-pair probe (`07ad532`) used thin mark lines Claude specified, so its clean EDGE_CASE reflects construction, not product. **Sizing (do not quote 25% as the defect size):** absolute passage NONE rate is 25% under BOTH R3a and R10 (`passage-correspondence-baseline.md` `4caf1d1`, 50/200 figure-bearing pairs each arm). R10 did not introduce it. Of those 50 R10 NONE cases, 41 have no better figure-bearing sentence in that source (correct behaviour when the figures are absent). Real attention failures are 3 to 9 of 200 figure-bearing pairs (~1.5% to 4.5%): 3 with an unused ALL-figure sentence, plus up to 6 SOME-only (includes F18_S7). Instance accepted as cost: **B117** F18_S7. Live instance still open: F93_S2. **B48 landmine:** do not add MOIC multiples to the magnitude backstop without a corresponding-passage check; that would code-force F93_S2-style false reds. **Correction (2026-08-29): F01_S11 is not an instance of this row, and this row's sizing does not rest on it.** F01_S11 (the procedural closer) was investigated and is **not** a passage-selection miss: the deciding source line WAS among the passages Stage 2 saw, and it was misclassified anyway. It is a classification fault, and specifically the no-label-for-a-non-claim case recorded as **B128**. The sizing above stands unchanged — it rests on `passage-correspondence-baseline.md` (`4caf1d1`) and names F18_S7 (**B117**) and F93_S2, not F01_S11. Recorded so the case is not counted twice. **Correction (2026-08-30): F02:S6 is not an instance of this row.** B115 is figure correspondence, R10 SOME. B123 is figure ALL and incomplete for a NAME. Do not fold them. F02 is figure-corresponding in the same baseline (F02 ALL=5 NONE=0). The miss is a **name** in a sibling paragraph, then an overlay that treats the thin Stage 2 snippet as the whole source. That is **B123**, a sibling, not this figure-correspondence row. Do not file it here and do not add it to the 1.5% to 4.5% band. | richness probe; exposure; correspondence baseline `4caf1d1`; R10; F01_S11 read 2026-08-29; B123 re-file 2026-08-30; B123 accepted-not-fixed 2026-08-31 | M | AFTER | Resized. Not next. Client sharp end: rich-mark. F01_S11 excluded. F02:S6 is B123 (figure ALL, name incomplete). |
| B118 | **OPEN UNCERTAINTY (not a known regression). Meridian draft statement 7.** "On balance, we believe the fund should deliver returns broadly in line with its predecessor and we recommend the commitment." R3a production verify: not_supported / high (`72f61aa`). R10 production verify: supported_partial / moderate (`baed6ed`). One run against one run; noise floor around 9%; unscored. Not nothing. Source: Halden `meridian_source.txt` (eval-ablation). | R3a/R10 production verifies | L | AFTER | Watch. Do not treat as R10 regression. |
| B120 | **Rewrite coverage incomplete on Suggest. OPEN. Watch.** Suggest1 (`25ae739`) left the lead-commitment sentence untouched despite a flagged partial gap (`suggest-after-r10.md`). Honesty repair (**B119**) makes the marker tell the truth about that; it does not make the rewriter address the gap. Separate from marker honesty. Source: eval-ablation Meridian / `meridian_source.txt`. **Re-scope needed after the silence reversal (2026-08-29), not adjudicated.** The product no longer edits on silence at all, so leaving a sentence untouched is now correct behaviour wherever the gap rested on a source saying nothing. Whether the lead-commitment gap in this row was of that kind, or one a source actually spoke to, was not determined here. Re-measure before treating this as an open coverage defect. | Suggest measure `25ae739`; honesty diagnosis `b487e61`; silence reversal 2026-08-29 | M | AFTER | Coverage, not honesty. Needs re-measuring against the silence rule. |
| B121 | **Judgement turned into fact recital on Suggest. OPEN. House-voice question, not a defect.** Suggest1 replaced "in our view, genuinely exceptional" with sourced 2.4x / 21% / 17 exits from `eval-ablation/meridian_source.txt`. Not fabrication. Ben's call whether that is the right soft of evaluation. Do not treat as a marker or honesty bug. | Suggest measure `25ae739`; honesty diagnosis `b487e61` | L | AFTER | House voice. Ben's call. |
| B124 | **Per-request author identity. Every tenant on a deployment shares one author identity.** `isAuthoringOrganisationName` / `resolveAuthoringOrganisationName` (`lib/qc/first-person-actor.mjs`) resolve from the **`AUTHORING_ORGANISATION` environment variable**, which is a per-deployment global, rather than from the per-request `authoringOrganisation` that `api/analyse-statements.js` already parses and threads. Invisible today because there is one client. Wrong the moment a second client shares a deployment. Cross-ref **B95** (request supply route exists and has never executed) and **B96** (the deployed value is a fixture name). | author-anchor narrowing 2026-08-29 | L | LAUNCH | Latent multi-tenant fault. Cross-ref **B95**, **B96**. |
| B125 | **The drift alarm is too noisy to read.** OpenAI rotates `system_fingerprint` roughly daily, so `[model-drift]` warns near-constantly and is now ignored, which is worse than not having it. Downgrade to informational and treat the fingerprint as **metadata**, not a gate. What would actually be useful in its place is a **small periodic corpus subset** checked against the promoted baseline in `scripts/diagnostic/fingerprint-manifest.json`. Cross-ref Closed **B100c** (the fingerprint is OpenAI's serving snapshot, not a hash of prompt or source). **Confirm before anyone changes this alarm (2026-08-30).** It has two call sites, and they do not read the same thing. Review (`api/analyse-statements.js`): `stage=stage2`, fingerprints = non-empty `systemFingerprint` strings collected from Stage 2 `gpt-4o-2024-08-06` matches (`collectStage2Fingerprints`). Those populate (`fp_*`); this is the path whose daily rotation makes `[model-drift]` noisy. Suggest (`api/suggest-revision.js`): `stage=reviser`, fingerprints from `systemFingerprintFromCompletion` on `writing-rewrite` (`gpt-5.1-2025-11-13`). Null is skipped (`if (fingerprint)`); an empty set makes `reportModelDrift` return silent immediately (`current.length === 0`). Diagnostic confirmation: `system_fingerprint` is null on every gpt-5.1 Suggest call (`fingerprint-forensics.md`). The Suggest path therefore does not alarm today. Confirm this split before changing the alarm. | corpus re-baseline `3122708`; `fingerprint-forensics.md` `8a1edce` | L | AFTER | Confirm what it reads. Review watches gpt-4o Stage 2 fps; Suggest is silent on null gpt-5.1 fps. |
| B126 | **Two piles. Do not mix them.** Apply slice shipped 2026-09-02; do not delete either pile as a side effect of other work. **Pile 1 — retired revision machinery, reachable but disabled.** Three things are now dead in effect: deterministic whole-sentence removal, the author-statement exemption, and the no-deletion-without-a-marker invariant. **Correction to the obvious framing, verified 2026-08-29:** the files are **not unreferenced**, so this is not a three-file delete. `applyDeterministicUnsupportedRemoval` is still **called** at `build-revision-prompt.mjs:989`, gated on `opts.deterministicUnsupportedRemoval`, which `api/suggest-revision.js` passes as `false` at both call sites; `authorStatementExemption` is still called from `pr9-deterministic-unsupported-removal.mjs` and `revise-stage1.mjs`; and `pr9-note-what-from-diff.mjs` and `pr9-marker-honesty.mjs` still import `findStatementTextInDraft` / `tightestUnsupportedSpans` / `isAuthorStatementKeptNote` from those modules for the **live** marker path. Removing them means unpicking call sites and the option flag, and rehoming the still-used helpers. The in-code comment in `api/suggest-revision.js` calling the module "unreferenced" is itself inaccurate. Nothing waits on this: the per-statement path is abandoned (**B130**) and Stage 1 stays behind its flag. **Pile 2 — Writing workspace, nothing routes to it. Scoped 2026-09-02 from the Review surface map.** `/`, `/writing`, `/quality`, `/quality-review` all `Navigate` to `/assess`. Dead host and its version functions: `FocusWorkspace` in `App.jsx` (defined, no route), `DraftOutputPanel`, `RewritePanel`, `FocusLeftRail` (AppLayout skips it on assess), unused `QualityPageLayout` import, `DraftContextPanel` (only that layout). `injectManualDraft`, `saveWorkingDraftToSession`, `saveManualEditAsNewVersion` in `useDraftState.jsx` — only `DraftOutputPanel` called them. Writing `VersionsPanel` branch in `VersionHistoryDrawer` (`onAssess` is false). Chrome `ExportModal` in `AppShell` (Assess mounts its own). Outer `DraftStateProvider` still wraps the app and is shadowed on `/assess` by `AssessStateProvider`. **Landmine: the old Suggest-revision modal save handler `handleSaveSuggestRevisionAsVersion` in `StatementAnalysisPanel.jsx` still exists and still calls `setDraftText` / `captureAssessVersion`. No button opens the modal. Leave it strictly alone until this ticket. Do not revive it, do not wire near it, do not delete it as a side effect of other work.** | silence reversal 2026-08-29; code read 2026-08-29; surface map 2026-09-02 | L | AFTER | Two piles. Pile 2 is the Writing workspace. Suggest save handler is the live landmine. |
| B131 | **Review emits two `suggestedDirection` strings on one statement that cannot both be satisfied. OPEN.** Contradictory directive pair on one statement. **1 of 2** multi-directive statements in stored review output (`suggest-after-r10-review1.json` S1; also **1 of 36** statements, `b122-contradiction-count.mjs`, `b122-residual-adjudication.md`). Production occurrence not established. One concern says delete `'genuinely exceptional'`; the other says replace the sentence with a destination that contains `'genuinely exceptional'`. Suggest made the conservative voice-only edit and was scored as missing both. Cross-link **Pr16**: Review does not check its own output against itself. Do not treat those six Suggest cells as a follow-rate gap. Specified as R1. Child of **B136**; do not bundle with **B132** or **B138**. | B122 residual `cf32a76` | M | AFTER | Stored output only. Production occurrence not established. Cross-ref **Pr16**. Child of B136. |
| B133 | **`note_quote` spans truncate on apostrophes. OPEN. Cosmetic.** `deriveConcernSpan` in `lib/qc/editorial-compliance-reviewer.mjs` L730-750 via `extractQuotedSnippets`. A possessive such as `'The team's ...'` closes at the apostrophe, so the highlight target becomes `'The team'`. Evidence spans are a **separate path** (`buildUnsupportedSpans`) and are unaffected. Sized: **1 of 17** `note_quote` spans (`b122-notequote-sizing.md`). Do not fix as a Suggest or evidence defect. Specified as R3. | B122 note_quote sizing `4f88ad2` | L | AFTER | Highlight only. Evidence spans separate. |
| B135 | **Measured stability floor for Suggest. OPEN. Not urgent; nothing blocked on it.** The same prompt, byte for byte, same seeds, same dated model (`gpt-5.1-2025-11-13`), produced a different DECISION about whether to edit a sentence 78 minutes apart: r10-review1 S7 left first person (b122-rescore 05:48Z, 0 of 3) then rewrote it with the actor present (b134 reference arm 07:06Z, 3 of 3). `system_fingerprint` is NULL on every gpt-5.1 Suggest call (`fingerprint-forensics.md`), so provider snapshot rotation and model variance cannot be separated. Either way, two Suggest runs at different times cannot be certified comparable. Design sketched in `scripts/diagnostic/revise/conflict-vs-stability.md` section 4d. About $0.70 for a week. Supersedes the B98 note-wording framing for this measurement. | `reference-arm-contradiction.md` `117cfeb`; `fingerprint-forensics.md` `8a1edce`; sketch `11dfb4a` | L | AFTER | Not urgent. Calendar, not an A/B. |
| B136 | **Design principle, UNSCOPED.** A style directive should never reach Suggest on a card where the evidence rule forbids the edit it asks for. Either the rule permits the edit, or Review should not raise it. Parent of **B131** (contradictory style pair on a partial), **B132** (worked-example over-apply on a coordinated verb; **DEFERRED 2026-08-31**, fixture-only), and **B138** (live silence breach; **shipped 2026-08-30**, `b138-silent-card-withhold`). Do not bundle the children; you lose the ability to attribute a regression. Cross-link **B134** (silence vs first-person; now shipped). Remaining flap cell: r10-review1 S1 (**B131**). Evidence (`conflict-vs-stability.md`): 6 CLEAN directives stable across four runs, and every cell that moved had a style instruction fighting an evidence rule. Refinement: contradictory style rules alone did NOT flap (condition-b S1); contradictory style PLUS an evidence keep-and-flag did. | `conflict-vs-stability.md` `11dfb4a` | L | AFTER | Unscoped. Principle, not a spec. Do not bundle children. |
| B137 | **HYGIENE. `ANCHOR_ENTITY_RE` fragments all-caps names. OPEN. Net negative alone, AND it does not combine.** Regex `/\b[A-Z][a-z]+(?:\s+[A-Z][a-z]+)*\b/g` at `lib/qc/evidence-relationship.mjs` L172 requires a lowercase letter after a capital, so "CPP" does not match and "CPP Investments" becomes the bare word "Investments". Real defect. Widening the regex does not rescue any **B123** overlay approach and adds the F19:S8 regression (`portfolio` to `portfolio esg`). Do not treat B137 and B123 as separable. If ever done, copy the **B64** caution: admit all-caps acronyms of two or more letters without letting EUR / EBITDA / AI / IV become the corroboration entity, and without changing the pre-filter. | B123 window replay 2026-08-30; overlay pass 2026-08-31; B64 `review-claim-anchors` | L | AFTER | Hygiene. Net negative alone and does not combine. Not separable from B123. |
| B141 | **False "Revised this span." from a LEFT leak. OPEN.** Out of **B140**'s scope on purpose. B140 confined the KEPT honesty region at the right (neighbour-sentence first-token leak). A leak to the LEFT still produces a false "Revised this span." on an unchanged marker span. Sized: **2 of 8** stored rows. Operator observation: not seen across three production runs of the Meridian fixture, 2026-08-30 and 2026-08-31. The whole-draft substring test that would catch it is REJECTED: it silences genuine contradictions on short spans. Do not revive that test. Do not stretch the B140 clip to reach these. | B140 ship `c469fac`; eight-row replay 2026-08-31; operator observation 2026-08-31 | M | AFTER | Left leak. Whole-draft substring test rejected. Not seen on three Meridian production runs (operator observation). |
| B142 | **Honesty repair replaces the model's specific note with a generic one. OPEN. Cosmetic, seen live.** The repair fires correctly; only its replacement text is wrong. Same function as B140 (`applyMarkerHonestyCheck`). Do not touch while B140 is one run old. | live Meridian 2026-08-31; B140 `c469fac` | L | AFTER | Cosmetic. Repair correct; replacement text wrong. Same function as B140. Do not touch yet. |
| B143 | **"Run until clean" does not converge. RULED 2026-08-31: change nothing in Review.** Raising every instance at once makes cards noisier; capping the rule per sentence hides real problems. The chain only feels wrong because there is no way to stop it. Answer shipped as **Pr17** (2026-09-01–02; no action-list tag; residual **B151**). This row stays ruled, not closed. Operator ruling 2026-08-31. | live Meridian x3 2026-08-31; operator ruling 2026-08-31 | M | AFTER | Ruled. Change nothing in Review. Answer shipped as Pr17. |
| B145 | **A house-style serial comma from Suggest flips the Stage 1b compound prefilter and roughly doubles Stage 2 spend for that sentence. OPEN. Cost lever.** No correctness effect observed. Distinct from **B144**. | live Meridian 2026-08-31; `isCompoundCandidate` | L | AFTER | Cost lever. No correctness effect observed. |
| B146 | **The corroboration anchor is frequently a function word (`the`, `we`, `if`, `in`), so the overlay's name check often decides nothing. OPEN. Free to size.** 108 of 355 anchors are absent from the quoted snippet. Do not propose a fix in the same pass. | overlay pass 2026-08-31; `corroborationAnchor` | L | AFTER | Free to size. Do not propose a fix in this pass. |
| B150 | **CONSTRAINT. Autosave will persist a rebuilt draft if the working text is written before Save. Not a defect in autosave.** The live surface is `/assess` (Review). `draftText` is the live draft and is in the Neon snapshot (`useReviewStatePersistence`, 2s debounce plus flush on tab hide). `currentVersion` is synthetic from that text, so there is no separate original to restore. Apply must hold the rebuilt string beside the live draft; Save captures it as a version; Cancel discards the overlay. Do not implement Cancel as restore-from-`currentVersion.text`. **Obeyed 2026-09-02.** Decisions live in panel `useState`, not in `REVIEW_STATE_SNAPSHOT_FIELDS`. The rebuilt string is not session state: it is computed at Save from the analysed snapshot, appended via `captureAssessVersion`, then written once with `setDraftText`. Discarding resets decisions and never writes the textarea. Refresh loses decisions on purpose; surviving a refresh is a separate choice. | apply/save proposal 2026-09-02; surface map 2026-09-02; apply slice 2026-09-02 | H | AFTER | Obeyed. Do not "fix" autosave. Decisions die on refresh and on Review re-run. |
| B153 | **WORDING PASS still OPEN and frozen. Acknowledgement on conflict-free partials SLICED 2026-09-10, not a row close.** Evidence `partial_no_edit` and editorial/compliance `partial_policy` replace the old silence claim on those cards (`lib/revise-actions/sort.mjs`). `statementIsSilent` is unchanged, so disposition stays ACKNOWLEDGE. True-silence `silence_no_edit` / `policy_forbids` unchanged. Remaining freeze: header pill, Save count (`Save with no changes` / `Save with 1 change` / `Save with N changes`), `Expand all`, `Collapse all`, `Proposed change pending`. **Carve-out 2026-09-11:** the proposed-sentence caption is now `Proposed change` (card-chrome slice); that one frozen string is deliberately renamed here and is no longer in the freeze. **Annotation 2026-09-11:** per-card expand/collapse (**B177**) will need a further carve-out of those two strings, or its own wording; do not spend the freeze here. `Bulk accept` and `Bulk reject` stay wrong until that pass. Standard: `ai/AI_OPERATING_MANUAL.md` QC Output Language Standard. Sequenced after the Review group (**B155**, **B154**); operator ranking 2026-09-02. Do not mark this row shipped. | polish batch 2026-09-02; silence copy 2026-09-05; partial ack slice 2026-09-10; caption carve-out 2026-09-11; per-card expand filed 2026-09-11 | M | AFTER | Wording pass still open. Caption carve-out only. Do not rename Bulk accept / Bulk reject here. |
| B156 | **OPEN. Scoped 2026-09-03. Not built.** Fifth evidence **display** state: a source spoke and gave a different period or value. Not a conflict. Do not present as one. Stage 2 classification stays `no_support`. Do not move `applyPeriodGateBackstop`. Do not change the Stage 2 pin. Copy `preBackstopClassification` and `periodAssessment` onto the public card (today they die at `lib/qc/pipeline-v4/index.mjs` L364-377). Stage 4 should still emit the competing excerpt. Stage 5 must not say no source addresses. Unlock of Implement Changes is a **later slice** and needs an exact-quote locator (29 August rails: only the source's stated value, only the addressed element, never a surrounding rewrite, no proposal if it cannot be quoted exactly). Wording is Ben's; fifth freeze carve-out on **B153**, not invented here. Hash-pinned R10 blast scores `classification`, not `displayVerdict`. | operator 2026-09-03; period-gate-discard 2026-09-03 | H | LAUNCH | Display honesty first. Do not move the gate. Do not file as B154. |
| B157 | **OPEN. Filing only. Not built.** Mechanical style rules have no raise backstop. The filters in `STYLE_RULE_DETERMINISTIC_FILTERS` (`lib/qc/editorial-compliance-reviewer.mjs`) can only DROP a false positive; they never RAISE a miss. If the model does not flag the rule, nothing does. **Annotation 2026-09-12 (B180):** `em_dash` and `smart_quotes` now have DROP referees (character presence on `statementText`). That does not close this row: still no RAISE. A flag can still be silently lost between identical runs. The product's own re-run banner uses an em dash (`src/modules/assess/AssessModule.jsx` L782). This is not a currency-referee flap. | referee audit 2026-09-05 | H | AFTER | Do not build here. Still no RAISE. |
| B158 | **SECOND EXCERPT GAP. OPEN.** 11 conflict cards on `runs/evidence-pass-lift-1` carry their quote in `primaryExcerpt` with `conflictExcerpt` empty. Span-only empty excerpt is shipped (`3214646`). This remaining hole is the dedicated conflict field empty while the competing passage sits in the primary slot. Fix before the sources drawer is built. Distinct from **B147** (`conflictValues` still null). **Annotation 2026-09-10:** live F18 Review (`live-2026-09-10-f18-review.json`) has `primaryExcerpt` as a string on 10/10 cards and `conflictExcerpt` as an object on 1/10 (null on 9). Stringify `conflictExcerpt` to the same wire type Design 2 already uses for `primaryExcerpt` (passage string or null) so a card stops carrying two excerpt fields in two shapes. Shape fix is not the empty-slot hole; do both before the drawer. | accuracy lift-1 2026-09-08; live F18 Review 2026-09-10 | H | LAUNCH | Fix before the drawer. |
| B159 | **QUALIFIER OVERCALL. OPEN.** "gross IRR" against an unqualified IRR reads as a contradiction. Ben P (Ruling 2), pipeline X. Needs a Stage 2 prompt change, which moves the pinned hash and re-baselines the corpus. Waits for the expanded corpus and a held-out split. Exhibit: F13 "2.6x MOIC and 21% gross IRR". | accuracy open questions 2026-09-08 | M | AFTER | Hash-pinned. Do not change Stage 2 against this pack. |
| B160 | **F05 HALDEN. OPEN.** Invented actor "Halden Group will support continued growth". No contradicting passage exists to find. Rubric: an invented actor reads as an omission. Remaining Group A catch miss. Cover in the expanded corpus. | accuracy open questions 2026-09-08 | M | AFTER | Rubric. Not a passage-selection miss. |
| B161 | **F13 EBITDA matcher. OPEN.** Widened matcher returns the 11.1% passage and never the "approximately 13%" line. The wider window is not reliably wider. Remaining Group A catch miss. Intra-source reducer cannot vote a span it never received. Cover in the expanded corpus. | accuracy open questions 2026-09-08 | M | AFTER | Matcher, not reducer. |
| B163 | **INGESTION. OPEN.** 7 Sep 11-file check is superseded as a planning number by this pass. Report `scripts/diagnostic/delivery-check/b163-ingestion-reality.md`. Of 20 real in-scope PDFs, 13 produced a Review a reader could trust. 19/20 extract on production inside 300 s; d13 JPM letter `fetch failed` at 301388 ms. The 389 KB 3i PR (d01) that timed out at 60 s now finishes in 207727 ms. Original four that never arrived: three HTTP 413 `FUNCTION_PAYLOAD_TOO_LARGE`, one (`pg-annual-results-2024.pdf`) under 4.5 MB and blocked only by the 4.2 MB client guard. Extract wall tracks page count / Workiva-InDesign, not bytes. Distinct from **B79**. **B164** does not speak on 413, timeout, or `PDF_INLINE_TEXT_NOT_ALLOWED`. | B163 diagnostic 2026-09-21 | H | LAUNCH | Real PDFs. Timeouts and tables, not just size. |
| B165 | **SPAN-ONLY EMPTY PRIMARY. OPEN. Out of scope for the Design 2 card-face quote.** 23 of 261 lift-1 cards (`runs/evidence-pass-lift-1`: 13 `supported_full`, 10 `supported_partial`) carry a nonempty `supportSpans` passage and an empty `primaryExcerpt`. The drawer can highlight the span; the card face still has no quote. Distinct from **B158** (dedicated `conflictExcerpt` empty on 11 conflict cards) and from **B147** (`conflictValues` still null). Fixing it means putting `supportSpans` on the card face, which is a different contract. Do not build it in the primaryExcerpt stringify/paint slice. | Design 2 C6 recount 2026-09-10 | M | LAUNCH | Out of scope. Do not fold into B158. |
| B166 | **COMPETING PROPOSALS. OPEN. Filing only.** Evidence and editorial on one sentence can propose incompatible actions. Live F18 Review: **4** of 10 cards (S0, S2, S5, S7) are `conflict` plus a nonempty editorial `suggestedDirection`. S5 is update-the-figure versus delete-the-sentence. Apply already refuses a double write (`plannedWrites` drops the statement if `list.length !== 1`); bulk accept leaves colliding rows unset. Harm is confusion on the card, not a silent merge. Duplication judge keeps voice/delete as INDEPENDENT. **Annotation 2026-09-11:** later live F18-S3 now also has this shape (evidence 380→412 versus editorial 240'000→240,000). That second pair is **B176**; do not close this row into that one. Do not fold into Design A or B. | proposal-layer diagnostic 2026-09-10; live F18 Review; inverted separator 2026-09-11 | M | LAUNCH | Confusion, not a double write. |
| B167 | **EXCERPT TRUNCATION MARKERS. OPEN. Filing only.** Card-face quotes terminate three ways. Intended: Stage 4 sentence-boundary cap (`trimExcerptTo300` in `lib/qc/pipeline-v4/stage4-select-excerpts.mjs`) plus ASCII `...` after a cut, and no marker when the passage is under the cap. Unintended glyph: four ASCII dots (`....`) when that `...` is stacked on a sentence that already ended. Second alphabet: Stage 2 `trimPassageToLimit` (`stage2-match-sources.mjs`) appends Unicode `…`. Live F18 Review: 2 four-dot, 1 period plus `…`, 1 no marker (S2 ends on `")`), 6 single period. Design 2 paints the string verbatim. Do not retune Stage 2 prompt. | proposal-layer diagnostic 2026-09-10; live F18 Review | L | AFTER | Cosmetic. Stage 4 marker is the card-facing intent. |
| B170 | **DESIGN B RESIDUE. OPEN.** Shipped the one draft-presence rule for writing the configured house name, and the placeholder strip so example wording cannot reach a reader. Review still does not supply the organisation in the request (**B95**). One environment identity is still shared per deployment (**B124**). The gate does not remove the configured env value (**B96**). Do not close **B95**, **B96**, **B124**, or **B155**. | Design B build 2026-09-11; live F18 Review | M | AFTER | Presence check not loosened. Editorial not-identified claim unchanged. |
| B171 | **USER-SUPPLIED AUTHORING ORGANISATION. OPEN. Filing only.** Ben 2026-09-11: the house name should be supplied by the user, not by a server setting. Account settings (do not exist yet) or a pre-populated input field. The request-body path already exists and has never been exercised (**B95**). Environment value is one identity per deployment (**B124**). After `202b357` a wrong or missing value is no longer dangerous but silently does nothing. When built, draft-presence changes job from gate to sanity check: if the user states an organisation the draft never mentions, tell them rather than acting silently. Do not close **B95**, **B96**, **B124**, or **B170**. | operator 2026-09-11; quantity-matching design | M | LAUNCH | Record only. Not this quantity-matching build. |
| B172 | **UNSUPPORTED DEPENDENTS AS A FINDING. OPEN.** Ben 2026-09-11: when a source contradicts a figure that other figures in the same sentence are derived from, the product knows those dependents are now unsupported and currently says nothing. **Line on the existing conflict finding shipped** (F18-S7). Confirming-passage clause **B175** now landed. Same class of silence as **Pr16**; do not duplicate. Do not close **Pr16**. | operator 2026-09-11; explain-the-decision design 2026-09-11; explanation-slot build 2026-09-11; confirming-passage build 2026-09-11 | M | AFTER | Line on the existing finding. Pr16 remains. |
| B174 | **EXPLAIN THE DECISION ON THE CARD. OPEN.** Ben 2026-09-11: the card should say why a change is or is not proposed. Explanation slot shipped; confirming-passage clause **B175** landed; **B173** now names both figures when two sources disagree. Do not close **B159**, **B171**, **B172**, or **Pr16**. | operator 2026-09-11; explain-the-decision design 2026-09-11; explanation-slot build 2026-09-11; confirming-passage build 2026-09-11; B173 2026-09-13 | M | AFTER | Slot, confirming clause, and cross-document ask shipped. |
| B178 | **EDITORIAL REVIEWER IS BLIND TO SOURCES. SHIPPED 2026-09-12 as the input removal; row stays OPEN for the given-up capability.** Cause of **B176**: the source was in the editorial payload at all. Four separate prompt passages existed only to tell the model to ignore it. Compliance already reviewed blind (`buildComplianceUserPayload` has no evidence excerpt). The fix removes the input rather than adding a fifth instruction: EVIDENCE EXCERPT and SOURCE EVIDENCE blocks deleted from combined and legacy editorial payloads; editorial/style fidelity gates passed `null` so a concern quoting a source figure drops; `overreach_unsupported_causal` and `underreach_hedging` rewritten as draft-only. **Given up:** editorial can no longer observe that a draft is hedged where the source is clear, or that a causal claim lacks source support. Both are now Evidence matters. Tests `tests/b178-editorial-source-blind.test.mjs`. A/B `scripts/diagnostic/delivery-check/b178-source-removal-ab-result.md` (B180 confirmation appended). **B176** closed by B178 at the payload and **B180** at the filter (F1 met). Do not close **B157**, **B133**, or **B166**. | B176 A/B 2026-09-12; operator 2026-09-12 | H | AFTER | Input removed. Given-up editorial-vs-source capability remains. |
| B182 | **OPEN. Filing only. Not built.** `first_person_plural` has failed `firedOnViolation` on its own violation fixture at every measurement on 2026-09-11 and 2026-09-12, across four harness arms, and again at B181 HEAD on 2026-09-13. Consistent, not noise. Not caused by **B176**, **B178**, **B180**, or **B181**. Cause not investigated. **Annotation 2026-09-15:** the editorial first-person rule fired on fixture 18 live (opening sentence, EUR 158 million card, EUR 95 million card, In summary card; `live-2026-09-15-f18-run.md`). Recheck whether that is this rule: **B197**. **Annotation 2026-09-18:** **B197** answered. Those live hits were `voice_consistency` (**B219**). This row is still the style-harness miss on reporting commentary. | style-guide harness 2026-09-11–13 | M | AFTER | Record only. B197 answered. |
| B196 | **THE FULL END-TO-END DEFECT SWEEP IN PRODUCTION. SWEPT 2026-09-18 `4b1f530`.** Production Assess, frontend HEAD `5c20a02` (Build: 5c20a02), backend HEAD `25e1ea6`. Report `scripts/diagnostic/delivery-check/live-2026-09-18-b196-sweep.md`; screenshots `scripts/diagnostic/delivery-check/b196-2026-09-18/`. Pins: P1 PASS, P2 PASS, P3 PASS, P4 PASS, P5 PASS, P6 FAIL. Findings by tag: BROKEN 4, INCONSISTENT 6, UNCLEAR WORDING 2. Filed as **B208** to **B217**. Sweep itself is done. Launch is RECORD, not an open launch task. | operator 2026-09-15; ran 2026-09-18; B305 | H | RECORD | Production, screenshots, tagged findings. Not a usability review. |
| B199 | **The "Single paragraph" shape rule in the public templates now sits oddly against a writer-set word limit.** | B188 2026-09-15 | L | AFTER | Shape rule vs writer-set ceiling. |
| B223 | **OPEN. Filing only.** `first_person_plural` `applies_to` is still `reporting_commentary` only. **B220** moved press release to the third-person house-voice list, so first-person in a press-release body is now the same class of fault, but the style rule will not fire on it. Do not close **B182**. | B220 2026-09-18 | M | AFTER | Widen applies_to to press_release. Not built. |
| B224 | **OPEN. Filing only.** Press-release body is third person; first person is correct only inside an attributed quotation. **B220** stated that as prompt instruction (`reviewerNoteByOutput` and the writing line). There is no quote-versus-body detector, so a quoted "we" can still be flagged and a body "we" can still be missed. Do not build detection in a drive-by. | B220 D3 2026-09-18 | M | AFTER | Instruction only. Detector not built. |
| B231 | **OPEN. Filing only.** Card dead reads left after the face census: `claimType` never written; `displayMode === "confirmed"` / `supportState === "confirmed"` branches v4 never hits; `citationHovers` always `[]` so source chips do not render; `concernLevel` computed, shown in the export as evidence concern (**B233**), not on the card. Do not delete vacant keys in a drive-by. | dead-read census 2026-09-19 | L | AFTER | Hygiene. Face census was never filed as a row. |
| B236 | **OPEN. Filing only.** Reviewer assessment and constructive feedback are two model writers of overlapping prose. Nothing asserts they agree. **Annotation 2026-09-19:** the output pass compared the numbers those writers are given, not the model paragraphs. Still no observed contradiction. | claims inventory 2026-09-19; claim-disagreements 2026-09-19 | M | AFTER | S7. No observed contradiction in this pass. |
| B237 | **OPEN. Filing only.** `summaryBullets` returns at most 3 lines. A fourth class of problem is silent. The readiness badge still uses the full counts. | claims inventory 2026-09-19 | L | AFTER | Omission. Badge honest. |
| B238 | **OPEN. Filing only.** `recommendationDisplayText` / `getStatementVerdict` / `deriveAdvisoryLine` still run on every card, keyed off legacy `row.sourceCount` / `row.level`, and the string is never mounted. | claims inventory 2026-09-19 | L | AFTER | S5+S6. Do not close **B231**. |
| B239 | **OPEN. Filing only.** Evidence slug allowlist exists in three places: frontend `displayVerdictLabels.js`, backend `evidence-display-verdict.mjs` (badge, export, **B235** `evidenceCountClass`), and frontend `summariseReview.js` (point-of-use copy). Screen, file, and summary agree today. Recommendation: one shared module for the evidence slugs only. Editorial/compliance remain a different allowlist. Do not merge those in a drive-by. **B246** now fails if the two `summariseReview` copies disagree on readiness or counts. That is not the shared module. | claims inventory 2026-09-19; B235 2026-09-19; B246 2026-09-19 | L | AFTER | S7. Drift would split the badge from the summary. |
| B243 | **OPEN. Filing only.** The reviewer assessment still says "You'll need" in Assess context. Role mix. Out of scope for **B240**. Do not fix in a drive-by. | B226 report leftover; B240 D8 | L | AFTER | Assess copy. Not this covering-note row. |
| B248 | **OPEN. Filing only.** Stage 1 dropped the last sentence of the R6 153-word draft (`This note covers the March 2025 close.`). Cards cover 146 words. A reviewer can think that sentence was checked. Distinct from **B74** (bullet-marker strip). Do not close **B74**. **Annotation 2026-09-19:** pipeline-integrity live rerun reproduced it. Mechanism is `isClaim=false` then the L378 filter, not an LLM omit. Log: `dropped 1 non-claim statement(s)`. Class of silent drop closed as **B249** (disclose, do not override). This row is the one-sentence exhibit. | claim-disagreements 2026-09-19; `tests/fixtures/b247/r6-near-limit.json`; pipeline-integrity 2026-09-19 | M | AFTER | Missing card. Live draft still has the sentence. |
| B252 | **OPEN. Filing only.** Stage 1b `MAX_DECOMPOSED_SENTENCES=12` reverts extras undecomposed (`over_sentence_cap`). Parent sentence still gets a whole-sentence card. Measured 8 of 20 on the long draft. `console.warn` only. | pipeline-integrity 2026-09-19 `fc0e391` | M | AFTER | Inner claims never matched. Sentence still reviewed. |
| B253 | **OPEN. Filing only.** `WIDENED_SCOPE=supporting_pairs` skips single-pick `no_support` pairs. Extra passages on those pairs are never sought. Measured r6 8 of 20, compound 3 of 15. Cost control by design. No user report. | pipeline-integrity 2026-09-19 `fc0e391` | L | AFTER | Skip, not a dropped statement. |
| B254 | **SHIPPED 2026-09-19.** A failed Stage 5 call is not a finding. Empty `evidenceSummary` / `reasoningParagraph`, `commentaryNotReviewed: true`, export `Not checked.`, QRS `notChecked`. The four canned `Specific commentary is unavailable from the system` strings are gone. Backend `30d3b9a`. Frontend `9621ff2`. Tests `tests/b254-commentary-not-a-finding.test.mjs` plus a B245 corruption. Production proof: 186/186 real findings, 0 stand-in. Report `scripts/diagnostic/delivery-check/async-review-build.md`. Document-scale exhibit **B262** closed by the same commits. | pipeline-integrity; shopify-messy-full; this spec | H | RECORD | Miss is not checked. Stage 3 verdict unchanged. |
| B255 | **OPEN. Filing only.** Fidelity gate, deterministic style filters, and the editorial duplication judge drop concerns with console or Langfuse canary only. The card shows fewer notes. Judge fail keeps concerns (fail closed on suppress). | pipeline-integrity 2026-09-19 `fc0e391` | L | AFTER | Some drops are intentional. None are labelled as drops. |
| B256 | **OPEN. Filing only.** Fallback splitter caps at 40 candidates and splits at 240 chars. LLM Stage 1 has no count cap. Fallback-only. Did not fire (all three drafts used the LLM path). | pipeline-integrity 2026-09-19 `fc0e391` | L | AFTER | Silent slice if Stage 1 falls back. |
| B257 | **OPEN. Filing only.** On recovered PDF text the card statement keeps mid-sentence hard line breaks; the quoted passage flattens them to spaces. Offsets still cover the region via repair-normalise. Typed fixtures and both accuracy corpora never contain this class. CONFIRMED recovered Shopify slice: A1 17 exact, B1 2 exact / 30 whitespace. | fidelity-and-identity 2026-09-19 `050313c` | H | LAUNCH | Wrong kind of test text. Drawer region right; card bytes not the source slice. |
| B258 | **OPEN. Filing only.** Extractor turned curly apostrophes into ` n s` (`Shopify n s`, `That n s`). Stage 1 copied that onto the card as the statement. Exact to the extract, not to the PDF. | fidelity-and-identity 2026-09-19; `Shopify_text_longform_messy.pdf` | H | LAUNCH | Reviewer is shown words nobody typed. |
| B260 | **OPEN. Filing only.** Recovered memo headers: six lines dropped as non-claim with no user report; `Date: October 12, 2010` kept and marked not supported even though it is in the draft and the source. | fidelity-and-identity 2026-09-19 | M | AFTER | Headers are not in typed fixtures. |
| B261 | **OPEN. Filing only.** Stage 5 commentary expands `$7mm` to `7 million` (and `$132mm` to `132 million`). Sampled from recovered-review prose fields against draft plus sources. Not on the statement face. | fidelity-and-identity 2026-09-19 | L | AFTER | Figure wording in the note, not the claim. |
| B262 | **SHIPPED 2026-09-19.** Same commits as **B254**: backend `30d3b9a`, frontend `9621ff2`. Production comparable run: Stage 5 186/186, 0 stand-in. Fixture after `tests/fixtures/b247/shopify-messy-full-after.json`. Before picture kept at `shopify-messy-full.json`. Tests `tests/b254-commentary-not-a-finding.test.mjs`. Report `scripts/diagnostic/delivery-check/async-review-build.md`. | shopify-messy-full 2026-09-19; this spec | H | RECORD | Document-scale canned column is gone. |
| B263 | **SHIPPED 2026-09-19 `1d9df5e` tag `b263-function-duration-300`.** `vercel.json` `api/*.js` `maxDuration` 60 to 300. Deploy Ready. Production comparable Review 81898 ms, HTTP 200. Did not raise to 800. | shopify-messy-full 2026-09-19; this spec | H | RECORD | 3700-word memo now fits. |
| B264 | **OPEN. Filing only.** 26 cards quote the B1 stub sentence `The firm is evaluating an investment of up to $7,000,000.` as `primaryExcerpt`, including `Date: October 12, 2010` marked Conflicting. 20 of those have no locatable `supportSpans`. Drawer cannot highlight. 158 cards have no quote. Related **B259**. **Annotation 2026-09-20:** **B259** now hides a quote with no locatable span, so those 20 cards should show a hole rather than the B1 sentence. The 6 with spans can still quote B1. Classification unchanged. Do not close this row into B259. **Annotation 2026-09-21 (B305 3.7):** this is the B259 residue. The unrelated-document quote fell from 26 cards to 6. The six remaining show an honest hole rather than another document's words only where the span was unlocatable. The cure for the six spanned B1 quotes is the span work and needs real documents. No new row. | shopify-messy-full 2026-09-19 | H | LAUNCH | Wrong sentence on the card. Locate gate hides the unspanned quotes. |
| B265 | **SHIPPED 2026-09-19 `3fe9348`.** Stage 5 and Stage 6 use `mapPool`. Stage 5 stays at 24, the Stage 2 cap. **B268** later set Stage 6 to 4. Shared helper `lib/qc/map-pool.mjs`. Tests `tests/b265-stage-concurrency-pool.test.mjs`. | this spec | H | RECORD | Peak Stage 6 in-flight is now 8 (two calls per statement at cap 4). |
| B266 | **SHIPPED 2026-09-19 `0ffce91`.** `callLLM` retries 429 with the server delay plus jitter. 4 attempts, 2000 ms cap. OpenAI SDK `maxRetries: 0`. Tests `tests/b266-rate-limit-retry.test.mjs`. | this spec | H | RECORD | Belt behind the pool. Stage 5 no longer 429d. Stage 6 at document scale is **B268**. |
| B267 | **SHIPPED 2026-09-19 `6c20b67`.** `EXTRACTION_TIMEOUT_MS` 60_000 to 300_000, tracking the Function cap. Tests `tests/b267-extraction-timeout.test.mjs`. Not exercised by the Shopify proof (text already extracted). | this spec | H | RECORD | Copy of the cap, on purpose. |
| B268 | **SHIPPED 2026-09-20 `badb55e` tag `b268-stage6-pool-4`.** `STAGE6_CONCURRENCY` 24 to 4. Stage 2 and Stage 5 stay 24. Named try 8 still lost the last 39 checks on this memo. Pool 4: editorial 186/186, compliance 186/186, wall 123368 ms, list USD 11.1897. Honest `not_reviewed` path unchanged. Tests `tests/b268-stage6-pool.test.mjs`. Report `scripts/diagnostic/delivery-check/b268-stage6-pool.md`. **The ~7400-word stop was a stub-source hypothesis. Replaced by B277 measurement: about 3700 words with a realistic source.** | shopify-messy-full 2026-09-20 `4b8de172-d062-49ac-9ebd-b2b4fe1aab49` | H | RECORD | Missing checks closed. Longer wait. |
| B270 | **SHIPPED 2026-09-20 `6efe866`.** Stage-replay harness: one QC stage from a recorded review payload, no Stage 1/2/5. `scripts/diagnostic/stage-replay/`. Tests `tests/b270-stage-replay-harness.test.mjs`. Does not change the Stage 6 pool. | this spec | H | RECORD | Cheap A/B instrument. |
| B271 | **MEASURED AND NOT SHIPPED.** Dropping `FULL DRAFT` from the v4 editorial user payload. Keep CONTEXT BEFORE / AFTER. 40 succeeded editorial cards, spread across the Shopify memo, mixed 29 concern / 11 clean. Old-versus-old codesDiffer 13/40. Old-versus-new mean codesDiffer 26.25/40. Twelve stable shifts, eight of them `materiality` going clean. Small control (17 cards, 2001-char draft) old-versus-new 4.5/17 vs old-versus-old 5/17, zero stable shifts. Instrument is right; the long-memo findings moved. Do not drop the draft. Tightening the Stage 6 pool is the fallback for **B268** completions, not this cost save. | stage-replay 2026-09-20 | H | RECORD | Document-level rules need the draft. |
| B273 | **MEASURED AND NOT SHIPPED.** Honest editorial scope line (materiality, first-use jargon, throughout-voice may use the draft). Same 40-subset. Mean codesDiffer 24/40 vs floor 13/40. Four stable shifts. Line left alone. | this spec 2026-09-20 | H | RECORD | Honesty line is load-bearing. |
| B276 | **SHIPPED 2026-09-20 as B278.** Wait the TPM window until the request fits. The 4-attempt / 2s cap is gone. Bound: remaining Function time minus remaining TPM-floor work. Stamp `not_reviewed` if the bound is hit, never `clean`. Commit and tag: `8f15563` `b277-b278-scheduler-and-wait`. Tests `tests/b278-rate-limit-wait.test.mjs`. | rate-limit-and-redesign 2026-09-20; B277-B279 | H | RECORD | Completeness. Findings-identical. |
| B306 | **THE ENDPOINT ACCEPTS A REVIEW WITH EVERY CHECK OFF. OPEN. Filing only.** The Review modal refuses it (`ReviewOptionsPopup.jsx` `anyOn`; confirm disabled). `resolveReviewOptionsFromBody` does not. Reachable by API only today. Do not build in a drive-by. | B304 Part 4 | M | AFTER | Modal lock only. |
| B307 | **`joinCheckNames` EXISTS TWICE, ONCE PER REPO, WITH NO SHARED PACKAGE. OPEN. Filing only.** Backend `lib/qc/review-summary.mjs`. Frontend `src/modules/drafting/checkStateDisplay.js`. Acknowledged duplication that can drift. Do not invent a shared package in a drive-by. | B304 | L | AFTER | Two copies. |
| B308 | **A STAGE 5 MISS CAN LEAVE A FINDING WITH BLANK TEXT. OPEN. Filing only.** `evidenceSummary` and `reasoningParagraph` are left empty when `commentaryNotReviewed` is true. Latent: it did not fire on any recorded payload. | B301 Q4 | M | AFTER | Empty prose on a classified card. |
| B312 | **EXTRACTION TIME ON THE 300s PATH. MEASURED 2026-09-21.** Same eleven public PDFs as 7 September. 10 of 11 return text. 0 of 10 in under 10 seconds. Median 100.8 s. Slowest `3i-audited-fs-2025.pdf` 464.3 s, past the Function cap. Three slowest that reach the function sum 830.4 s before any model call. Report `scripts/diagnostic/extraction-check/REPORT-2026-09-21.md`. Product code unchanged except convert timings on the extraction object. | this spec | H | RECORD | Time is the verdict. PRODUCT-BLOCKING against the 10 s / 20 s proposal. |
| B313 | **PROMPT-CACHE RATE-LIMIT PROBE. MEASURED 2026-09-21.** Four sequential editorial calls on the recorded Shopify memo. Second-call cache 98.6% today's order, 99.0% reordered. remaining-tokens did not drop across either pair. Report `scripts/diagnostic/cache-probe/REPORT.md`. | this spec | -- | RECORD | Cache fires. TPM header did not move. |
| B314 | **TWO CONVERTS CAN OVERRUN THE FUNCTION. OPEN. Filing only.** `extractTextFromSource` runs `OfficeConverter.convert` "text" then "chunks", each with `EXTRACTION_TIMEOUT_MS` 300_000. Confirmed live: `3i-audited-fs-2025.pdf` 232 s + 232 s = 464 s. Production would kill the request with no partial result. Do not fix in a drive-by. | B312 C2 | H | LAUNCH | Observed. Not latent. |
| B315 | **EXTRACTION SPENDS THE REVIEW'S FUNCTION BUDGET. OPEN. Filing only.** `api/analyse-statements.js` calls `prepareUploadedSourcesForPipeline` on the Review request. Confirmed L172. Combined with B312 times, one real PDF can consume the 300 s before Stage 1. Do not fix in a drive-by. | B312 C3 | H | LAUNCH | Same request. |
| B316 | **SOURCES EXTRACT SEQUENTIALLY. OPEN. Filing only.** `prepareUploadedSourcesForPipeline` awaits each source in a for loop. Three slowest B79-passing files from B312 sum 830 s. Do not fix in a drive-by. | B312 C4 | H | LAUNCH | N sources cost the sum. |
| B317 | **SHIPPED 2026-09-22.** `QC_EXTRACT_STRUCTURE` default off. When off, skip the officeparser chunks convert and return the empty page/slide/sheet shape. Text convert unchanged. Tests `tests/extract-structure-flag.test.mjs`. Times `scripts/diagnostic/extraction-check/structure-flag-result.md`. | this spec | H | RECORD | About half the extract wall. Text byte-identical. |
| B318 | **EXTRACT-AT-UPLOAD GROUNDWORK. FILED 2026-09-22.** Read-only. What the frontend holds, what summarize-source returns, every contentBase64 guard, storage options, same-name re-upload, and which of the twenty B163 files would still miss 300 s with the structure flag off. Report `scripts/diagnostic/delivery-check/extract-at-upload-groundwork.md`. No product code. | this spec | -- | RECORD | Facts for the next spec. |
| B319 | **EXTRACTOR BAKE-OFF. MEASURED 2026-09-22.** officeparser text convert vs raw pdfjs item-order vs pdfjs y-then-x on the B163 twenty. Combined wall 755 s vs 1.4 s (530x). Gate 7 quotes 19/19 A, 13/19 B and C; the six misses are A's flatten and officeparser spaces. Scanned fixture empty on B/C with no warning. Report `scripts/diagnostic/extractor-bakeoff/REPORT.md`. No product code. | this spec | -- | RECORD | Fast path is not yet honest. |
| B320 | **B222. `.cursorrules` ship-report opener contradicted the spec template.** The paragraph "The report's FIRST line states" conflicted with `ai/SPEC_TEMPLATE.md` rule 5 and report rule 1 (open with the scoreboard). Replaced in both repos: the scoreboard's first row is the pushed sha and the verbatim verify:ship verdict. | B222 2026-09-22 | -- | RECORD | Contradiction removed. |

## 3. Process & governance

| ID  | Item | Source / context | Priority | Launch | Notes |
|-----|------|------------------|----------|--------|-------|
| P2  | Reproducibility test pattern for future model evals | R1.2.5.3 finding | M | AFTER | Standard for any future eval involving reasoning models or temperature > 0: run 2-3 times, check for stability, average results. Rule is now in `ai/AI_OPERATING_MANUAL.md` (B269). Row remains if a harness is still wanted. |
| P5  | Single technical opinion risk | userMemories | M | AFTER | Pre-existing. Claude is the only technical sounding board. No current cross-validation. Worth flagging if anything goes wrong. |
| P6  | Business backlog gap | userMemories | M | LAUNCH | Observability, cost modelling, pricing, second pilot user. Materially lighter than product backlog. Needs active attention. |
| P7  | Singapore government AI funding eligibility | userMemories | L | AFTER | Startup SG Tech POV primary, AI Accelerate parallel. Confirm prerequisites before applying. |
| P9  | **R7 test assets + diagnostics (retain).** Synthetic corpus `tests/extraction-corpus/` (MANIFEST + multi-format ground-truth files); real ingestion samples `scripts/diagnostic/r7-samples/`; diagnostics retained: `docs/R7_STAGE2_SHADOW_DIAGNOSTIC.md`, `docs/R7_OFFSET_EXTRACTION_DIAGNOSTIC.md`, `docs/R7_EXTRACTOR_SURVEY.md`, `docs/R7_EXTRACTOR_CHECK2_ATTRIBUTION.md`, `docs/R7_BUNDLE_ANALYZE_FINDINGS.md` (+ related stage-2/build-a docs). Not a product feature — keep for revalidation. | R7 2026-08 | L | AFTER | Asset inventory |
| P29 | **Delete superseded diagnostic reports.** `scripts/diagnostic/` carries many reports describing behaviour that no longer exists — most of the removal, soften-and-cut and author-exemption material was invalidated by the silence reversal on 2026-08-29. **Ben prefers deletion to marking.** A report that describes a mechanism the product no longer has is a trap for the next reader, and the git history keeps it anyway. Do this as one sweep, not opportunistically. **Protect from this cleanup (keep):** `scripts/diagnostic/revise/author-confusion-sweep.json` (the old scorer, the BEFORE side); `scripts/diagnostic/revise/b122-rescore.json` (the drafts that closed B122); `scripts/diagnostic/revise/b134-carve-out-gate.json` (the only same-process A/B); `scripts/diagnostic/revise/directive-follow-scorer.mjs` and `tests/directive-follow-scorer.test.mjs` (the working instrument); `scripts/diagnostic/lib/suggest-call-record.mjs` (conditions recorder); `scripts/diagnostic/anchor-fix-corpus.json` (360 stored pairs); `scripts/diagnostic/anchor-window-replay.mjs` (window replay); `scripts/diagnostic/revise/b132-voice-false-positive.mjs` (voice concern replay); `scripts/diagnostic/revise/b139-silent-hyperbole.json` (B139 synthetic silent-card hyperbole fixture); `scripts/diagnostic/accuracy/labels.json`; `scripts/diagnostic/accuracy/group-a-design.json`; `scripts/diagnostic/accuracy/sample-manifest.json`; `scripts/diagnostic/accuracy/statements.json`. | silence reversal 2026-08-29; B122 close 2026-08-30; B134 ship 2026-08-30; B123 re-file 2026-08-30; B139 ship 2026-08-30; accuracy labelled set 2026-09-05 | L | AFTER | Delete, do not mark. Fourteen files protected. |
| P32 | **OPEN. Filing only.** No test asserts any user-visible claim from real input to the screen or the exported file. Unit tests that hand a helper its input do not count. Spec for the next end-to-end pass: `scripts/diagnostic/delivery-check/claims-inventory.md` Part 3. | claims inventory 2026-09-19 | M | AFTER | Strict e2e. All C1-C87 currently no. |
| B272 | **SHIPPED 2026-09-20 `b449975`.** Spend ledger prints list USD and discounted USD. List is Langfuse `calculatedTotalCost` (full input rate). Discounted applies cached-input at 50% on gpt-4o (`lib/observability.js` L49). Existing rows recomputed; both numbers kept. Ledger `docs/SPEND_LEDGER.md`. Docs-only; no dedicated test file. | this spec | -- | RECORD | The list figure is not a flat 40% high. |
| B310 | **WE CANNOT SEE OUR OWN SPENDING. OPEN. Filing only.** The API key the product uses cannot read the provider's usage or billing endpoints (403, missing `api.usage.read`). Nobody could see the credit balance running down before it stopped production on 20 September. Needs a separate admin key with usage read, and a balance alert. | B291 Q1 | H | LAUNCH | Operator cannot see the bill. |

---

## 4. Product

| ID  | Item | Source / context | Priority | Launch | Notes |
|-----|------|------------------|----------|--------|-------|
| Pr2 | Merge duplicate concerns | userMemories | L | AFTER | Merge duplicate concerns — R3.4 partial fix superseded by **R6.3** (shipped 2026-05-31, `r6.3-principle-based-suppression`). Evidence-vs-Editorial duplication on conflicting statements is now handled by per-instance LLM judgment via `editorial-duplication-judge.mjs` at card assembly (not rule-ID suppression). Canary: `editorial_concern_suppressed_by_judgment`. **Remaining open:** (a) Evidence-vs-Editorial duplication on `partially_confirmed` verdicts (R3.4/R6.3 deliberately scoped to conflicting only). Reactivate if the same noise pattern shows up on partials. (b) Editorial-vs-Compliance overlap on promotional language (e.g. `marketing_language_excess` + `regulatory_prohibited_language` firing on the same sentence). Reviewed and judged intentional — both signals serve distinct reviewer decisions (craft vs regulatory). No fix planned; revisit if reviewer feedback indicates the overlap is genuinely noisy rather than complementary. |
| Pr3 | Align Direction intensity — Evidence softer than others | userMemories | L | AFTER | Folded into **R6.1** (ROADMAP). |
| Pr4 | Reviewer comments follow house style | userMemories | M | AFTER | Pre-existing. |
| Pr5 | Hide Editorial on conflict | userMemories | L | AFTER | Pre-existing. |
| Pr6 | Fidelity log traceability (add draft identifier) | userMemories | L | AFTER | Pre-existing. |
| Pr13 | **Approved-text library.** A library of past approved texts (list, picker, reuse). | persistence scoping 2026-08-21 | L | AFTER | **Unblocked-but-not-built:** a store exists, but `review_state` is one active review per browser (autosave/restore, `v8.71.0-review-state-restore`). Explicitly not a library of past reviews; no list, picker, or history UI shipped. |
| Pr16 | **Internal consistency in Review — the draft is never compared against itself. Ben has called this a must-have. Still not built.** Every check the product runs is draft-against-source. Nothing reads the draft as a whole, so a memo can state that a commitment was made in June and then recommend approving that same commitment, and **pass clean**. The live Meridian fixture does exactly this (**P30**). Splits cleanly in two: a **deterministic half** — the same quantity stated two ways, a date that contradicts a tense, a figure that disagrees with itself — and a **judgement half** needing a model. **The deterministic half is worth doing first**: it is cheap, it has no verdict-layer exposure, and it catches the shape above. Distinct from `narrative_coherence`, which is per-statement and, per **B99**, not blind. **Cross-link B131:** Review also does not check two `suggestedDirection` strings on the same statement against each other. That is the same class of defect (output unchecked against itself), scoped to one card rather than the whole draft. **Cross-link B172:** when a source contradicts a base figure, dependents in that sentence hang and the product currently says nothing; surface as a finding, do not duplicate this row. **Pr17 shipped 2026-09-01–02** (no action-list tag; residual **B151**). Sequenced after the Review group and the wording pass (operator 2026-09-02). | Ben, must-have; recorded 2026-08-29; B131 2026-08-30; sequenced after Pr17 2026-08-31; Pr17 shipped 2026-09-02; B172 2026-09-11 | M | LAUNCH | After Review group and wording pass. Deterministic half first. Exhibit: **P30**. Same class: **B131**, **B172**. |
| WR2 | **Multi-client prompt libraries** — generalize WR1's PG-hardcoded prompt library into a client-keyed, data-driven layer: client id -> event types -> versions -> prompt blocks + client bindings (entity name, verb-by-event-type, house-style rules, exclusion lists) as DATA, not literals. Exactly one client library active at a time (single global config value, not per-user). Deterministic exclusion filter reads exclusions from the active library. Provisional storage: committed files, one per client, active client via one config value. Also folds in WR1 deferrals: blank-transaction-date modal guard; graceful fallback when an event type has no library entry. Defer true both-version output separately. | post-WR1 scoping | M | AFTER | Post-demo follow-on from **WR1**. Keep one active client globally; no per-user client switching. Attention-Router throughline (ROADMAP). |
| WR2.1 | **True both-version output** — single generate produces Complete and Public variants (post-WR1 demo). Currently one version per Generate. | WR1 ship 2026-07-06 | M | AFTER | Follow-on from **WR1**. Deferred separately from **WR2**. See ROADMAP **Recently shipped → WR1**. |
| B32 | **Event-type awareness** (writing matrix + review expectation handling) — see ROADMAP **R6.14**. Review-side SHAPE UNDECIDED (Option 1 filter dimension vs Option 2 expectation profiles). Sequenced after B21–B23. | roadmap-eventtype scoping 2026-06-25 | M | AFTER | Generate/Rewrite framing retained; QC path cleaned (**B28**). Demo scaffold shipped (**WR1**, 2026-07-06). Re-add to QC if R6.14 ships. |
| B311 | **AN OPERATOR BILLING FAILURE LOOKS LIKE A PRODUCT FAILURE. OPEN. Filing only.** "The review did not run" is honest for the reader but the cause on 20 September was an unpaid account. When someone other than Ben is using this, that must not read as the product being broken. Belongs with the accounts work. | B291 | M | AFTER | Cause is billing. Copy is a product miss. |

### B26 — Watch (signoff duplication; chapter closed, watch remains)

Readiness/signoff logic now exists in two places — `lib/qc/signoff-verdict.mjs` (backend, feeds Constructive Feedback) and the frontend hooks `useDraftState.jsx` / `useAssessState.jsx` (Reviewer Assessment). They are intentionally kept in lockstep to guarantee the two surfaces never disagree on whether a draft is ready. Any change to the frontend signoff thresholds must be mirrored in `signoff-verdict.mjs` (and vice versa). Alignment is verified live by the B26 Step 4 test (same run → same readiness on both surfaces). See ROADMAP **Watch items → B26 — Signoff logic duplication**. **B26 / B26.1 / B26.2 / B26.2.2 / B26.2.4** shipped — watch item remains open.

### Long-horizon ideas (below top 13)

- **Per-house / per-reviewer language profile** — capture preferred phrasings, tone, capitalisation, and term substitutions per reviewer or per house, included as prompt context for the language layer only (Stage 5 commentary, B26 register, future reviewer-facing prose). HARD BOUNDARY: this never touches Stage 2 classification, Stage 3/4/7 aggregation, or any verdict-layer logic. The deterministic LLM-last architecture is preserved. Origin: 'centurion vs learning' framing from Ren Education (Straits Times, June 2026).

---

## Parked

PARKED, DEFERRED, and WATCH rows. Not scheduled. Launch may still be LAUNCH (needed for a first client) or AFTER.

| ID  | Item | Source / context | Priority | Launch | Notes |
|-----|------|------------------|----------|--------|-------|
| F22 | **PARKED. Panels overlap at about 800px viewport width.** Real, reproduced twice. Not a phone or tablet product. | layout 2026-09-15 | L | AFTER | Parked. Not a phone or tablet product. |
| B18 | Stage 5 evidence commentary editorialises rather than describes (e.g. 'This is a significant discrepancy'). Calibrate Stage 5 prompt toward neutral description per QC Output Language Standard. **Watch item** — pending next diagnostic batch after `commentary-calibration` (2026-06-03); 0 editorialising tone markers in 2026-06-01 batch. Close if next batch also clean. | R2.7.2 Run 1 testing | M | AFTER | |
| B27 | **Named-individual suppression reliability on subtle attribution drift.** R6.6.3 proviso (suppress only when source uses the named person the same way the draft does) verified only on a clear-cut case — fabricated forward-looking quote. Unproven on subtler drift (same role, shifted emphasis; paraphrased vs invented quote). Fail-safe bias means subtle cases should fail toward KEEPING the concern, so risk is residual noise not silent over-suppression — but confirm on a future diagnostic batch that suppression never drops a genuine consent gap. | R6.6.3 testing | L | AFTER | Watch only |
| B30 | **Thorough regression calibration matrix (deferred).** R6.13.2 shipped light coverage (two guard fixtures). Full version: every regression fixture declares version/output-type/publication-state asserted against expected rule subsets. Build only if calibration drift starts showing up. Source: R6.13.2. | R6.13.2 | L | AFTER | Deferred |
| B31 | **Silent-default handler canary (deferred).** Scoped narrowly: emit a canary when a draft-review request reaches the API handler without review intent (always wrong for a real UI request); low-noise unlike per-default canaries. Source: R6.6-audit. | R6.13-audit | L | AFTER | Deferred |
| B38 | **API batching (parked).** Batch API (50% off, up to 24h latency) for offline diagnostic/regression sweeps only. Do not apply batch latency to interactive Review. Prompt-caching half moved to **B3** (OpenAI automatic prefix cache is live for gpt-4o; blocked by current Statement-then-Source order). | post-A10 scoping 2026-07-09 | L | AFTER | Parked (batch only) |
| B56 | **Source-recency sensitivity gap: margin / contract-length (watch/decision).** Source-recency did not fire on a present-tense margin + contract-length claim (clean S4) supported only by the 3-year-old IC memo. Decide whether margin is recency-sensitive (a stale margin presented as current is arguably the target) or durable. May need `recencySensitiveReasons` expansion or a DURABLE_RE exclusion review. | Nordholt clean-draft run 2026-08-19 | -- | AFTER | Watch / decision |
| B57 | **Editorial embellishment slips (watch).** "surged" (overreach vs source "climbed") and "$155m" (currency symbol + USD vs sources' EUR) drew no editorial or style flag on the dirty run. Group with house-style / framing coverage watch. | Nordholt dirty-draft run 2026-08-19 | -- | AFTER | Watch only |
| B62 | **PARKED — Qualitative-claim validity rule.** **VERDICT-ADJACENT.** Separate from extractor fixes (**B64**, shipped). Claims like "almost entirely organic" or "the sector is fragmented" are verifiable but contain no number/date/name, so they revert as `anchorless_claim`. Fix would keep the anchor test **strict for the coverage guard** and relax **claim validity** to "contains an anchor OR is a complete predicative clause of at least N words". **Parked 2026-08-21:** widening the claim-span funnel was measured to produce no additional upgrades. `stage1b_v1.md` also tells the model to omit spans with no number, date, or proper noun, which duplicates the code-side anchor rule; if revived, relax the prompt as well. | `review-claim-spans` gate 2026-08-19; Stage 1b revert dump; funnel measurement 2026-08-21 | M | AFTER | Parked. No additional upgrades measured. |
| B66 | **PARKED — Claim-span connective blocklist is over-broad.** Would narrow to genuine causation and ordering (`driven by`, `following`, `because`, `resulting in`) and drop pure comparisons (`up from`, `down from`, `compared with`, `compared to`, `versus`). Seven of twelve original matches were `up from`. Word-boundary matching shipped in `b81929b` (killed "step-up from"). **Parked 2026-08-21:** widening the claim-span funnel was measured to produce no additional upgrades. | Sizing count 2A 2026-08-19; funnel measurement 2026-08-21 | S | AFTER | Parked. No additional upgrades measured. |
| B68 | **PARKED — Arithmetic self-consistency check.** L. Deterministic, no LLM cost. 14 corpus sentences pass the strict test (at least two of base / change / result stated, third derivable); all 14 are base-and-result pairs. Would build only that narrow case. Additive signal, never feeds the verdict. **Parked 2026-08-21: logged but not building.** | Sizing count 2B 2026-08-19 (strict recount) | L | AFTER | Logged but not building. |
| B132 | **`voice_consistency` fires on a coordinated third-person verb by over-applying the first-person worked example. DEFERRED. Fixture-only.** Exhibit: `suggest-after-r10-review2.json` S7, original `On balance, Halden Group believes the fund should deliver returns broadly in line with its predecessor and recommends the commitment.` Review asked to replace `'recommends'` with `'Halden Group recommends'`. Suggest obeyed on all three seeds and produced a duplicate subject (`...and Halden Group recommends...`). A follow-rate metric scores this as a **SUCCESS**. **Cause (restated 2026-08-30):** the MODEL over-applies the worked example in `FIRST_PERSON_ACTOR_INSTRUCTION` onto a coordinated verb. There is no Review verb list. It survives because `voice_consistency` has no pronoun backstop while `first_person_plural` does, and a valid editorial id routes past the style filter (**R6.11a** / **B14**). **Fix is FREE:** extend `STYLE_RULE_DETERMINISTIC_FILTERS`, scoped to `reporting_commentary` and `press_release`. Do NOT change the prompt. **Unblocking is not free:** it needs an investor-letter fixture plus a billed Review run. Three live Production Meridian runs, never once seen, and on every one the `voice_consistency` raise was CORRECT. **Size (fixture):** 1 of 36 statements, 1 of 8 voice flags. **Controls:** 7 stored concerns must keep firing; condition-b S8 and r10-review1 S8 ("available to us") are the ones that fail a filter written as leading-"we" only. There is NO stored genuine no-pronoun voice concern, so bluntness cannot be disproven on this corpus. Replay: `scripts/diagnostic/revise/b132-voice-false-positive.mjs` (free). Child of **B136**; do not bundle with **B131** or **B138**. **Composes with shipped B138:** B138 withholds by class on silent cards (first-person actor rules still reach); this ticket's Review filter is what removes the false member of that class. Complementary; must stay separate. After B138 the junk raise still reaches the reviser by design, on the fixture. **Annotation 2026-09-18 (B219):** `voice_consistency` now has a pronoun backstop on reporting commentary, gated on finding text that claims first-person misuse. Worked examples label Fault and Corrected. Press release is first-person house voice, so the backstop is not applied there. Coordinated-verb over-apply on a named-firm sentence is not re-measured. This row stays deferred. | B122 rescore `ee170f7`; residual `cf32a76`; harness 2026-08-30; B138 ship 2026-08-30; live Meridian x3 2026-08-31; B219 2026-09-18 | M | AFTER | DEFERRED. Fixture-only. B219 shipped the RC pronoun half. |
| B154 | **PARKED 2026-09-05. Last measurement.** Comparative no source addressed coming back partially confirmed on a peer or own-fund figure. Characterisation (`characterisation-2026-09-05.json`, n=20): 17 `no_support`, 3 `partially_confirmed`. Still a flap. Not gateable. Ben's three production runs the same day were 3/3 false green; that streak is a short look at the known flap, not a harness/production mismatch. Isolated stored S7 and span elicitation off cannot change this sentence's whole-sentence grade. The 2026-09-03 full-pipeline exhibit already returned `no_support`. Do not measure again. Fix moves the pinned corpus and goes last. Do not fold into **B84**. Date case is not this row. Do not build a peer-word backstop. | operator 2026-09-02; exhibit 2026-09-03; stability 2026-09-03; characterisation 2026-09-05; parked 2026-09-05 | H | AFTER | Parked. Same mark, different grade. Hash-pinned if a backstop is built. |
| P3  | Re-eval trigger: new gpt-5-family model with materially better latency | R1.2.6 conclusion | -- | AFTER | Watch only. Re-run R1.2.5 if a candidate appears that addresses gpt-5's latency profile. |
| P4  | Re-eval trigger: new Anthropic Sonnet/Haiku with closed agreement gap | R1.2.6 conclusion | -- | AFTER | Watch only. |
| P8  | **Grok 4.5 output-quality eval (parked).** Language layers only (Stage 5, editorial, compliance, B26); Stage 2 held on gpt-4o. Hold Stage 2 output constant across baseline and Grok arm for clean comparison. Full diagnostic batch ~$25–30; prefer `--only` subset first. Revisit when measured cost/quality problem points at model. | post-A10 scoping 2026-07-09 | L | AFTER | Parked |
| P10 | **Stage 2 concurrency cap 24 unproven under load.** Cap is `STAGE2_CONCURRENCY=24` (`e6e59a6`). No fixture case exceeds 24 pairs, so the pool never filled during measurement (mean 3619 ms/case on the subset). A real full-length draft will exceed it. Measure on the first full client draft, not on fixtures. **Annotation 2026-09-19:** Shopify messy full, 184 Stage 2 jobs, pool filled, Stage 2 about 10 s. Not the timeout. Stage 1 was (35.2 s; **B263**). | Stage 2 timing `--timing-once` 2026-08-19; shopify-messy-full 2026-09-19 | -- | AFTER | Watch. Pool now filled once. |
| Pr10 | **A11.1 — Per-version QC storage in Assess.** Single flat `analysisResult` — loading earlier version shows "re-run Review" banner even when that version was reviewed. Prerequisite for tabbed base↔adaptation view. Post-Monday. | A10 sprint 2026-07-09 | M | AFTER | See ROADMAP **Parked → A11.1** |
| Pr11 | **A11.2 — Tabbed base↔adaptation view (deferred).** Depends on **Pr10** + multi-version model in Assess. Current: labelled timeline entries. | A10 sprint 2026-07-09 | L | AFTER | Deferred |
| Pr12 | **A11.3. Single-view consolidation.** Collapse Assess/Writing into unified Content Engine view. Deferred until A10 settles. Writing route off/dead. **Evidence 2026-08-22 supports deletion rather than repair:** `addUrlSource` is unreachable from any rendered control; its only caller is the URL field in `SourcesPanel.jsx`, which nothing imports; `/writing`, `/quality` and `/quality-review` all redirect to `/assess`; `FocusWorkspace` and `QualityPageLayout` remain in `App.jsx` but are on no live route. A miswiring in `addUrlSource` (it calls the URL-building helper `apiFetchUrl` rather than a client for `/api/fetch-url`) is therefore not user-visible. Deliberately not fixed: repairing a control nobody can reach would be building an unrequested feature. See ROADMAP **R4.2**. | A10 sprint 2026-07-09; reachability check 2026-08-22 | L | AFTER | Deferred. Evidence favours deletion. |
| Pr14 | **DEFERRED. Blob storage for source files (removes the 3 MB ceiling).** The real fix for **B79**. The browser uploads directly to object storage and sends only a reference; the backend fetches the file server-side where the 4.5 MB rule does not apply. Also removes the re-upload-after-refresh limitation (**F16**). Scoped in full on 2026-08-21 and deliberately deferred the same day. Reasoning: the ceiling does not bite during development; building it means storing client source documents, which the persistence chapter deliberately avoided; the client deployment model is undecided, so it may be superseded by a client's own storage; **B9** is the higher-value next step. Full scoping (recommended provider, permanent choices, three decisions already taken) sits in the Claude project note `claude/blob-storage-scoping-note.md` (Claude project doc, not in this repo). Trigger to revisit: a real source document over 3 MB needs reviewing, or the client deployment model resolves in favour of the hosted app. | blob-storage scoping 2026-08-21 | L | LAUNCH | Deferred. Not scheduled. |
| W2  | **R6.12 residual LinkedIn editorial noise** — F12 S6 `structural_integrity` and S8 hyperbole borderline on social formats after R6.12 ship. Monitor if more LinkedIn fixtures added. See ROADMAP **Watch items → R6.12 residual LinkedIn editorial noise**. | R6.12 verification 2026-07-05 | -- | AFTER | Watch only |
| W1  | **internal_plausibility** — may still attempt to fire on statement-vs-source figure discrepancies (rounded vs exact). Scope wording in place (R6.2e); in testing suppression came via the fidelity gate (cited source figure absent from statement), not the model obeying the scope constraint — B14 pattern. User-visible result correct. Action: review next diagnostic batch; add targeted deterministic backstop only if it recurs in a form the fidelity gate misses. See ROADMAP **Watch items → internal_plausibility**. | R6.2e verification | -- | AFTER | Watch only |
| B33 | **B21 residual (observable, not open):** a category mis-tag whose `ruleId` is in NEITHER rulebook cannot be salvaged by reclassification and would still fall back. Not observed across the B21-diag-confirm set (zero such drops). Now self-identifying in `pipeline.log` via `rejectReason` `ruleId_not_in_either_rulebook:<id>`. No action unless it appears. | B21 | L | AFTER | Watch only |

---

## Findings log

Record-only: accepted costs, recorded observations, abandoned or rejected attempts, standing measurement constraints. Not tasks.

| ID  | Item | Source / context | Priority | Launch | Notes |
|-----|------|------------------|----------|--------|-------|
| F16 | **Accepted limitation (restore / Review).** After a refresh, a PDF or Office source must be re-uploaded before Review can run again. Autosave still restores draft, extracted text, version history, and cards (`v8.73.0-review-state-no-blobs`). Reason: the backend refuses inline text for PDFs (`PDF_INLINE_TEXT_NOT_ALLOWED`) so extraction always happens server-side from the original bytes. Pill copy: "Re-upload to run again" (`v8.74.0-review-state-tidy`). Blob storage (**Pr14**) would also remove this limitation; that work is deferred. | review-state 2026-08-21 | -- | RECORD | Accepted. Not a bug. |
| B75 | **Do not overstate B73.** B73 was insurance, not a live-path fix. The dash-as-sentence-boundary bug existed only in the fallback splitter (`splitDraftIntoCandidatesV2`, maxLen 240) and D-METRIC-LLM. Production Stage 1 uses the LLM extractor and reaches fallback only when that output is rejected. Shadow: LLM card texts and verdicts unchanged (100% cache). Recorded so later release notes do not treat B73 as a production-path correction. | B73 splitter gate 2026-08-21 | -- | RECORD | Record only. |
| B104 | **Decomposition does not improve verdicts.** 32 decompositions across the corpus, zero verdict changes, while roughly doubling Stage 2 volume on the stage carrying about 97% of spend. Its value is giving the reviser a span. **B53a** earns its place on the revision side, not the verdict side. Cross-ref Closed **B88**. | claim-upgrade measurement 2026-08-25; `review-upgrade-off` | M | RECORD | Revision-side value. Not a verdict lever. |
| B105 | **The corpus is almost entirely single-source, which blocks measuring any multi-source mechanism. Fixture gap.** Coverage-union population was zero on this corpus in part because remaining `supported_partial` cards are single-source fixtures. Multi-source promotion and similar mechanisms cannot be exercised until fixtures carry two or more contributing sources on partial cards. | `review-coverage-diagnostic` 2026-08-25; `review-coverage-union` | H | RECORD | Fixture gap. Blocks multi-source measurement. |
| B108 | **MEASURED AND REJECTED. Stage 2 conflict hardening R4. Keep as record; do not archive.** Tried: R3a + one mutually-exclusive-figures/plans sentence. Measured: nordholt-dirty S1 (counts) recovered to conflicting on R3a and R4; **S5** (plan years / bolt-on count) stayed soft under R3a/R4 and remains soft under R10 (accepted cost **B116**). HOLDs intact. Cost more than it bought: no ship. Do not retry the same wording without a new instrument. R3a stands historically (`stage2-rewrite-r3a`); live is R10 (`stage2-basis-conflict-r10`). R10 also recovered nordholt-dirty S1 on the press-release pair (`ce3d85e`). Report: `scripts/diagnostic/eval-ablation/r4-conflict-hardening.md`. | R4 measure 2026-08-26; corpus blast `36776f7`; R10 blast `ce3d85e` | M | RECORD | Measured-and-rejected. S5 -> **B116**. |
| B110 | **ACCEPTED COST (R3a ship). F92_S0 unsupported characterising adjective ("small startup").** One adversarial fixture. R3a made it worse (partial -> confirmed on corpus blast). Remained confirmed under R3a, R4, and R10. Source: `91_adversarial_shopify_2010_trimmed`. Not unknown; chosen when R3a shipped. | corpus blast `36776f7`; R4; R10 `ce3d85e` | M | RECORD | Accepted cost. Adversarial only. |
| B113 | **Stage 2 noise floor is 2 of 23 at temperature 0 with seed 1.** Unstable on A x3: F12_S0 and F08_S2. Modality (F08_S2) is soft and will flap in production. Effects must beat this floor. This is a noise floor on identical planted statements. It is not a home for **B144** and not a home for **B123**. | `noise-floor` / `90249e5`; rewrite ladder standing rule | M | RECORD | Standing measurement constraint. Not B144. Not B123. |
| B116 | **ACCEPTED COST (R3a ship). nordholt-dirty S5 lost contradiction.** Statement: at least five bolt-ons; exit by 2027. Source LP update: two further; realisation not before 2028. Was conflicting under pre-R3a; softened to partially_confirmed under R3a and still soft under R10 (`ce3d85e`). Chosen when R3a shipped accepting two lost contradictions; S1 was later recovered by R10 (press-release pair). Distinct from R4 measure (**B108**). | `r3a-corpus-blast.md` `36776f7`; R10 rows | M | RECORD | Accepted cost. |
| B117 | **ACCEPTED COST (R10 ship). F18_S7 lost contradiction (ARR 38 against 35).** PASSAGE DRIFT instance of **B115**, not a scoped-basis-gate defect (`f18-s7-loss-diagnosis.md` `519257a`). R3a cited ARR 35-vs-38; R10 cited MOIC/IRR base-case span with none of the draft ARR figures; label softened to partially_confirmed. Survived noise confirmation. Chosen when R10 shipped. | F18_S7 diagnosis; blast `ce3d85e` | M | RECORD | Accepted cost. B115 instance. |
| B123 | **F02:S6 thin Stage 2 passage. ACCEPTED AND NOT FIXED, at the overlay AND at Stage 2. 2026-08-31.** Not closed as done. Not left open as pending. The bug is still live for a user and there is no fix coming. Statement: "Partners Group will reinvest alongside CPP Investments and Equinix and retain up to 10% of the Company going forward." Source `02_pg_atnorth_exit.txt`. Sibling of **B115** (figure correspondence, R10 SOME). B123 is figure ALL and incomplete for a NAME. Do not file it under B115. Do not file it under B113. (1) The evidence format is ONE contiguous quote capped at 400 characters, and F02's two facts sit 952 characters apart. No instruction makes a single span hold both. (2) The entity-omission rule that should have caught this is ALREADY live in `stage2_v4.md` L156-158 and did not fire. Definitional absorption. (3) Seven overlay approaches are dead. The last one never worked: it "recovered" F02 because the anchor string "investments" contains the relation string "invest", so the check confirmed itself. Previously recorded dead: (0) every candidate anchor recovers F02:S6 but loses F21:S0; (1) regex widening of `ANCHOR_ENTITY_RE` no recovery, F19:S8 regresses true to false; (2) parent-paragraph W2 no recovery; (3) full-source W3 recovers F02:S6, BREAKS `F22:S0:ALP_update_memo` and `nordholt-dirty:S0:fact sheet`. **B137** does not rescue any of these and is not separable from this row. (4) 29 of 360 quotes are corresponding-but-incomplete; exactly ONE produces a user-visible false red of this shape (`F02:S6`). **Falsifier:** a SECOND live false red of that shape. Reopening condition: `claude/evidence-model-limits.md` (Claude project doc, not in this repo; do not summarise it here). Tokeniser fragmentation is real and is **not** the cause; that is **B137**. **F21:S0 is a VACUOUS lock** for tokenisation and for window widening. Replay bed: `scripts/diagnostic/anchor-window-replay.mjs` (360 pairs, free). | `anchor-narrowing.md`; `anchor-fix-corpus.json`; `anchor-window-replay.mjs`; `02_pg_atnorth_exit.txt` L5/L9/L13/L15; overlay and passage-selection passes 2026-08-31 | H | RECORD | Accepted and not fixed. Live for a user. No fix coming. Sibling of B115. |
| B127 | **RECORDED, not a defect today. A person and an organisation are indistinguishable as a rival actor.** The named-actor branch in `lib/qc/evidence-relationship.mjs` (`excerptCreditsADifferentActor`) treats any plausible multi-word name in subject position before the relation as a rival actor, and cannot tell a person from a firm. Nothing in the corpus trips on it. Record only; no action. | anchor narrowing `3122708` | L | RECORD | Record only. Nothing in the corpus trips on it. |
| B128 | **RECORDED. Stage 2 has no label for "this is not a verifiable claim", so that judgement empties into `no_support`.** A procedural or non-assertive sentence has nowhere to go and is reported as unsupported, which reads to a reviewer as a fault in the draft. **One instance in the whole corpus: F01_S11**, the procedural closer (`arm-d-examples-removed.md`, `arm-e-boundary-examples.md`). This is the correct home for that case — it is explicitly **not** a passage-selection miss, see the correction on **B115**. | F01_S11 read 2026-08-29; corpus arms D/E | L | RECORD | Record only. One corpus instance (F01_S11). |
| B129 | **RECORDED. The Stage 1 validator rejects a directive-authorised edit.** The span the validator enforces comes from the **evidence** finding, so an edit that an **editorial** directive explicitly authorises can fall outside it and be refused. Only matters if the per-statement path is revived — see **B130**. | stage 1 restriction `2528a32` | L | RECORD | Record only. Dormant while **B130** stays abandoned. |
| B130 | **ABANDONED ON EVIDENCE, deliberately. The per-statement revision rebuild. Keep as record; do not archive and do not re-propose without a new instrument.** Not failed and not deferred — its case was **measured away in three stages**. (1) The equity cheque left scope when silence stopped being editable, because the whole-sentence removal it was meant to make safe no longer runs. (2) Conflicts turned out to be handled **identically** by both paths. (3) Directives came back **level** across 14 cases — 29 of 42 whole-draft against 30 of 42 per-statement. Those 29/42 numbers were a measurement artefact; **B122** is now closed. Stage 1 remains on disk behind `body.perStatementRevise`, off in production, costing nothing. **What it retains if it is ever revived:** run-to-run determinism; a **structural** rather than requested guarantee on silence; and lower cost. Residual defect if revived: **B129**. Landmine for **B134**: Stage 1's shared prefix still says everything else stays exactly as written (`revise-stage1-prompt.mjs` L89), a second copy of silence that is not sliced from rule (b). **B134 shipped on the live whole-draft path** (`revise-style-carve-out`); this landmine remains if Stage 1 is revived. Follows the **B108** measured-and-rejected precedent. | stage 1 restriction `2528a32`; directive measure 2026-08-29; silence reversal 2026-08-29; B122 close 2026-08-30; B134 ship 2026-08-30 | M | RECORD | Abandoned on evidence. Record. Stage 1 parked behind its flag. |
| B144 | **Review is ambivalent about unsourced attribution of motivation. RECORDED. Not to be built or billed.** Nine stored cards of the same sentence, zero partials: green is the modal verdict and the flag was the outlier. Operator observation 2026-08-31: an earlier write-up had this backwards. Not a home in **B113** (noise floor on identical planted statements) and not a home in **B123**. | live Production 2026-08-31; stored cards; operator observation 2026-08-31 | L | RECORD | Recorded. Not to be built or billed. Earlier write-up had the direction backwards (operator observation). |
| B147 | **RECORDED. `conflictValues` remains hardcoded `null` at assembly.** Span-only empty `conflictExcerpt` CLOSED 2026-09-08 (`3214646`): a conflict excerpt can come from the span that caused the verdict. Remaining layout gap is **B158** (11 conflict cards quote in `primaryExcerpt` only). Do not fold B158 into this row. | Stage 0 Q1 2026-09-01; accuracy arc 2026-09-08 | L | RECORD | conflictValues still null. Excerpt gap is B158. |
| B148 | **RECORDED. `findingRestsOnSilence` is wider than the shipped principle and all of its callers are dormant.** Direct callers: `authorStatementExemption` (`lib/revise-author-statement.mjs` L269), `stage1SendDecision` (`lib/revise-stage1.mjs` L374), `tests/revise-author-statement.test.mjs`, `scripts/diagnostic/revise/stage1-under-the-principle.mjs` L152, plus the definition at L128. Live Suggest does not run them: `perStatementRevise` is off and `deterministicUnsupportedRemoval` is false (`api/suggest-revision.js` L56-64, L176-177). Exhibit: coverage-gap S0 (source states Q3 2026; predicate would call it silence). Do not delete. **The per-finding path shipped 2026-09-02 uses `lib/revise-actions/silence.mjs`, not this predicate.** | Stage 0 Q2 2026-09-01; Pr17 ship 2026-09-02 | L | RECORD | Record only. Do not delete. Per-finding silence is a different test (Closed **B149**). |
| B151 | **DEVIATION, accepted for v1, revisit.** No preview screen. The user decides row by row, Save appends a version, the total is visible in history. This inverts the stated flow (decide, preview the rebuilt memo, then commit). Accepted because every row already shows its own resulting sentence and versions are append-only, so disliking the total costs one revert. Recorded during the apply slice 2026-09-02. | apply slice 2026-09-02; operator ruling | M | RECORD | Deliberate. Revisit when preview is worth the extra surface. |
| B152 | **REPLACED, not forgotten.** Default-accept / passive save is retired. A live test showed the product owner accepted two proposals, left the rest alone, saved, and got changes he had not chosen: every proposal started accepted and the buttons gave no feedback. Invisible state plus default-accept meant the user could not predict Save. Replaced 2026-09-02: nothing is applied unless the user chose it. The pre-selected state is a recommendation that is shown, not a decision that is enacted. A passive save now changes nothing. Collision "neither is preselected" is redundant because everything starts unchosen; choosing a second write on the same sentence unchooses the first; Accept all leaves both unchosen. | live test 2026-09-02; operator ruling | H | RECORD | Replaced. Do not restore default-accept. |
| B162 | **MEASUREMENT NOISE FLOOR. OPEN as a standing constraint, not a defect.** This 100-statement pack cannot validate a judging change smaller than the measured noise: leave-alone moved 4 percentage points on identical cache-off runs; stability 97, 95, 96, 94 of 100; catch of 11 Wilson interval ~43 points at 9 of 11. Point at `scripts/diagnostic/accuracy/README.md`. Do not tune judging against this pack until the corpus is larger. | accuracy README 2026-09-08 | -- | RECORD | Constraint. Next work is corpus expansion (Ben). |
| B271 | **MEASURED AND NOT SHIPPED. Dropping the full marked draft from every editorial user payload moved findings past this stage's own wobble.** Same instrument, two fixtures. Long memo 40/40 succeeded in all four runs: old-old 13 codesDiffer, old-new mean 26.25, 12 stable shifts (mostly `materiality`). Prefix 17/17: old-old 5, old-new 4.5, 0 stable shifts. Cache hit on the 40 rose 82-85% to 94-95%. Input tokens on the 40 fell 592509 to 401572. The neighbours are not a substitute for document-level rules. Do not retry the same deletion. | stage-replay 2026-09-20; `tests/fixtures/b247/shopify-messy-full-after.json` | H | RECORD | Measured-and-rejected. Pool tighten is B268. |
| B273 | **MEASURED AND NOT SHIPPED. Telling the editorial model that materiality, first-use jargon, and throughout-voice may use the draft moved findings past this stage's own wobble.** Same 40-subset as B271. Old-versus-old 13/40. Old-versus-new mean codesDiffer 24/40. Four stable shifts. The scope line still names only `narrative_coherence` as the exception (`editorial-compliance-reviewer.mjs` L76). Do not correct it until a wording holds the floor. | stage-replay 2026-09-20; `scripts/diagnostic/delivery-check/b268-stage6-pool.md` | H | RECORD | Honesty line is load-bearing. |
| B275 | **MEASURED AND NOT SHIPPED. Document-level editorial call plus per-sentence call without the draft.** Quote-locate is proven (planted first-person lands on sentence 1, never on claimed index 2). Layer B on the Shopify memo returned only `voice_consistency` (first person). It recovered **0 of 7** materiality findings both olds had agreed on (indexes 44, 65, 86, 99, 107, 113, 180). 40-subset: floor 12/40, mean old-versus-new **28.5/40**. 17-card control: floor 4/17, mean **9.5/17**. Production path left unchanged (pool 4, `FULL DRAFT` still on every sentence call). Do not ship this split until Layer B actually recovers materiality. Report `scripts/diagnostic/delivery-check/b275-document-level-editorial.md`. | stage-replay 2026-09-20; this spec | H | RECORD | Third kill in this class (B271, B273, B275). |
| B309 | **THE HONEST CEILING OF ABOUT 3,700 WORDS WAS MEASURED ON A HALF-SPENT TOKEN WINDOW. RECORD.** It is a floor on the ceiling, not the ceiling. One clean re-run is owed when the number matters. | B277 Part D | -- | RECORD | Measurement constraint. |
| B276 | **RECORDED then SHIPPED as B278 `8f15563`.** Recording commit `b702249`. 2M TPM is this org's gpt-4o Tier 4, not a law. Limits are per model family. Waiting the window is now the retry path (**B278**). Live headers 2026-09-20: gpt-4o 2M TPM, gpt-4o-mini 10M, gpt-5.1 4M. The old 4-attempt / 2s cap is gone. | this spec 2026-09-20; B277-B279 | H | RECORD | Wait slice shipped. |

---

## Standing rules

Process lessons. Do not schedule. P5, P6, P7 and other actual work stay in Open work.

| ID  | Item | Source / context | Priority | Launch | Notes |
|-----|------|------------------|----------|--------|-------|
| P13 | **`npm ci` install: dependency commits must include the lockfile.** Backend Vercel `installCommand` is `npm ci` (`vercel.json` since `9b5bd90`, 2026-01-25). `npm ci` fails if `package-lock.json` is missing or stale. Node 22 pin and Frankfurt region shipped the same day as persistence (see Closed **P11** / **P12**). | vercel.json / 2026-08-21 reconcile | H | RECORD | Standing rule |
| P14 | **Payload-size behaviour cannot be tested locally.** `vercel dev` does not enforce the 4.5 MB request-body limit. An 11.3 MB file that fails in production runs successfully on localhost. Any local verification of a size-related fix is a false negative. Production is the only valid check for this class of bug. | production vs localhost 2026-08-21; `v8.75.0-source-size-guard` | H | RECORD | Standing rule |
| P15 | **The Pr9 marker harness measures honesty, not correctness.** Three separate faults passed it clean with perfectly consistent notes: an invented figure, meaningless filler, and a malformed sentence. Any future Pr9 change must be judged by inspecting quoted output rather than the cross-tabulation. | `pr9-cut-punctuation` 2026-08-23 | H | RECORD | Standing rule |
| P16 | **Planted faults belong only in invented fixtures.** A real document with an invented fault in it is no longer either thing, and cannot be cross-referenced against the original. Real published sources are kept verbatim. See `scripts/diagnostic/README.md` for the real-versus-invented list. | review-layer chapter 2026-08-24; `fixtures-real-vs-invented` (`2f5f403`) | H | RECORD | Standing rule |
| P18 | **Telling a model what NOT to put in a gap makes it fill the gap.** A prompt line forbidding the revision from narrating its own cut produced invented filler instead. When an instruction about absence backfires, stop writing instructions and write a check. | review-layer chapter 2026-08-24 | H | RECORD | Standing rule |
| P19 | **A fix verified only by its own harness is not verified.** Confirm on the deployed path. The first-person actor harness returned 18 of 18 clean on logic that was unreachable in production. See closed **B86**, **B94**, **P22**. | Meridian re-run 2026-08-24; named-actor reconcile 2026-08-25 | H | RECORD | Standing rule |
| P20 | **Where a deterministic mechanism consumes text a model wrote, the model becomes the specification.** Four instances so far: the change-log note (bit), the routing verb (**B82**, may not be latent), the restated resulting phrase (bit twice, closed **B87**), and the extracted claim list that composes a card verdict (bit, and is the false greens, **B84**). Whenever a deterministic step reads model prose, ask what happens when that prose is wrong. | Meridian re-run 2026-08-24; chapter five 2026-08-25 | H | RECORD | Standing rule |
| P21 | **An instruction that asks a model to reason about a hypothetical post-transformation state gets reasoned against the actual state.** The view-marker prompt was correct and was ignored. If a rule depends on what the text will become, compute it deterministically rather than asking. See Closed `review-view-marker-subject`. | named-actor reconcile 2026-08-25 | H | RECORD | Standing rule |
| P22 | **A harness score is itself evidence that can be wrong.** Confirm on the deployed path. The first-person-actor scorer reported dropped hedges on a rewrite that kept them. Do not treat the fraction as a gate until it is looked at. See **B94**, **P19**. | named-actor reconcile 2026-08-25 | H | RECORD | Standing rule |
| P23 | **Good investigation only outputs bad news.** Judge a feature on the trend in defect severity, not the length of the list a fault-finding session produced. Set a falsifiable stopping rule before the run and evaluate it on the same ground twice. | named-actor reconcile 2026-08-25 | H | RECORD | Standing rule |
| P24 | **When a config layer resists diagnosis, run the test where the config already works.** Four probes and most of an afternoon went into a local environment that would not load one variable, while the same test in production answered the question in one fifty-cent run. See **B103**. | chapter five 2026-08-25 | H | RECORD | Standing rule |
| P25 | **If a question cannot be answered from the output, that is the finding.** A missing log line is a defect, not an inconvenience. See **B100**. | chapter five 2026-08-25 | H | RECORD | Standing rule |
| P26 | **After two failed hypotheses about the same behaviour, stop theorising and instrument it.** Three successive hypotheses about deletion-direction wording were each stated with more confidence than the evidence carried. | chapter five 2026-08-25 | H | RECORD | Standing rule |
| P27 | **A tag landing is not a row closing.** When a spec says annotate rather than close, the closing decision belongs to the run that verifies the fix. `review-direction-actionable` (`5ff7ced`) shipped the B87 residues; B87 closed only after the production run confirmed them. | chapter five 2026-08-25 | H | RECORD | Standing rule |
| P28 | **Check whether the data already records that the mechanism fired.** `claimUpgrade` is a boolean on every card and it said false in every Meridian run while four days went into that path. | `review-upgrade-off` 2026-08-25 | H | RECORD | Standing rule |
| P30 | **The Meridian fixture contradicts itself, deliberately, for now.** `meridian_source.txt` / the Meridian draft state "In June 2026, Partners Group committed to..." and then "We recommend approval of the commitment." A commitment cannot be both already made and awaiting approval. It is **left as is on purpose** so that current measurements stay comparable with every earlier run on this fixture — do not quietly fix it. When it is next regenerated for another reason, the fix is one phrase: **"is considering a commitment to"**. This is also the exhibit the internal-consistency capability (**Pr16**) would catch. | fixture read 2026-08-29 | L | RECORD | Deliberate. Fix only on next regeneration. Exhibit for **Pr16**. |
| B222 | **STANDING AUTHORITY.** Cursor may change code without a spec when the change removes a contradiction between two places that already state the same thing, fixes user-facing copy that is plainly wrong, adds a test for behaviour that already shipped, or removes duplication, provided it changes no verdict logic, no data contract, no user-facing behaviour beyond the copy itself, and is recorded in this file in the same commit. Everything else still needs a spec. Run to completion without pausing for confirmation, asking only when a decision changes behaviour, the data contract or verdict logic, or is hard to reverse. Live in `.cursorrules` on both repos (`04c145f`, `560b082`). | B222 2026-09-18 | -- | RECORD | Standing rule. Log here in the same commit. |
| P31 | **A missing input must be loud at the point it goes missing.** Every silent default in this product is a future paragraph of confident invention. Refuse, or log, at the writer: assessment, constructive feedback, export, Implement Changes. Working shapes: placeholder guard (`lib/prompt-library/placeholder-guard.mjs`) and readiness refusal (`api/synthesize-review.js`). Census `scripts/diagnostic/delivery-check/dead-read-census.md`. Live in `.cursorrules` on both repos (B269). | dead-read census 2026-09-19 | -- | RECORD | Standing rule. Not a spec. |
| P33 | **Anything a future test or spec depends on is committed, never pasted into a conversation.** The B226 Meridian reconstruction is the case study: fixture 1 was rebuilt from chat, the 18 Sep live payload was not in the repo, card wording and margin-note counts differed, and later specs had to rediscover the live run. Live dump later committed as `tests/fixtures/b226/1-meridian-reporting-live-2026-09-18.json`. | B226 / B240 / this spec 2026-09-20 | -- | RECORD | Standing rule. In the operating manual. |
| P34 | **A spec that changes a shared helper must enumerate every caller and state what the change does to each.** A helper that is safe for one caller is not automatically safe for the rest. B278 changed the rate-limit retry wrapper having considered exactly one of its twelve callers (`analyse-statements`). The other eleven inherited an unbounded wait that hung the product in production on 20 September 2026. Already in `ai/AI_OPERATING_MANUAL.md`. | B278 / B285 2026-09-20 | -- | RECORD | Standing rule. Do not schedule. |
| P35 | **A spec that claims to fix an observed symptom must name the log line that will prove it, before the fix is written.** B285 named none and fixed a real bug that was not the reported symptom, while sounding certain. | B285 2026-09-20 | -- | RECORD | Standing rule. Do not schedule. |
| P36 | **A default that treats a missing answer as yes is a lie waiting to happen.** `!== false` on a check-ran flag made a card name Compliance clean on a run with no compliance call. Fifteen occurrences were found and changed in B299. Prefer `=== true` for any flag that answers "did this happen". | B299 2026-09-21 | -- | RECORD | Standing rule. Do not schedule. |

---

## Closed

| ID  | Item | Resolved by |
|-----|------|-------------|
|     | Trace metadata for QC runs | r1.2-pre, r1.2-pre.1 |
|     | Stage 2 prompt v2 (conflict detection) | r1.2.2-prompt-eval, r1.2.3-prompt-v2-production |
|     | Multi-provider infrastructure (OpenAI + Anthropic) | r1.2.4-multi-provider-infra |
|     | Cross-provider Stage 2 eval | r1.2.5-eval, r1.2.6-eval-closed |
|     | gpt-5-mini reproducibility test | r1.2.5.3 (under r1.2.6-eval-closed) |
| P1  | Cost model spreadsheet update | R1.x |
| Pr1 | Reviewer Assessment synthesis (narrative, not dashboard) | R4.x |
|     | R1.2 gpt-4o-mini Stage 2 source-matching eval | R1.2, R1.2.4, R1.2.5 |
|     | Pipeline v4 scaffolding | r2.1-pipeline-scaffolding |
|     | Stage 1 (statement extraction) in v4 | r2.2-stage1-extraction |
|     | Stage 2 (source matching) in v4 | r2.3-stage2-matching |
| B16 | **R2.7.2 — Stage 2 semantic frame matching** | `r2.7.2-frame-matching` — period scope explicit-vs-explicit only; relative-source-period gap → **R2.7.2.1** (B17) |
| Pr2.7.2 | **R2.7.2 — Stage 2 semantic frame matching** (Product duplicate ref) | `r2.7.2-frame-matching` — see ROADMAP **R2.7.2.1** |
|     | qcCard.pipelineVersion hardcoded `"v3"` in shared `stage7-assemble-card.mjs`, now stamped from actual route | `fix-pipelineversion-label` |
| F3  | `<>` JSON copy button missing/broken in Assess module | shipped (small frontend change, tag not recorded) |
| F4  | Enter-to-execute on Assess module's primary action button | shipped (small frontend change, tag not recorded) |
| B19 | **RESOLVED** — absorbed into and closed by **B22** (anchor eliminated; excerpt meta-phrasing count 0). | Was `commentary-calibration` — v4 `stage5_v2.md` |
| B22 | **RESOLVED** — closed by B22 / B22.1 / B22.2. Prompt-only register calibration of evidence (Stage 5) and editorial/compliance concern prose. **ANCHOR (B19):** 'excerpt' meta-phrasing eliminated — 53 instances in batch 2026-06-01 → 0 across all commentary fields (Stage 5 LLM prose + Stage 5 deterministic fallback [B22.1] + editorial/compliance concern prose [B22.2]). **REGISTER:** plainer terms (active/passive, 'hedging language', 'immediate context'), preachy framing removed, Directions concise. **SPECIFICITY (B22.1):** evidence prose must name the source's own verb/direction/figures on directional/quantitative claims — vague frames ('the same growth','this aligns') banned except where a non-quantitative source genuinely adds nothing beyond confirmation (verified acceptable, F01 S5). **MATERIALITY (B22.2):** source confirmation is no longer treated as redundancy; materiality judged against surrounding DRAFT sentences. **GUARDRAIL held:** substantive observation (conflict specifics, narrative_coherence, materiality reasoning) preserved; F18 conflict prose retains both sources' figures. **VERIFICATION CAVEATS (recorded, not papered over):** (1) B22 firing-regression was compared against the 4-week-old 2026-06-01 batch with intervening pipeline work (R2.7.2, R6.6, R6.2e/f, R6.11a), so prose-vs-logic effects were confounded rather than cleanly A/B-isolated; firing-behaviour unchanged is supported by mechanism + spot-check, not by clean isolation. (2) B22.2 materiality reasoning fix verified on diagnostic fixture 01 only; materiality on fixtures 04/08/11 not re-checked under the new principle — review on next full diagnostic sweep. Tags: `commentary-calibration` (orig ship: `commentary-calibration`), `b22.1-commentary-specificity`, `b22.2-editorial-excerpt-removal`. | See ROADMAP **Near-term — Review output** |
| B21 | **RESOLVED** — Editorial schema-fallback (reliability + observability chapter). | Diagnosis (**B21-diag**) found the 5 batch fallbacks (`2026-06-01-122541`) were not malformed output or over-strict schema — they were valid JSON with real concerns discarded by a pre-R6.11a fail-fast normalizer that rejected the whole payload on the first category mis-tag (model tagged `first_person_plural` as `editorial`; it is a `style_guide` rule), with retries resending the same prompt. **RELIABILITY:** already closed by R6.11a (per-concern salvage + cross-book reclassification + retry correction note); verified across the full set (**B21-diag-confirm:** F01/F06/F14×2/F20 — zero fallbacks, zero non-salvageable drops). **OBSERVABILITY:** card already distinct via R6.11b ("Needs manual review", amber, explicit copy); raw failed output now persisted to `pipeline.log` with per-attempt `rejectReason` (**B21**, this work) — closes the gap that forced Langfuse recovery during diagnosis. **Closed decision:** F06's batch `internal_plausibility` second-concern was not reproduced (model variance); fail-fast removal evidenced indirectly by two concerns surviving on one card — not chased; re-creating a historical model output proves nothing the per-concern salvage mechanism doesn't. Tags: `r6.11a-editorial-schema-salvage`, `r6.11b-not-reviewed-state` / `v8.54.0-r6.11b-not-reviewed-state`, **B21**. See ROADMAP **R6.11**. |
| B23 | **RESOLVED** — Editorial rule bug-fix pass shipped **2026-06-03** (`r6.2e-editorial-rule-bugfix`, `r6.2f-compliance-rule-bugfix`). Per-rule outcomes: ROADMAP **Near-term — Review output → EDITORIAL RULE BUG-FIX PASS** (line ~431) — `date_format`, `percentage_notation` + `number_spelling` percentage guard, `internal_plausibility` scope, `passive_voice_overuse`, `comparative_claim_without_basis`, `forward_looking_statement_without_qualifier`. | See ROADMAP line ~431 |
| B24 | Source-public-state awareness — R6.6 chapter (R6.6.1–R6.6.4). | R6.6 closed 2026-06-25 — see ROADMAP **Recently shipped → R6.6** |
| B20 | Writing-module QC (`useDraftState.jsx` `runStatementAnalysis`) did not send the user's required version or output type, so the pipeline silently ran every Writing review as Complete / Reporting Commentary. Complete was accidentally correct (default matched); Public was dropped and never engaged public calibration. The `visibility:null` log was a separate stale-field-name defect (`editorial-compliance-reviewer.mjs` logged `visibility` while the pipeline carries `requiredVersion`). Both fixed in R6.13.1; verified by 7→8 compliance rule-subset flip on Public + press_release. Regression guard added in R6.13.2. | R6.13.1 |
| Pr8 | **RESOLVED (B26, 2026-06-30)** — Reviewer Assessment reframe. Closed by **not** reframing Reviewer Assessment: **B26 / B26.1** takes detailed author-facing constructive feedback; Reviewer Assessment (`api/synthesize-review.js`) unchanged as short reviewer-facing overview. ROADMAP open list **#19**. | **B26** |
| B26 | **SHIPPED 2026-06-30** — Constructive Feedback chapter (**B26** base + **B26.1** consolidation). Tags: `b26.1-feedback-consolidation` (backend), `v8.56.0-b26.1-feedback-consolidation` (frontend). Card-derived feedback consolidated group-by-statement — one point per flagged statement, bundled by **`cardIndex`** (not `statementText`); statements ordered worst-first (conflicting/not_supported evidence → compliance-bearing → editorial-only, `cardIndex` ascending tiebreak); within bundle Evidence → Compliance → Editorial. **Supersedes** B26 global signal-tier point ordering. Register: `CONSTRUCTIVE_FEEDBACK_EDITOR_REGISTER` (editor-to-writer, warm-through-specificity, sparing earned praise, no schoolroom/system language). UI: scrollable modal, per-run cache keyed to `activeAnalysisRunId`, Regenerate, Escape/backdrop close, focus trap (**B26.1.1 / B26.1.2** folded). Resolves **Pr8** and ROADMAP **#19**. **Watch:** signoff duplication — subsection above. | See ROADMAP **Recently shipped → B26 / B26.1** |
| B26.2 | **SHIPPED 2026-06-30** — Document-level craft pass added to constructive feedback. Tags: `b26.2.1-craft-assembly` (backend), `v8.57.0-b26.2-craft-pass` (frontend). Second temp-0 LLM call reads the full reviewed-draft snapshot (`analysedDraftText`, snapshotted at QC-run time in `useDraftState` + `useAssessState`, keyed to analysis run id) across six FT-shaped dimensions (structure/flow, core-message clarity, conciseness & precision, register & tone, opening/closing, internal coherence). Draft-anchored document-level patterns; craft section first then card-derived points; one editor voice (`CONSTRUCTIVE_FEEDBACK_EDITOR_REGISTER`); single opening + continuous numbering (**B26.2.1** folded: preamble strip, renumber, strengthened dimension-6 figure-clash scan). Scoping decision #3 relaxed to draft-anchoring for craft section only; card points remain card-anchored. Internal coherence text-internal only. Independent of assess-path `lastAssessedDraftText`. Follow-up **B26.2.2** + **B26.2.4** shipped. | See ROADMAP **Recently shipped → B26.2** |
| B26.2.2 | **SHIPPED 2026-06-30** — Constructive feedback readability + UI polish. Tags: `b26.2.2-feedback-readability` (backend), `v8.58.0-b26.2.2-feedback-readability` (frontend). **Quote-length discipline:** short fragment + opening…closing ellipsis for long sentences in both craft and card prompts (prompt-only). **Figure-overlap dedupe:** craft owns internal-contradiction flag; card point on same figure addresses source reconciliation only — craft section passed to card pass + prompt instructions; neither signal suppressed. **Frontend:** Reviewer Assessment + Quality Review Summary + generate control in one bordered collapse container; generate button inline on REVIEWER ASSESSMENT header (caption removed); **Generate/View author feedback** button; modal title **Author feedback**. Resolves queued quote-length + figure-dedupe items from B26.2 testing. | See ROADMAP **Recently shipped → B26.2.2** |
| B26.2.4 | **SHIPPED 2026-07-05** — Output-type-aware craft pass. Tags: `b26.2.4-craft-output-type` (backend), `v8.59.0-b26.2.4-craft-output-type` (frontend). Frontend sends `outputType` on author-feedback request; backend normalizes (`normalizeOutputType` / `resolveConstructiveFeedbackCraftOutputType`; absent/invalid → null → generic craft). Normalized type threaded into craft call only — per-type calibration on the six existing dimensions (LinkedIn: conversational/first-person, hook opening OK; press release: factual lede, formal register; investor letter: salutation/narrative-arc, strict structure/register; reporting commentary: default). Card pass, selection, bundling, ordering, snapshot, `{ ok, feedbackText, isReady }` contract unchanged. Resolves **B35**. Editorial voice/register closed by **R6.12** (2026-07-05). | See ROADMAP **Recently shipped → B26.2.4** |
| R6.12 | **SHIPPED 2026-07-05** — Editorial output-type voice/register calibration. Tag: `r6.12-editorial-output-type` (backend). Prompt: `buildEditorialOutputTypeCalibration` in v4-combined + legacy editorial system prompts; `reviewerNoteByOutput.linkedin_post` on `voice_consistency`, `register_mismatch`, `structural_integrity`; `formatRulesForPrompt` emits editorial `reviewerNote`. Verified F12 (voice/register over-fire cleared) + F09 control. Closes editorial half of document-type-awareness gap (craft: **B26.2.4**). Residual watch: **W2** (F12 S6/S8). Resolves **Pr7**. | See ROADMAP **Recently shipped → R6.12** |
| SRC1 | **SHIPPED 2026-07-05** — Source publication-state pill alignment + override guard. Tag: `v8.60.0-src1-source-status-override` (frontend). Pill right-aligned in Assess and Drafting source rows (name → spacer → pill → remove). Manual override guarded against late-returning summarize-source inference via `publicationStateSource: "auto" \| "manual"`; shared `applySourceSummaryPatch.mjs` used by both hooks (skips `publicationState` write when `"manual"`, still applies `description`); covered by `tests/source-publication-state-patch.mjs`. No backend/payload/Compliance-calibration change — `publicationState` already consumed by Compliance (R6.4a). Partially resolves ROADMAP Active Backlog **#18(b)**. | See ROADMAP **Recently shipped → SRC1** |
| B34 | **SHIPPED 2026-07-05** — Remove Assess Review Settings auto-detect. Tags: `b34-remove-auto-detect` (backend), `v8.62.0-b34-remove-auto-detect` (frontend). Removed `trySessionAutoDetect`, `setDraftTextTracked`, override lock refs, and `isAutoDetectedType` badge from `useAssessState.jsx` / `AssessModule.jsx`; dropped `apiDetectOutputType`. Review Settings manual-only — default **Reporting commentary** / **Complete**. Backend `api/detect-output-type.js` and `detect-output-type` model-config stage entry deleted as dead code. No change to Drafting, generate/rewrite/analyse/export, or QC. | See ROADMAP **Recently shipped → B34** |
| B35 | **RESOLVED (B26.2.4 + R6.12, 2026-07-05)** — Constructive Feedback craft pass was output-type-blind. Craft layer closed by **B26.2.4**; editorial voice/register layer closed by **R6.12**. Document-type-awareness gap fully addressed across both layers. | **B26.2.4**, **R6.12** |
| Pr7 | **RESOLVED (R6.12, 2026-07-05)** — Editorial voice/register over-fire on LinkedIn posts (fixture 12). Closed by **R6.12** editorial output-type calibration. Residual watch: **W2**. | **R6.12** |
| B36 | **CLOSED 2026-07-05** — Vitest standardisation. `tests/constructive-feedback.test.mjs` (backend) and `tests/source-publication-state-patch.test.mjs` (frontend) converted to real Vitest suites (`describe`/`test`; assertions unchanged as `node:assert/strict`). `npm test` runs `vitest run` in both repos, scoped to `tests/**/*.test.mjs`. Other node:assert scripts unchanged — run via `node`. Future unit tests named `*.test.mjs` are auto-picked up by `npm test`. | Vitest standardisation 2026-07-05 |
| B25 | **SHIPPED 2026-07-05** — Verdict-label consistency across surfaces. Export (`api/export.js` `normalizeVerdict`) verdict strings aligned to card vocabulary — Confirmed / Partially confirmed / Conflicting / No support — with `supported_partial` split from `supported_full` (was collapsed to "Supported"). "not reviewed" → null and "Unverifiable" default unchanged. Conflict long-line in `displayVerdictLabels.js` (`evidenceVerdictLineFromCard` + `evidenceVerdictLineFromSupportState`) corrected "Conflicting sources" → "Conflicts with sources" (short badge "Conflicting" unchanged). Display strings only; no verdict enum/logic change. Tags: `b25-verdict-label-consistency` (backend), `v8.63.0-b25-verdict-label-consistency` (frontend). | See ROADMAP **Recently shipped → B25** |
| B29 | **SHIPPED 2026-07-05** — v4 review-toggle honouring + skipped-signal card rows (**B29** backend + **B29.1** frontend). Tags: `b29-v4-review-toggles` (backend), `v8.65.0-b29.1-not-reviewed-rows` (frontend). v4 now honours `editorialEnabled` / `complianceEnabled` / `evidenceEnabled` — each stage skipped when its toggle is off (evidence via existing skipped fast path; editorial/compliance halves gated in `runEditorialComplianceReview`). Deterministic aggregation unchanged. Frontend: evidence **Not reviewed** label + grey dot when skipped (fixed misleading **Confirmed**); **B29.1** adds **Not reviewed** grey rows for skipped editorial/compliance (keyed on `meta.reviewOptions`, not card verdict). Export already omits skipped-evidence line. All-off Review button already disabled. Resolves ROADMAP Active Backlog **#17**. | See ROADMAP **Recently shipped → B29 / B29.1** |
| B28 | **SHIPPED 2026-07-05** — Remove unused `eventType` from QC path (lean decision). Tag: `b28-remove-eventtype` (backend). Removed editorial DOCUMENT CONTEXT line (v3 split + v4 combined), `buildEditorialReviewContext` field, `evidence-skipped-fast-path` read, and `normalizeEventType` / `getEventTypeLabel` imports from reviewer. Compliance prompts already excluded it; no rule/verdict change. Generate/Rewrite event-type framing retained (**B32** / **R6.14**). Re-add to QC if R6.14 ships. Resolves ROADMAP Active Backlog **#3**. | See ROADMAP **Recently shipped → B28** |
| WR1 | **SHIPPED 2026-07-06** — Writing scaffold (PG demo). Tags: `wr1-writing-scaffold` (backend), `v8.66.0-wr1-writing-scaffold` (frontend). **Backend:** PG prompt library (`lib/prompt-library/pg-writing-prompts.mjs`) keyed `eventType` × visibility for `NEW_DIRECT_INVESTMENT` and `NEW_FUND_COMMITMENT`; consumed by `buildBasePrompt`; binding-precedence constraints; mandatory Methodology Note (`---METHODOLOGY---`); deterministic backstops (`pg-commentary-cleanup.mjs`, `normalizePgInvestmentName`, `applyPgFundCommitmentPostFilter`, sentence-boundary word-limit enforcement); canaries `pg_fund_exclusion_filtered`, `pg_word_limit_exceeded`. **Frontend:** `WritingInputModal`; Assess generate wiring (`useAssessState`); one version per Generate; Methodology Note panel (default collapsed, excluded from word count); Drafting **Configure text inputs** secondary button. **Demo sources:** `Shopify (text).txt`, `CVC VIII.txt`; Armitage IC memo as fund-filter stress-test fixture. **Learning:** source shape drives output — track-record-dense sources fight exclusions; close-memo/strategy-led sources generate clean. Generation path only; no QC change. Resolves **ROADMAP WSC1** / **Near-term — Writing scaffold**. Follow-on: **WR2**, **WR2.1**, **B32**. | See ROADMAP **Recently shipped → WR1** |
| A10 | **SHIPPED 2026-07-09** — Adapt into Assess sprint. Tag: `v8.51.0-rA10-adapt-into-assess` (frontend). **A10** Adapt modal + `handleAdapt`; labelled timeline entries with `meta.derivation`; full QC on adapted drafts. **A10.1** button/Beta/labels. **A10.2** / **A10.2.1** / **A10.2.2** derivation carry-forward, chip label, panel fill, scroll-to-top, resize-none. Backend `/api/adapt` unchanged. | See ROADMAP **Recently shipped → A10** |
| B40 | **SHIPPED 2026-08-09** — R7 Build B: offset population on `supportSpans`. Deterministic locate (exact `indexOf`, then repair-normalised with index map); authoritative-span-or-drop; offsets relative to stored source text; verdict path untouched. Tag: `r7-build-b`. | See ROADMAP **Recently shipped → R7 Build B** |
| B46 | **SHIPPED 2026-08-09** — R7 Build C: emit aligned source text + excludedSources in analyse-statements response (F12 data prerequisite). Deterministic; verdict path untouched. Tag: `r7-build-c`. | See ROADMAP **Recently shipped → R7 Build C** |
| B47 | **SHIPPED 2026-08-09** — Office-source ingestion fix: base64-encode docx/pptx/xlsx uploads so officeparser runs; backend rejects inline office-zip text (`OFFICE_INLINE_TEXT_NOT_ALLOWED`). Fixes Assess Office sources previously analysed as raw zip bytes. Tags: `b47-office-inline-guard` (backend), `v8.67.0-b47-office-upload-fix` (frontend). | See ROADMAP **Recently shipped → R7 F12** |
| F12 | **SHIPPED 2026-08-09** — R7.F12 Sources Drawer v1: reader (left source list + highlighted text pane), three openers (Sources button / row magnifier / card finding icon), verdict-coloured highlights (green/amber/red), per-span hover naming the statement. Tag: `v8.68.0-f12-sources-drawer`. | See ROADMAP **Recently shipped → R7 F12** |
| B6  | Wire Visibility (Complete vs Public) into rebuild — visibility now affects QC behaviour via R4.3 rule filtering and prompt calibration; Writing-path review intent wired in R6.13.1 (`versionType` / `selectedTypes` → `requiredVersion` / `outputType`). Residual Tier B `eventType` decision closed **B28** (2026-07-05). | R4.3 (`r4.3-visibility-wiring`), R6.13.1 (`r6.13.1-visibility-log-fix`), **B28** |
| F1  | **"Highlight in draft" link broken in Assess module.** **VERIFIED WORKING 2026-08-09** — F1 (Highlight in draft) + F2 (statement colour-coding) function in Assess via the in-place DraftOverlay path (R5.3a/b + R5.4), not a Draft Context pane. No dedicated fix tag; confirmed by code-path diagnostic + visual check. | Verified working 2026-08-09 |
| F2  | **Statement-level colour-coding broken in Assess module.** **VERIFIED WORKING 2026-08-09** — F1 (Highlight in draft) + F2 (statement colour-coding) function in Assess via the in-place DraftOverlay path (R5.3a/b + R5.4), not a Draft Context pane. No dedicated fix tag; confirmed by code-path diagnostic + visual check. | Verified working 2026-08-09 |
| F7  | **Reinstate colour-coding / Highlight in Draft in Assess (Draft Context limiter).** **RESOLVED 2026-08-09** — highlight + verdict colour-coding render in-place in Assess via DraftOverlay; the 'no Draft Context pane' limiter is outdated. If a fuller Draft Context pane is wanted later, log fresh. | Resolved 2026-08-09 |
| B45 | **SHIPPED 2026-08-11** — B45 AI-provenance URL scan: deterministic pre-Stage-1 scan flags AI-tool tracking params (utm_source=chatgpt.com etc.) in draft URLs; surfaced in the review summary. Additive, no verdict change. Tags: `b45-url-provenance-scan` (backend), `v8.69.0-b45-provenance-display` (frontend). | Sprint 1 (Attention-Router) |
| F13 | **SHIPPED 2026-08-11** — F13 excluded-sources display: the Sources drawer shows unreadable sources under 'Excluded · Not reviewed' with a plain reason; exclusion now catches scanned/image-only PDFs (extraction status `unsupported_scanned`), not just empty text. Tags: `f13-scanned-source-exclusion` (backend), `v8.70.0-f13-excluded-sources` (frontend). | Sprint 1 (Attention-Router) |
| Pr9 | **SHIPPED 2026-08-14** — Suggest revised draft: deterministic gather of all Review/Assess card concerns → single temp-0 LLM call → whole-draft holistic rewrite; finding-handling rulebook + note-wording pass. Tags: `Pr9-BE`, `Pr9-handling-BE`, `Pr9-notes-BE` (backend); `Pr9-FE`, `Pr9-handling-FE` (frontend). See ROADMAP **Recently shipped → Pr9**. | `Pr9-BE`, `Pr9-handling-BE`, `Pr9-notes-BE`, `Pr9-FE`, `Pr9-handling-FE` |
| B48 | **SHIPPED 2026-08-16** — Evidence conflict-vs-partial calibration. **VERDICT-ADJACENT** — shipped via diagnose-first + read-only shadow gate. Tag: `review-B48`. **LANDMINE (seen by anyone touching magnitude backstop / collectBackstopFigures):** do not add MOIC multiples to the magnitude backstop without a corresponding-passage check; that would code-force rich-mark / B115 false reds (F93_S2 shape). Cross-ref open **B115**. | `review-B48` |
| B109 | **SHIPPED 2026-08-27** — EA_E3 (eval-ablation Meridian): unrealised mark presented as realised return. Fixed by Stage 2 R10 quantity-scoped basis-conflict rule. Tag: `stage2-basis-conflict-r10` (`971370f`). Graded `8dc6be7`; blast `ce3d85e`; production verify `baed6ed`. Not claim-spans CS_E3; not corpus E3:S0:ic_memo. | `stage2-basis-conflict-r10` |
| R3a-package | **SHIPPED 2026-08-26** — Stage 2 rewrite R3a: five known false-green shapes fixed (EA_E2, claim-spans CS_E3 on `source_ic_memo.txt`, F01_S10, F04_S20, F12_S0). Tag: `stage2-rewrite-r3a` (`7ff4aa4`). Verified in Production (`72f61aa`). Sixth shape (eval-ablation EA_E3) closed separately by **B109** / R10. Arc write-up: `claude/stage2-prompt-saturation.md` (Claude project doc). Accepted costs from that ship: **B110**, **B116**. | `stage2-rewrite-r3a` (`7ff4aa4`) |
| B84 | **CLOSED 2026-08-27 with the false-green arc.** Route A closed earlier by `review-upgrade-off`. The six known Stage 2 false-green shapes are fixed (R3a five + R10 EA_E3); residuals have their own rows (**B110**, **B115**-**B117**, **B118**). A new false green gets a NEW row. Arc diagnosis: `claude/stage2-prompt-saturation.md` (Claude project doc). | `stage2-rewrite-r3a`; `stage2-basis-conflict-r10`; false-green arc 2026-08-27 |
| B111 | **SHIPPED 2026-08-26** — `extractPercents` now matches `"40%"` (trailing `\b` after `%` removed; keep `\b` on spelled `per cent` only). Zero corpus verdict changes. Commit `1581bb8` (untagged). Tests in `tests/stage2-b48-calibration.test.mjs`. Related earlier tags `review-percent-extract` / `review-percent-metric-scope` / `review-percent-number-style` did not cover this bug. | `1581bb8` |
| B13 | **SHIPPED 2026-08-16** — Stage 5 material-vs-pedantic partial distinction / materiality signal. Tag: `review-B13`. | `review-B13` |
| F8  | **SHIPPED 2026-08-18** — `number_spelling` quarter-notation deterministic backstop (`isQuarterNotationSpan`). Tag: `review-F8`. | `review-F8` |
| F9  | **SHIPPED 2026-08-18** — Duplicated suggestion-text de-noiser in editorial concern rendering. Tag: `review-F9` (frontend). | `review-F9` |
| B37 | **SHIPPED 2026-08-18** — Framing-goes-beyond-source flag collapsed to framing-escalation; material-only. Tags: `review-framing-BE` (backend), `review-framing-FE` (frontend). | `review-framing-BE`, `review-framing-FE` |
| B49 | **SHIPPED 2026-08-18** — Source-recency flag: structural source as-of-date extraction (`extractSourceAsOfDate`) + 18-month, claim-aware present-tense recency signal; additive, verdict-safe. Tags: `review-recency-BE` (backend), `review-recency-FE` (frontend). | `review-recency-BE`, `review-recency-FE` |
| B50 | **SHIPPED 2026-08-18** — Card density UX (Option A compact clean-line): clean dimensions collapse to one quiet line. Tag: `review-card-density` (frontend). | `review-card-density` |
| B51 | **SHIPPED 2026-08-18** — Source supersession (verdict-layer): period-aware; newer dated source's figure supersedes an older different-period source; draft-matches-current → supported + historical note (`qcCard.supersededSourceNotes`); same-period restatement and draft-behind stay conflict; confident-dates-only; reconciled with B48 magnitude backstop. New module `lib/qc/supersession.mjs`. Fixture `scripts/diagnostic/supersession/`. **VERDICT-ADJACENT** — shipped through diagnose-first + read-only shadow gate. Tag: `review-supersession`. | `review-supersession` |
| B52 | **SHIPPED 2026-08-19** — Recency anchoring + first-person-plural FP fixes: `recencySourceIndices()` anchors to supporting source (confirmed/partially_confirmed); `STYLE_RULE_DETERMINISTIC_FILTERS.first_person_plural` requires a real pronoun; `suppressNoOpSuggestions()` drops any house-style concern whose `suggestedRewrite` equals the statement text. Tag: `review-recency-anchor-firstperson`. | `review-recency-anchor-firstperson` |
|     | **SHIPPED 2026-08-24.** First-person removal names the actor. `first_person_plural` and `voice_consistency` share a substitution contract: replace the pronoun with the named authoring organisation as the grammatical subject; never recast agentless or passive; preserve hedges. Fixes a live defect where a style rule could strip the owner from a forward-looking return statement, turning a hedged opinion into an unattributed institutional prediction. Observed identically across two production runs on the same draft. Actor identified only when a known house name already appears in the draft; otherwise flag and leave first person in place. Harness: `scripts/diagnostic/first-person-actor-harness.mjs`. Tag: `review-first-person-actor` (`63de82f`). Live cards on the Meridian re-run took the fallback path. Closed by **B86** / `review-actor-supply` (`9285e95`). | `review-first-person-actor` (`63de82f`) |
|     | **SHIPPED 2026-08-24.** Authoring organisation is configuration (`AUTHORING_ORGANISATION`) rather than a hardcoded one-entry list. Production default was a real firm. Synthetic fixtures de-identified to the fictional house Halden Group. Tag: `review-actor-config` (`cc69abc`). Default removed by **B86**. Deployed env is now the fixture name (**B96**). | `review-actor-config` (`cc69abc`) |
| B86 | **CLOSED 2026-08-24 by `review-actor-supply` (`9285e95`).** Diagnosis inverted the fix. There was no detection. The resolver read an env var, fell back to the constant "Partners Group", and then tested whether that already-chosen name appeared in the draft. Since the env var was unset, every production review ran with a real firm as the assumed author, and the named-actor path fired only on a draft containing that firm's name. The substitution inserts the CONFIGURED name, never a name read out of the draft, so the presence check is the only thing preventing the tool writing one firm's name into another firm's document. It is safety, not identification. Do not loosen it. Fix: default is now null, a request-body supply route was added, and the invariant has its own test. Residues: the request-body path has never executed (**B95**); Production and Preview env is the fixture name Halden Group (**B96**). | `review-actor-supply` (`9285e95`) |
|     | **SHIPPED 2026-08-24.** Real published press releases restored to the real house name. Real-versus-invented principle recorded in `scripts/diagnostic/README.md`. Tag: `fixtures-real-vs-invented` (`2f5f403`). | `fixtures-real-vs-invented` (`2f5f403`) |
|     | **SHIPPED 2026-08-24.** A parenthetical view-marker is deleted when the sentence subject is already the named actor, and converted when it is not. Tag: `review-view-marker` (`bd45d08`). Follow-on: `review-view-marker-subject` evaluates that test after first-person substitution. | `review-view-marker` (`bd45d08`) |
|     | **SHIPPED 2026-08-24. Added straight to Closed; was never an open backlog row.** The delete-versus-convert rule lived only in the prompt, which already said to apply the test after the first-person substitution. The model scored the original sentence, where the subject was still "We", and converted where it should have deleted. Now backstopped deterministically: if substituting We/we will make the authoring organisation the grammatical subject, a parenthetical view-marker is deleted, not converted. Tag: `review-view-marker-subject` (`a448e18`). | `review-view-marker-subject` (`a448e18`) |
|     | **SHIPPED 2026-08-24.** Unsupported evaluative language is deleted rather than substituted for a milder word. Same laundering shape as approximating an unsupported figure. Tag: `review-hype-delete` (`63c359f`). | `review-hype-delete` (`63c359f`) |
|     | **SHIPPED 2026-08-24.** Evaluative-deletion directions state the resulting phrase so literal application does not leave stranded scaffolding. Tag: `review-hype-repair` (`02a0212`). | `review-hype-repair` (`02a0212`) |
|     | **SHIPPED 2026-08-24.** A Delete restatement that re-authors the sentence is discarded. Compared to clause-minus-span after whitespace collapse and orphan punctuation strip; Levenshtein budget 2. Divergence emits the refusal form. Tag: `review-direction-bound` (`eef3f3c`). Residues closed by `review-direction-actionable`. | `review-direction-bound` (`eef3f3c`) |
| B87 | **CLOSED 2026-08-25 by `review-direction-actionable` (`5ff7ced`), verified on a production run.** "a track record that is, in our view, genuinely exceptional" became "its track record". Hype word deleted, no milder substitute, view marker deleted, nothing stranded. That is chapter two's never-substitute rule executing on a live draft for the first time. The fix was three causes, not one: the actionable refusal form; a live note leak (`gatherConcerns` passes both `note` and `suggestedDirection`, so the reviser was receiving the discarded restatement and the refusal form simultaneously); and a soften-kind instruction in the revision prompt telling the reviser to leave wording unchanged. All three were fixed in one commit, which by the bundling rule should have been split. Acceptable only because the needed outcome was binary. The attribution is still clean: run three had the actor resolved and the inert form, and the phrase survived; the production run has the actor resolved and the actionable form, and it does not. | `review-direction-actionable` (`5ff7ced`) |
| B100c | **CLOSED 2026-08-25 as not a defect.** The Stage 2 fingerprint is OpenAI's `system_fingerprint` copied off the completion, not a hash of prompt or source. It is not in the LLM cache key (`COMPONENT_ORDER`: `stage`, `inputText`, `parentSentence`, `sourceText`, `promptHash`, `modelId`, `temperature`, `seed`, `cacheVersion`). Two documents cannot collide. `QC_LLM_CACHE_DISK` is safe on this count. Open **B100** (a) and (b) remain. | code read 2026-08-25; not a defect |
|     | **SUPERSEDED 2026-08-25.** `review-evaluative-anchor` (`82bb870`) added a phrase-lookup residual guard on the claim upgrade. Correct work on a path that turned out not to be the live one. It is not a revert; the accident fixtures it produced are what proved the distinction. Closed as SUPERSEDED by `review-upgrade-off`. | `review-evaluative-anchor` (`82bb870`); SUPERSEDED |
|     | **CLOSED 2026-08-25 by `review-upgrade-off`.** Disable the claim upgrade. Across 296 corpus cards, exactly one reached `rollupClaimVerdicts` with `vToday=partially_confirmed` and all claims confirmed, and that one was synthetic E1, where the upgrade produced a false green. On 254 production fixture cards the upgrade fired zero times; every confirmed card was already confirmed at Stage 3. Zero observed correct firings, one observed incorrect one. Every Meridian production card carried `claimUpgrade` false. Route A of **B84** is closed. | `review-upgrade-off` |
| B88 | **SHIPPED BEHIND A FLAG 2026-08-25. NEEDS RE-ASSESSMENT (not parked).** Statement-level unsupported span for the reviser. Spans exist only when `QC_STAGE2_SPAN` is ON; that flag also governs the wire (no second switch). Tags: `review-stage2-span` (`04f4779`, SUPERSEDED appended-prompt path, removed from tree, recoverable at tag only); `review-span-two-step` (`76032fc`, two-step elicit, zero verdict deltas); `review-span-on-card` (`2d1edd3`, `qcCard.unsupportedSpans`); `review-span-wired` (`e6bee3a`, concern block names shorter validated spans, suppresses whole-statement spans); `review-span-wired-gate` (`26145a3`, three-fixture three-repeat revision measurement, report only). **2026-08-27:** evidence base is stale (verdicts changed under R3a and R10; Review now emits plain-language reasoning naming the specific problem). Re-measure value before further span investment. Residuals: **B105**, **B106**; Route B of **B84**. **B107** closed 2026-09-08 (`9f8fc41`). | `review-span-wired`, `review-span-wired-gate`, `review-span-on-card`, `review-span-two-step` |
|     | **UNEXERCISED 2026-08-25.** Multi-source coverage-union promotion behind `QC_MULTISOURCE_COVERAGE` (inert unless `QC_STAGE2_SPAN` also ON). Diagnostic population zero; gate promotions zero. Premise (jointly covered partials stay partial) was refuted by any-confirmed-wins already greening the demos. Not working, not disproven. Tags: `review-coverage-diagnostic` (`a5a8bef`), `review-coverage-union` (`ec639b7`). Cross-ref **B105**. | `review-coverage-diagnostic`, `review-coverage-union` |
| P17 | **SHIPPED 2026-08-24.** Invented material must not carry real names, of firms or of people. The harm is fabricated claims wearing a real name, not the name itself. Real published documents keep their real names, because they contain only what was actually said in public. Fixture `04_synth_vc_pinterest_style_memo` From-line de-identified to Nathan Calder / Helen Rusk. Same principle as the house-name rule, applied to individuals. Commit: `acdfcb8`. | `acdfcb8` |
| Pr15 | **CLOSED 2026-08-24 and stays closed.** Pr9 narrative-flow question, the original complaint. The end-to-end Meridian re-run was done and the revised draft was read. The flow holds. The unsupported equity cheque clause was cut rather than softened, a conflicting IRR figure was corrected, an unsupported evaluative word was deleted correctly, a compliance qualifier was added, an overlong sentence split into two that read properly, and the closing recommendation kept its owner rather than becoming an unattributed institutional prediction. The two contributing rules were `first_person_plural` and `marketing_language_excess`, both addressed in the review-layer chapter (chapter two). Subsequent re-runs after this close were about the named-actor path, not narrative flow. Mechanical residual **B87** is now closed (`review-direction-actionable`, `5ff7ced`); that was never a reason to reopen Pr15. Tags already shipped: `review-first-person-actor` (`63de82f`), `review-hype-delete` (`63c359f`), `review-hype-repair` (`02a0212`). | Meridian re-run 2026-08-24; named-actor reconcile 2026-08-25; chapter five 2026-08-25 |
| F14 | **SHIPPED 2026-08-19** — `number_spelling` false positive on spelled-out 0–12 (e.g. "twelve" / "twelve months"). Deterministic filter drops cited spans that contain a spelled-out 0–12 word; durations treated as prose counts; units carve-out = measured physical units only. Tag: `review-percent-number-style`. | `review-percent-number-style` |
| B54 | **SHIPPED 2026-08-19** — `percentage_notation` under-fire on British "per cent". Rule text now names "per cent" alongside "percent"; drop-filter uses `\bper\s?cent\b` (still drop-only). Tag: `review-percent-number-style`. | `review-percent-number-style` |
| B59 | **SHIPPED 2026-08-19** — `extractPercents` "per cent" visibility + same-metric magnitude guard. `extractPercents` matches `per\s?cent` so supersession and the B48 magnitude backstop see per-cent figures; `hasEgregiousMagnitudeGap` requires a non-empty metric-key intersection before forcing a **percent** conflict (fixed cross-metric false pair: 19% margin vs 70% contracted). Money/count later guarded by **B60** / **B60.1** (shipped). **VERDICT-ADJACENT** — shipped via diagnose-first + read-only shadow gate. Tag: `review-percent-extract`. | `review-percent-extract` |
| B53a | **SHIPPED 2026-08-19** — Internal claim spans with upgrade-only rollup. One card per sentence retained; whole-sentence match stays authoritative; claims may only upgrade `partially_confirmed` → `confirmed`. Full-corpus shadow: exactly one verdict change (**F06 S5**, signed off) and zero regressions. Determinism harness: verdict-unstable statements 1/38 flag OFF → 0/38 flag ON. Flag `QC_CLAIM_SPANS` default ON; set `0`/`false`/`off` to disable without a deploy. Tags: `review-claim-spans` (`c290cee`), `review-claim-spans-on` (`e6e59a6`). Untagged infra/diag in the same arc: `48d8483` (harness, sizing, shadow baseline cache), `b81929b` (seed, `systemFingerprint`, concurrency cap, two-arm harness, word-boundary connectives). Residuals: **B61** (hasConflict only; B63 does not close it), **B53b**. **B62** and **B66** parked 2026-08-21 (funnel widening measured, no additional upgrades). | `review-claim-spans`, `review-claim-spans-on` |
| B67 | **CLOSED 2026-08-19** — Draft-achieved vs source-projected figure. Prompt does not name this direction; planted probe did not confirm it. Draft: "ARR reached EUR 95 million." Sole source occurrence: IC memo "Our base case envisages ARR growth to approximately EUR 95 million." Stage 2 on that pair: **`conflicting`** (completed action vs future projection). Card verdict: **`conflicting`**. No spec. **Preserve:** the IC memo catch is behaviour to keep, not to change — any **B60** fix must still return `conflicting` on that pair. Press-release / fact-sheet false conflicts on EUR 155 million revenue are **B60**, fixture `scripts/diagnostic/b67-probe/`. | `scripts/diagnostic/b67-probe/` |
| B63 | **SHIPPED 2026-08-20 as a COST OPTIMISATION only.** LLM result cache on the verdict path (Stages 1, 1b, 2). Flag `QC_LLM_CACHE` default ON; set `0`/`false`/`off` to disable without a deploy. Process-local in-memory LRU (`qc_llm_cache`; 1024 entries / 16 MiB). Does not survive a cold start or an instance change, so cross-session repeatability is not delivered. **Does NOT close B61.** Tags: `review-llm-cache` (`e6fd99c`), `review-llm-cache-on`. | `review-llm-cache`, `review-llm-cache-on` |
| B69 | **SHIPPED 2026-08-20.** Disk-backed LLM cache for local diagnostics. `createDiskStore` in `lib/qc/llm-cache.mjs`; enabled only when `QC_LLM_CACHE_DISK` is a path. Diagnostic scripts default that path to gitignored `scripts/diagnostic/.llm-cache.json`. Live-measurement scripts (`run-gate`, `run-stage1-stability`, `stage2-determinism`) force the cache off. Does not close B61. Tag: `review-cache-disk`. | `review-cache-disk` |
| B64 | **SHIPPED 2026-08-20.** Claim-spans anchor test also sees spelled-out numbers (one to twenty, tens, hundred/thousand/million/billion), all-caps acronyms of two or more letters, and capitalised tokens that are not the first token of the parent sentence. Local to `extractClaimSpanAnchors` in `lib/qc/claim-spans.mjs`; used by claim validity and `residualHasUnclaimedAnchor`. Pre-filter, `collectBackstopFigures`, and materiality unchanged. Shadow: `anchorless_claim` 14 to 8, decomposed 20 to 26, zero verdict/`hasConflict` changes. Tag: `review-claim-anchors`. | `review-claim-anchors` |
| B70 | **SHIPPED 2026-08-20.** `extractMoney` treats plain `m` as million (case-insensitive, word boundary) only in the currency-prefix alternative. `$155m` / `$155M` / `EUR 155m` scale to 155 million; `155m` / `155 m` with no currency stay non-money. Fixes four corpus magnitude-backstop false conflicts against `EUR 155 million`. Tag: `review-money-scale`. Shadow shared with **B60**. | `review-money-scale` |
| B60 | **SHIPPED 2026-08-20.** Money figures carry a single canonical `metric` id from a longest-first word-boundary phrase list. The magnitude backstop suppresses a forced money conflict only when both sides resolved and the ids differ (fail closed). Count and percent untouched. Tag: `review-money-metric-keys`. Shadow: `scripts/diagnostic/b60-money/run-shadow.mjs`. Character-window lookup replaced by **B60.1**. | `review-money-metric-keys` |
| B60.1 | **SHIPPED 2026-08-20.** Money metric resolution uses the sentence containing the figure (Stage 1 fallback splitter), nearest phrase by character distance, whole-passage fallback if no boundary. Replaces the 48-character window that both missed a 71-character phrase and truncated `annual recurring revenue` to `revenue`. Percent untouched. Tag: `review-money-metric-scope`. | `review-money-metric-scope` |
| B72 | **SHIPPED 2026-08-20.** Percent figures carry one canonical `metric` id, sentence-scoped like money (shared `metric-scope.mjs`). Longest-first word-boundary phrases; nearest match. `margin_unspecified` does not match `gross_margin` / `ebitda_margin`. Guard forces a percent conflict only when both sides resolved and the ids match. Probe: gross 45 vs EBITDA 19 no longer forces. Tag: `review-percent-metric-scope`. | `review-percent-metric-scope` |
| B71 | **SHIPPED 2026-08-21.** Money figures record a recognised currency (USD/EUR/GBP/AUD/CAD from symbol or ISO code). The magnitude backstop does not force when both sides have a currency and those currencies differ; unknown currency fails closed. Tag: `review-money-currency`. | `review-money-currency` |
| period-overlap | **SHIPPED 2026-08-21.** No B-id assigned. Two figures whose parseable periods do not overlap cannot contradict. Magnitude backstop no longer forces; period gate rewrites `confirmed`/`conflicting` to `no_support` and names both periods. Unparseable fails closed. Tag: `review-period-overlap` (`8f8eb67`). | `review-period-overlap` |
| B65 | **SHIPPED 2026-08-21.** Stage 1b prompt requires character-for-character copies; forbids inserting entity names from earlier in the sentence; a pronoun or bare verb phrase is a valid claim start. Gestcompost / Partners Group negative examples. Tag: `review-claim-fidelity` (`fe57d1d`; first commit `7d6cf6b` untagged, tag moved to the F10 follow-up). | `review-claim-fidelity` |
| B53c | **SHIPPED 2026-08-21.** Superlative and record check in framing fidelity. Vocabulary: record, highest, lowest, best, strongest, unprecedented, first ever, all-time, never before. Fires only when the phrase is absent from matched source passages. Editorial only; evidence verdict and hasConflict unchanged. Tag: `review-superlative`. | `review-superlative` |
| B73 | **SHIPPED 2026-08-21 as INSURANCE, not a live-path fix.** `splitDraftIntoCandidatesV2` no longer treats a dash as a sentence boundary. Production Stage 1 uses the LLM extractor and already kept F19 S2 whole; fallback (maxLen 240) and D-METRIC-LLM had cut the 257-character sentence at dashes. Colons and semicolons unchanged. LLM card texts and verdicts unchanged on the gate (100% cache). See open **B75**. Tag: `review-splitter-dash`. | `review-splitter-dash` |
| B76 | **SHIPPED 2026-08-21** — Durable review-state store on Neon Postgres: `GET` / `POST` / `DELETE` `/api/review-state`. Autosave buffer (one row per review, overwrite is correct), not an audit record. Tag: `persist-review-state` (`8af163d`; store commit `efccb11`). | `persist-review-state` |
| B77 | **SHIPPED 2026-08-21** — Localhost CORS on the review-state route only (production frontend origin plus echoed `http://localhost:<port>` / `http://127.0.0.1:<port>`). Other routes unchanged. Tag: `review-state-cors-local` (`c5ff204`). | `review-state-cors-local` |
| F15 | **SHIPPED 2026-08-21** — Autosave and restore the in-progress review so a refresh no longer loses work. One active review per browser; no list, picker, or history UI. **Accepted limitation (see open F16):** a restored PDF or Office source needs re-uploading before Review can run again. Follow-on: **F17**, **F18**, **F19**. Tag: `v8.71.0-review-state-restore`. | `v8.71.0-review-state-restore` |
| F17 | **SHIPPED 2026-08-21** — Delete the abandoned `review_state` row on New output / `startNewReview` (best-effort `apiDeleteReviewState` of the outgoing id). Temporary snapshot-size log (removed in **F19**). Tag: `v8.72.0-review-state-cleanup`. | `v8.72.0-review-state-cleanup` |
| F18 | **SHIPPED 2026-08-21** — Stop storing raw source file bytes (`contentBase64`) in the saved review state. Measured: two PDFs 3.1 MB + 5.3 MB were 8,762,415B (8,761,484B base64) against a 4,194,304B save limit; a later post-Review session with one large PDF serialised to 47,182B. **Accepted limitation:** restored PDF/Office sources cannot run Review again until re-uploaded, because the backend refuses inline text for PDFs so extraction always happens server-side from the original. Tag: `v8.73.0-review-state-no-blobs`. | `v8.73.0-review-state-no-blobs` |
| F19 | **SHIPPED 2026-08-21** — Pill "Re-upload to run again"; Review `title` explains the disabled control; snapshot-size log removed; `[API BASE DEBUG]` gated to Vite DEV. Tag: `v8.74.0-review-state-tidy`. | `v8.74.0-review-state-tidy` |
| F20 | **SHIPPED 2026-08-22.** Client-side size guard. Refuses oversized sources and drafts before the request is sent, with a clear message naming the size and the remedy. Shared budget module (`sourceRequestBudget.js`). Untagged follow-up `3fce4fb` brought the Assess draft-upload path onto the same budget and removed a stale 10 MB PDF cap, so there is exactly one size constant in the frontend. Tag: `v8.75.0-source-size-guard` (`d0bbcc5`). Follow-up: `3fce4fb`. | `v8.75.0-source-size-guard`, `3fce4fb` |
| F21 | **SHIPPED 2026-08-22.** Size-limit messages no longer claim the limit is specific to Review, because the same upload feeds Drafting. Tag: `v8.76.0-size-message-wording` (`16a8cd1`). | `v8.76.0-size-message-wording` |
| B78 | **CLOSED 2026-08-22 as PROVEN, not as fixed.** The 2026-08-21 probe (upload a 5+ MB PDF) completed in production with an 11.3 MB PDF: Vercel 413 at the edge, CORS-masked as `TypeError: Load failed`. Standing limitation is open **B79** (GUARDED). Guard shipped as **F20** / **F21**. | production 11.3 MB PDF 2026-08-21 |
| P11 | **SHIPPED 2026-08-21** — Node `engines` pinned to **22.x** (Vercel production Node 22 LTS, not 24). Backend commit `86ada5d` (untagged itself; contained in `persist-review-state` / `review-state-cors-local`). Frontend commit `888f6a8`. | `86ada5d`, `888f6a8` |
| P12 | **SHIPPED 2026-08-21** — Vercel functions pinned to Frankfurt (`regions: ["fra1"]`). Commit `060d1ce` (untagged itself; contained in `persist-review-state` / `review-state-cors-local`). | `060d1ce` |
| Pr9-correctness | **SHIPPED 2026-08-23.** Rewrite correctness arc. Together these four tags fixed a revision that could attach a note claiming a removal to text it left untouched, reproduced 3/3 on the production sentence. `pr9-claim-spans` (`d8ab2df`) feeds per-claim evidence spans into the rewrite prompt. `pr9-soften-or-cut` (`0cd76a5`) applies one test: soften, cut the clause, or keep-and-flag; never approximate an unsupported figure. `pr9-marker-intent` (`00cce35`) declares CHANGED / KEPT / CUT with a deterministic honesty check. `pr9-cut-punctuation` (`71500c4`) tidies punctuation left behind by a clause cut. Residuals still open: **B80**, **B81**. Honesty repair-policy follow-on shipped as **B119** (`ade84fc`). **SUPERSEDED IN PART, 2026-08-29 — this row describes behaviour that no longer exists.** `pr9-soften-or-cut` (`0cd76a5`) is described above as applying one test — soften, cut the clause, or keep-and-flag. **The soften and cut branches were removed when the silence rule was reversed on 2026-08-29.** **The product no longer edits the claim on silence:** where a source is silent or vague on what the draft asserts, the claim is left as written and flagged (`build-revision-prompt.mjs` rule (b)). **B134** later permitted one first-person subject replacement on a silent card. Whole-sentence deterministic removal no longer runs; `api/suggest-revision.js` passes `deterministicUnsupportedRemoval: false` at both call sites. Keep-and-flag is the default on silence. This is a closed row kept accurate, not a reopen. Deletion of the now-dead machinery is **B126**. **Precision:** the editorial `soften` route for `marketing_language_excess` in `classifyEditorialKind` is untouched and still live — **B82** is unaffected. **Pr15** closed 2026-08-24 (Meridian re-run; flow holds). Process: **P15**. Narrative Claude project docs for this arc live outside the GitHub tree (sync does not carry `claude/*`); in-repo record is this row and ROADMAP Pr9 rewrite-correctness. | `pr9-claim-spans` (`d8ab2df`), `pr9-soften-or-cut` (`0cd76a5`), `pr9-marker-intent` (`00cce35`), `pr9-cut-punctuation` (`71500c4`) |
| B119 | **SHIPPED 2026-08-27.** Marker honesty repair policy after Suggest measure `25ae739` / diagnosis `b487e61`. Follow-on to closed **Pr9-correctness**, not a reopen and not folded into **B80** or **B81**. Intent flip to KEPT on unchanged-span contradictions; `remnant_missed_edit` keeps accurate notes when the containing sentence changed; pure keep-language note vs CHANGED/CUT intent mismatch flips to KEPT. `classifyNoteClaim` moved to `lib/pr9-marker-note-claim.mjs`. Tag: none (untagged commit). Deliberately not fixed here: rewrite coverage (**B120**), judgement-to-fact recital (**B121**). | `ade84fc`; diagnosis `b487e61`; Suggest `25ae739` |
| B3  | **CLOSED 2026-08-28 as a CORRECTION, not a completion.** The premise was wrong. OpenAI prompt-prefix caching was already ACTIVE on the Stage 2 path at a **97% hit rate**, with no opt-in and no Statement-then-Source reordering required; the reordering fix this row specified was never needed. What was actually missing was measurement and billing: `calculateLlmCostUsd` discarded the cached-token count and billed every input token at the full rate. `1560579` captures `prompt_tokens_details.cached_tokens` and bills it at the `cachedInput` rate, and adds the missing `gpt-5.1` / `gpt-5.1-2025-11-13` price entries. **Consequence for historical figures:** `calculateLlmCostUsd` returns zero for any model not in its table, so **every reviser cost this project reported before `1560579` was ZERO** — `writing-generate`, `writing-rewrite`, `adapt`, `ask-query` and `query-sources` all run on `gpt-5.1`. Any pre-`1560579` cost figure for those stages is **wrong, not imprecise**. Do not quote one. Stage 2 (`gpt-4o`) was always priced and is unaffected. Cross-ref **B38** (batch, still parked). | `1560579` |
| B122 | **CLOSED 2026-08-30.** Opened as "an editorial instruction naming an exact span is followed about seven times in ten, cause unknown". Every clause of that was wrong. Call-splitting had already been ruled out. Three diagnostic passes (`0d12440`, `ee170f7`, `cf32a76`) showed the 29 of 42 whole-draft number was not a Suggest follow-rate. What it was made of: **3 cells** the scorer could not register a success (`structural_integrity`; a possessive apostrophe truncated the target to `The team`; the rewrite was followed 3 of 3, verbatim). **3 cells** the scorer credited the wrong directive (r10-review1 S1 voice scored 3 of 3 under disappearance, 0 of 3 under destination; the edit was made for voice, not for the marketing delete). **6 cells** Review emitted two directives that cannot both be satisfied (r10-review1 S1; **B131**). **3 cells** Review emitted a wrong directive and Suggest obeyed it (r10-review2 S7 duplicate subject; **B132**). **The rest** silence-never-edits beating a craft directive on a mixed unsupported-plus-craft card (**B134**, now shipped). Operator observation 2026-08-31: the two scorer errors pointed in opposite directions and nearly cancelled, so the headline looked stable and meant nothing. Split: **B131** **B132** **B133** (Review) and **B134** (Suggest style carve-out, shipped). Protect from **P29**: see that row (B122 files plus `b134-carve-out-gate.json` and `suggest-call-record.mjs`). | `0d12440`, `235def4`, `4f88ad2`, `ee170f7`, `cf32a76` |
| B134 | **SHIPPED 2026-08-30.** Opened as decided-not-built. Live at tag `revise-style-carve-out` (`c0e1482` feat, `ef6bdbc` test, `6c05d9b` gate). Silence protects the CLAIM, not first-person voice. One permitted operation: replace we/our/us with the named authoring organisation, hedges preserved, actor preserved. B134 changed silence to protect the claim rather than first-person voice. It shipped on coherence: the prompt had been giving Suggest contradictory instructions on the same statement. The only same-process comparison of the amendment was 3 of 3 against 3 of 3. Do not cite B134 as a measured improvement in directive follow. Shipped on a VACUOUS gate: the reference arm was also 3 of 3 on the primary, and the coverage-gap S5 blocking control read 0 of 3 against 0 of 3 on a cell known to flap, so it discriminated nothing. S9 held 12 of 12 byte-identical; the control eight held; no actor stripped. Confirmed LIVE x3 on Production Meridian. | `revise-style-carve-out` (`6c05d9b`); feat `c0e1482`; test `ef6bdbc` |
| B138 | **SHIPPED 2026-08-30.** Live silence breach: "This relationship enabled deep insight during the diligence phase." Evidence `not_supported`. Review raised `overreach_unsupported_causal` (correct). Suggest rewrote it to "provided valuable insights". Re-review still unsupported. **Withhold, do not suppress:** `collectEditorialConcerns` copies only `voice_consistency` / `first_person_plural` onto the reviser when `resolveEvidenceKind` is `no_support`. Card `editorialConcerns` untouched. Prompt unchanged. Gate: `tests/b138-silent-card-allowlist.test.mjs` (PRIMARY absent from prompt; 13 locks; card still carries the flag). **Size:** 1 of 14 stored directives withheld; the corpus understates exposure — the protected class is any non-first-person editorial directive on a silent card. Composes with **B132** (Review filter removes the false member of the class). Child of **B136**. Do not bundle. Confirmed LIVE x2 on Production Meridian. | `b138-silent-card-withhold` (annotated; peels to `ac618b1`); feat `4baffc4`; test `ac618b1` |
| B139 | **SHIPPED 2026-08-30.** B139 reconciled two contradictory instructions in the reviser prompt: the silence rule forbade deleting evaluative language on an unsupported sentence while house style required it across the whole draft. The gate was a text assertion. No behavioural evidence was gathered, and none exists either way. Do not cite B139 as a measured change in what Suggest does. Suggest-only: `hyperbole_vs_qualitative` dropped from the L1085 entire-draft pointer and from `buildResolvedStyleGuideBlock`; it can arrive only as a named concern (B138 withholds that on silence). Review still receives the shared evaluative-language contract. **Principle:** formatting house-style rules are safe to apply globally; rules that can change what a sentence asserts are not. **Watched, not fixed:** `active_voice_preference`, `sentence_structure_clarity`, `register_consistency` remain entire-draft; none has been seen to fire on a silent card. Fixture: `scripts/diagnostic/revise/b139-silent-hyperbole.json` (P29 protected). Companion leftover of B134, own row. **Falsifier fired once and is RESOLVED AS FLAP:** deleted, retained, deleted across three live Production Meridian runs. Operator observation 2026-08-31. Not over-scoped. | `b139-hyperbole-reconciliation` (annotated; peels to `6e6f5ee`); feat `e2277d7`; test `6e6f5ee` |
| B140 | **SHIPPED 2026-08-31.** Confine KEPT honesty region to the marker's own sentence. Live at tag `b140-kept-honesty-confine` (`c469fac`). `applyMarkerHonestyCheck` KEPT-only: when spanStatus is CHANGED, `confineRegionToOwnSentence` is applied and a neighbour-sentence edit no longer makes a byte-identical KEPT span look rewritten. The gate was NOT vacuous: the new test was shown failing on the reference code first, and exactly one of eight stored rows flipped, the predicted one. LIVE x1, both halves. Out of scope on purpose: **B141** (LEFT leak, 2 of 8 stored rows) and **B142** (generic note replacing a specific one). Distinct from **B80**: B140 was a comparator lie; B80 is a labelling residual. Do not change the honesty check to paper B80 over. | `b140-kept-honesty-confine` (`c469fac`) |
| Pr17 | **SHIPPED 2026-09-01–02. No action-list tag.** Per-finding accept / reject / modify / apply on Review (`/assess`), behind `REVISE_ACTION_LIST` and `VITE_REVISE_ACTION_LIST`. Production flag state not verified in this sync. Rebuilds the draft from the decision set (`lib/revise-actions/apply.mjs`); nothing applied unless the user chose it (**B152**). **Apply proven against recorded Brackenhill sample `brackenhill-2026-09-02.json` (`924b859`), not a fresh Review.** Residual **B151** (no preview; deliberate v1 deviation) stays open. Also the answer to **B143**. Does not close **B9**. See ROADMAP **Recently shipped → Implement Changes (Pr17)**. | backend `d7a38d2` `924b859` `953ebb4` `4a65922` `958c3b5`; frontend `cdec139` (`v8.77.0-remove-suggest-revised-draft`) `77ab6cd` `4e856ac` `d9eda91` `ac32db9` `92a6e3b` |
| B149 | **SHIPPED 2026-09-03.** Silence no longer reads `supportSpans[].classification`. Widened spans never feed Stage 3; a span-level `conflicting` mark can no longer grant ACTION on a `partial` card. Test `tests/revise-actions-silence.test.mjs` failed on reference (`supportSpan_classification_conflicting` fired), then passed after that test was removed from `STRUCTURAL_TESTS`. Drawer mislabel of those spans remains **B89**. No verdict, classification, or Stage 2 pin change. | 2026-09-03 |
| B107 | **CLOSED 2026-09-08.** Stage 3 is conflict-wins. A contradicting source outranks a confirming one (`9f8fc41`). Zero any-confirmed-wins disagreements on the labelled set. Tag `review-conflict-wins`. | `9f8fc41` |
| B39 | **CLOSED 2026-09-08.** Conflict-with-confirmation card surfacing. Conflict-wins (`9f8fc41`) makes the badge `conflicting`, so a confirming source no longer paints the card green. Residual excerpt layout: **B158**. | `9f8fc41` |
|     | **SHIPPED 2026-09-08.** Accuracy runner cost meter. Stage 2 `costUsd` / `usage` copied through to the runner (`32f3b97`). Later pairs metered: reducer $5.41, rounding $5.36, lift $2.27. The 2026-09-05 pair has no meter and must not be re-run to recover it. | `32f3b97` |
|     | **SHIPPED 2026-09-08.** Accuracy runner frozen statement list. Stage 1 does not re-split; 261 statements, zero unmatched (`94bb017`). | `94bb017` |
|     | **SHIPPED 2026-09-08.** Intra-source reducer: most-serious of single-pick and locatable widened spans (`ba57db7`). Lift-only rounding on those spans (`0dee6fe`, correcting force-arm `66351a9`). Span-owned conflict excerpt (`3214646`). Tag `review-conflict-wins`. Residuals **B158** **B159** **B160** **B161**. | `ba57db7`, `0dee6fe`, `3214646` |
| B176 | **CLOSED 2026-09-12.** Inverted `thousand_separator` class (live F18-S3 signature: card says the draft is right and still offers to change `240'000` to `240,000`). Filter used to keep any concern with no locatable span; apostrophe grouping is the quote-parser trap (**B133**). Prompt layer **B176 step 1** (`b1fde9a`) did not lift the class. **B178** removed the source from the editorial payload. **B180** fails closed: statement-scoped DROP filters now run without a span for `thousand_separator`, `currency_format`, `smart_quotes`, `em_dash` (`percentage_notation` still reads `citedSpan`, so it is not in that set). F1 met: both b371929 compliant survivors gone (`oxford_comma` "No change needed"; `smart_quotes` on a straight-quote statement). Do not close **B157**, **B133**, or **B166**. **B181** corrects a defect in B180, not in B176. | **B178** `b371929` + **B180** |
| B180 | **CLOSED 2026-09-13.** STYLE FILTERS FAIL CLOSED. SHIPPED 2026-09-12. The filter used to keep a concern whenever it could not locate the flagged text. `smart_quotes` and `em_dash` had no referee; they now have a character-presence one on `statementText`. A concern whose own direction changes nothing is dropped (`suppressNoChangeDirections`). Statement-scoped predicates now run without a span for `thousand_separator`, `currency_format`, `smart_quotes`, `em_dash`. `percentage_notation` is not in that set: it already reads `citedSpan`. The cross-statement drop was deliberately NOT built because no fixture exercises it. Tests `tests/b180-style-fail-closed.test.mjs`. Confirmation on the B178 result file: F1 MET (both b371929 compliant survivors gone); F2 NOT MET (`firedOnViolation` 8/10 — `smart_quotes` violation dropped as a no-op after quote normalisation; `first_person_plural` still misses). Cross-ref **B176**, **B157**, **B133**, **B178**. **Annotation 2026-09-13:** shipped with a defect of its own making, a suppressed genuine `smart_quotes` violation, corrected by **B181** (G1 met). | **B180** `1d41172` + **B181** |
| B177 | **SHIPPED 2026-09-14.** Expand all / Collapse all placement. Same work as **B185**. Frontend `2d66da7` (`fix(review): scope the governing line to the disagreement and move the expand controls (B185)`). No tag. Do not close **B153**. | `2d66da7` |

---

### Moved from open tables (SHIPPED / MEASURED)

Rows moved whole from the former open tables. Launch is RECORD. Item / Source / Notes are unchanged.

| ID  | Item | Source / context | Priority | Launch | Notes |
|-----|------|------------------|----------|--------|-------|
| B103 | **Local development environment broken.** `AUTHORING_ORGANISATION` does not reach the resolver locally despite being present, unquoted, in a `.env.local` that is demonstrably read; moving it to line 1 changed nothing. The local `DATABASE_URL` credential is also stale, which is the same log noise on every run. Neither blocks production, where both work. Untested suspect: `vercel dev` taking environment from the linked project's Development environment, where neither variable is set. | chapter five 2026-08-25 | M | RECORD | Local only. Production works. See **P24**. |
| B155 | **SHIPPED 2026-09-04, two commits.** Cheap path: unnamed first-person is ACKNOWLEDGE, no model call (`25442f9`). System prompt interpolates the house only after the presence check (`editorial-compliance-reviewer.mjs`). Named-actor branch unchanged. Copy: `NO_PROPOSAL.first_person_unnamed`. Modify box parked. Do not remove `AUTHORING_ORGANISATION`. **B124** / **B95** remain for evidence author-identity. | operator 2026-09-04; cheap path 2026-09-04 | H | RECORD | Presence check not loosened. Invariant not rewritten. |
| B168 | **SHIPPED 2026-09-10.** Design A: a contradicted evidence finding never calls the rewrite model. Exactly one same-kind quantity pair → exact-quote token replace in code; otherwise ACKNOWLEDGE `conflict_unaddressed`. First-person-only prompt lead is silent cards or first-person-actor findings only. Pinned live F18: 4 replace (380/412, 142/167, two ARR 38/35), 3 acknowledge (two recommend-versus-complete, one two-pair MOIC/IRR). Residue: Design B house-name authority (**B170**); **B166** competing proposals; **B156** partial unlock. Do not close **B95**, **B96**, **B124**, **B156**, or **B166**. | Design A build 2026-09-10; live F18 Review | H | RECORD | Verdicts unchanged. Frozen corpus not re-run. |
| B169 | **SHIPPED 2026-09-11.** Unmarked contradiction: a source that states a different value for the same quantity, without correction wording, now produces a proposal. Multi-value replacement under R1–R6: every licensed figure in the sentence, or none. Live F18 pin: replace S3, S4 (38→35 and March→April 2025), S5, S8 (both returns, `gross` stays); acknowledge S0, S2, S7 (forecast, R5). Tests: `tests/revise-actions-quantity-match.test.mjs` (F18-S3/S4/S5/S8 now ask first per **B173**). Residue: **B172** hanging dependents; **B173** two documents disagree; **B174** say why on the card. Do not close **B159**, **B171**, **B172**, or **Pr16**. Frozen corpus not re-run. **Annotation 2026-09-13 (B183):** the R1 self-disagreement veto could not fire in the live product until B183. `sortedRecord` discarded `card` before `fillAction`. Unit tests supplied `card` by hand. **Annotation 2026-09-15:** live production F18 after the update-governs ruling produced the proposals this row pinned (412, 35/April, 167, 2.6x/21% gross). The B173 ask-first table is the before-ruling half. Evidence `365d6a4`. | quantity-matching build 2026-09-11 | M | RECORD | Ruled table pinned. Correction wording is naming-only. R1 inert live until B183. |
| B173 | **SHIPPED 2026-09-13.** When two sources disagree about a figure, the action list asks which document governs before proposing. Three new explain codes: `sources_disagree`, `sources_disagree_neither`, `sources_agreed_kept`. Rulings are session-scoped, keyed by frontend source ids, and travel in the `sourceRulings` request body; `runActionList` has no database. The Evidence badge stays Conflicting; Dealt with / Overridden is a separate status marker. **Pinned F18 table moved (Ben 2026-09-13, intended):** F18-S3, F18-S4, F18-S5, F18-S8 now ask first and propose only once the reviewer says the update governs. W2 stays a single-source propose. Fixture 24 verification: J1 met (net-IRR card unaddressed `sources_disagree`, no 12.4% proposal); J2 met (P1–P3 copy names both figures and both labels). Spend `LLM SPEND calls=34 in=131174 cached=80256 out=1917 costUsd=0.2458`. Findings `scripts/diagnostic/delivery-check/peer-sources-fixture-findings.md`. **Not built:** a permanent decision record. Do not close **B172** or **Pr16**. **Annotation 2026-09-13 (B183):** detection was correct but unreachable on the live `runActionList` path until B183 kept `card` on the internal entry. J1/J2 were scored by calling `applyConflictProposal` on inventory findings that still had `card`, so they described a path the runner did not take. Live `runActionList` on the same stored cards still proposed 12.4% on P1 and 1.2 billion on P3 until B183. The live look on 2026-09-13 is what found it. **Annotation 2026-09-14 (B184):** that first live look found three defects B184 ships: the card painted Stage 4's excerpt while proposing the other document's figure; a pairing-source ruling with no locator pairs dropped `governancePair`; editorial `overreach_unsupported_causal` still borrowed evidence language on a confirmed card. Fixture 18 has **not** been run live since B183 and is the outstanding regression check on the pinned table. **Annotation 2026-09-15:** fixture 18 WAS run live in production. Evidence `365d6a4` (`scripts/diagnostic/delivery-check/live-2026-09-15-f18-run.md` and `f18-2026-09-15/`). The cross-document question appeared, the ruling applied across the review, and all five contradicted figures came back corrected. **Pinned F18 table ruling:** it was not wrong, it was HALF. It recorded the before-the-ruling state (acknowledge, sources disagree, no proposal) and stopped there. After the ruling those same statements carry proposals, and the proposed sentences match the ones already in the record's other table. | operator 2026-09-11; peer fixture 2026-09-13; source-governance build 2026-09-13 | M | RECORD | J1 and J2 met on the locator-with-card path. Unreachable live until B183. First live look found B184. |
| B175 | **SHIPPED 2026-09-11.** Confirmed figure left unchanged. The confirming passage is selected from matched `supportSpans` on the same document as the conflicting excerpt, only when it supports a figure that remains in the sentence, and is painted beneath the conflicting excerpt. The second explanation sentence (`It confirms the residential unit count, which is left unchanged.`) is emitted only when that passage is present. Tests: `tests/revise-actions-confirming-passage.test.mjs`. Do not close **B174** (residue **B173**). **Annotation 2026-09-13 (B183):** the confirming-passage selector could not fire in the live product until B183. Same strip: `sortedRecord` discarded `card` before `fillAction`. Unit tests supplied `card` by hand. | confirming-passage build 2026-09-11 | M | RECORD | Passage on the card, then the clause. Inert live until B183. |
| B179 | **OBSERVABILITY SPEND ACCUMULATOR. SHIPPED 2026-09-12 `9f59c2f`.** Running total at the single provider call site (`recordLlmSpend` in `lib/observability.js`). Unpriced models reported as an undercount rather than silently free. Both attempts of a JSON schema retry counted. **Annotation 2026-09-13 (B181):** the accumulator is now wired into `scripts/diagnostic/run-style-guide.mjs` as well as `run-batch.mjs`. Residual, do not lose: `tests/r1_2_mini_eval/run_eval.mjs` and `run_r1_2_2.mjs` still call the deprecated `callOpenAI` and bypass the accumulator. Calls that throw are not counted. Tests `tests/llm-spend-accumulator.test.mjs`. | `9f59c2f`; B181 harness wire | M | RECORD | Residual: deprecated eval callers; thrown calls uncounted. |
| B181 | **NO-OP DETECTION EXEMPTS CHARACTER RULES. SHIPPED 2026-09-13.** B180's no-change predicate (b) normalised quote and apostrophe characters before comparing, which is the same class of trap as **B133** and **B176**: a normaliser removes the character the rule polices. Effect: a genuine `smart_quotes` violation was suppressed. Fix: `smart_quotes`, `em_dash`, and `thousand_separator` are exempt from replace-pair no-op detection. Predicate (a) ("no change needed" at the start) still applies to every rule. Deeper fix, not done: compare raw text before normalisation, which needs offset-preserving extraction. Tests T13–T17 in `tests/b180-style-fail-closed.test.mjs`. Confirmation on the B178 result file: G1 MET (`smart_quotes` fires on violation); G2 MET (`silentOnCompliant` 10/10); G3 NOT MET on raw 8/10 (`number_spelling` one-check miss is G5 noise; `first_person_plural` remains the accepted miss); G4 MET (spend `calls=30`). Cross-ref **B180**, **B133**, **B157**. | B180 F2 residual; operator 2026-09-13 | H | RECORD | G1 met. B180 closed. |
| B183 | **THE PROPOSAL STEP COULD NOT SEE THE SOURCES. SHIPPED 2026-09-13.** Cause: `sortedRecord` in `lib/revise-actions/sort.mjs` destructured `card` off the finding and discarded it (`void card`), introduced `d7a38d2` 2026-09-01 (Pr17). Every entry reaching `fillAction` had no `card`, so `supportSpansOf` was always empty. Three shipped features were inert on the live `runActionList` path: R1 self-disagreement (**B169**, believed shipped 2026-09-11), confirming passage (**B175**, believed shipped 2026-09-11), and B173 cross-source detection (believed shipped 2026-09-13). `publicEntry` already whitelists the response, so keeping `card` on the internal entry does not leak it. Fix: `sortedRecord` keeps `card`. Standing guard is T1 in `tests/b183-entry-carries-card.test.mjs` (through `buildSortedEntries`, not a hand-built locator call). Findings `scripts/diagnostic/delivery-check/card-stripping-findings.md`. Pinned F18 table not quietly updated; Ben to rule. **Annotation 2026-09-14 (B184):** fixture 18 has **not** been run live since B183. It is the outstanding regression check on that pinned table. **Annotation 2026-09-15:** that check is done. Live production run `365d6a4`. Ben's ruling: the pinned table was HALF (before-the-ruling); see **B173**. | live look 2026-09-13; card-stripping build 2026-09-13 | H | RECORD | T1 is the standing guard. |
| B184 | **THE DISAGREEMENT CARD TELLS THE TRUTH AND KEEPS THE ANSWER. SHIPPED 2026-09-14.** Found by the live look on fixture 24, 2026-09-13. Three defects: (1) a ruling that takes the proposal from the unpainted document still showed Stage 4's `primaryExcerpt`; the card now carries `disagreementPassages` for both sides, pairing source first, `governs` only after a ruling names that source. (2) a pairing-source ruling with no locator pairs returned bare unaddressed, dropping `governancePair` and the ruling line; the pair is now attached to every outcome after detection, including empty-pairs and qualifier-clash. (3) `overreach_unsupported_causal` still wrote "without supporting evidence" on a confirmed card; the rule is draft-only since **B178**, and a prompt line did not hold. **Judgement call (3b):** for `overreach_unsupported_causal` and `underreach_hedging` only, a concern whose note or direction contains evidence/source/supported/unsupported/substantiat is dropped. That loses a concern that may be sound but badly worded. Showing an evidence claim from a reviewer that cannot see evidence, on a card whose evidence verdict may say confirmed, is worse. Dropping is the safer of the two and is trivially reversible. Tests `tests/b184-disagreement-card.test.mjs`. Fixture 18 has **not** been run live since B183. **Annotation 2026-09-15:** fixture 18 WAS run live in production (`365d6a4`). Cross-document question, cross-review ruling, and five corrected figures all held. Pinned-table ruling on **B173**. | live look 2026-09-13; disagreement-card build 2026-09-14 | H | RECORD | Prompt plus backstop. Fixture 18 outstanding. |
| B185 | **SHIPPED 2026-09-14.** Expand all / Collapse all relocated to the action-list heading. Same work as **B177** (Closed). Frontend `2d66da7`. No tag. Do not close **B153**. | expand-control placement 2026-09-14 | H | RECORD | Same piece of work as B177. |
| B186 | **FIRST APPEND-ONLY REVIEWER-DECISION STORE. SHIPPED 2026-09-14.** Table `reviewer_decisions`: owner, review, time, kind, optional draft hash, the statement, and a self-contained `payload` (both document labels, both figures, both passages, chosen document or null for neither). First kinds: `source_governance` (a named governing document) and `source_override` (Leave as written / neither). Changing a ruling appends a new row; there is no update or delete path. `POST` / `GET` `/api/reviewer-decisions`; GET is that owner's rows for one review, newest first, cap 200. Retrieval beyond that GET waits for accounts. The write is a record, not a gate: missing database returns 503 and Review continues. **Verified live on production 2026-09-14** by reading a stored row back, not by the absence of an error. Production was migrated that day; the table exists. First rows: a reviewer changed their ruling twice and all three rows were preserved. Early rows have `draft_hash` null because the client did not send it; **B187** now sends the hash. Annotates **B9**: this table is the beginning; per-finding accept and reject are still not recorded. Tests `tests/b186-reviewer-decisions.test.mjs`. | B173 outstanding decision record; live production read-back 2026-09-14 | H | RECORD | Production migrated and verified by reading a row. |
| B187 | **DRAFT HASH ON EACH DECISION. SHIPPED 2026-09-15.** The client now sends `draftHash` on governing-choice and leave-as-written POSTs. Same algorithm as backend `getDraftHashPrefix`: trim, collapse whitespace, lowercase, sha256, first 8 hex chars. Browser digest; if unavailable, the hash is omitted rather than a wrong value. Fire-and-forget unchanged. Tests `src/modules/drafting/__tests__/draftHash.test.js`. | B186 first rows had null `draft_hash` | H | RECORD | Ties the ruling to the draft it was made about. |
| B188 | **ONE WORD LIMIT. SHIPPED 2026-09-15.** One word limit, pre-filled from the house figure for output type and visibility, set by the writer, stated to the model once as a ceiling, reported and never enforced. Seven places became one. Backend `15316f4`; frontend `be57661` and `0e91ccf`. Residue: **B188b**; leftover softer line in `api/rewrite.js` is **B198**; shape-rule tension is **B199**. | word-limit unification 2026-09-15 | H | RECORD | One ceiling. Not enforced. |
| B188b | **REWRITE PATH HELD A SECOND SHORTENING. SHIPPED 2026-09-15.** The rewrite path held its own second shortening instruction; removed, so generate and rewrite behave the same way. Backend `05aead7`; frontend `0678d06`. | B188 2026-09-15 | H | RECORD | Generate and rewrite share one ceiling. |
| B189 | **ENVIRONMENT VISIBLE. SHIPPED 2026-09-15.** `/api/health` reports the running environment (`pipelineRoute`, `authoringOrganisation`, `reviseActionList`, `ok`) and the header shows it as a pill, amber when off-nominal. New `api/_lib/env-flags.js` is the single reader; the two revise routes import it instead of holding copies. Backend `579dbfc`; frontend `150cdac`; tag `b189-environment-visible` (frontend only — backend commit is untagged). **Annotation 2026-09-15 (B198 ship):** backend tag `b189-environment-visible` now points at `579dbfc`. | environment visibility 2026-09-15 | H | RECORD | Pill is the operator check that the run is on the intended pipeline. |
| B190 | **REVISE FLAG DRIFT. SHIPPED 2026-09-15.** The pill warns when the backend `REVISE_ACTION_LIST` and the frontend `VITE_REVISE_ACTION_LIST` disagree. Frontend `287042c`; tag `b190-revise-flag-drift`. | B189 2026-09-15 | H | RECORD | Two flags, one warning. |
| B191 | **WORD LIMIT LABEL. SHIPPED 2026-09-15.** The draft meta line: field labelled "Word limit" on the left, live counter on the right, amber applies to the count and not the control. Frontend `08aea65`; tag `b191-word-limit-label`. | B188 2026-09-15 | H | RECORD | Amber is the count, not the field. |
| B192 | **RESULTS OVERFLOW. SHIPPED 2026-09-15.** A source filename can no longer widen the Results column past the viewport (`flex min-width: auto` on the Assess grid). Closed drawers also marked inert. Frontend `b020ffd`; tag `b192-results-overflow`. Cosmetic residue: **F23**. | layout 2026-09-15 | H | RECORD | Filename wrap, not column stretch. |
| B193 | **SUMMARY VERDICT MIX. SHIPPED 2026-09-15.** The review summary no longer counts conflicts and partials as unsupported. New `src/utils/verdictMix.js` is the single four-way count, used by BOTH payload builders; the prompt now states that a conflict has evidence. Frontend `35cc974`; backend `27e9a47`; tag `b193-summary-verdict-mix`. Does not close **B194**: the written assessment, the filter row, the Quality Review Summary badge, and the QRS bullets still have four definitions. | summary mix 2026-09-15 | H | RECORD | Four-way count. Conflict has evidence. |
| B194 | **ONE REVIEW VOCABULARY. SHIPPED 2026-09-17.** Backend `ef66891` tag `b194-one-review-vocabulary`; frontend `21da610` tag `b194-one-review-vocabulary`. The backend computes one summary (`lib/qc/review-summary.mjs`) and stamps each card; the badge, bullets, filter counts, and assessment closing label all read it. Failed editorial or compliance schema checks show as Not checked, not as findings. Needs attention is the one count that still responds to reviewer rulings. The written assessment remains a snapshot taken at review time. Saved reviews from before this change show a legacy note instead of a summary. Does not close **B195**. **Annotation 2026-09-17:** `21da610` did not build. Duplicate `ASSESS_DEFAULT_MODEL` in `useAssessState.jsx`. Fixed `d6a8c8b`. `verify:ship` now lints and production-builds before tests (`a00ab63`). | live F18 2026-09-15; B193; Ben ruling 2026-09-16 | H | RECORD | One vocabulary. Two deliberate differences: Needs attention after rulings; assessment is a snapshot. |
| B195 | **NOISE FLOOR FOR THE CATEGORY COUNTS. MEASURED 2026-09-17.** Harness `836cc4b`; report and 34 run JSONs `7de5e98`. Local cache-off, 10 runs each. A before-after category change counts as real only if larger than that fixture's range. No category range was 2 or more, so 3-before / 3-after is not required. **18:** All 10-10, Confirmed 1-2, Partial 1-2, Conflicting 7-7, No support 0-0, Editorial 5-5, Compliance 0-0, Not checked 0-0, compliance-null 0-0, Needs attention 8-9, readiness Needs significant work. **24:** All 7-7, Confirmed 2-2, Partial 1-2, Conflicting 3-4, No support 0-0, Editorial 3-3, Compliance 0-0, Not checked 0-0, compliance-null 0-0, Needs attention 5-6, readiness Needs significant work. **10:** All 7-7, Confirmed 7-7, Partial 0-0, Conflicting 0-0, No support 0-0, Editorial 0-1, Compliance 0-0, Not checked 0-0, compliance-null 0-0, Needs attention 0-1, readiness Ready / Minor points to address. Statement split never moved. Cache-on control: 0 hits on all 3 local repeats (P2 FAIL). Report: `scripts/diagnostic/noise-floor/REPORT.md`. **Annotation 2026-09-17 (B202):** the compliance-null-with-compliance-on counter could not fire on v4 cards before B202. Stage 7 turned a parse-failure `null` into `"clean"` before the card left the pipeline, so the zero was structural. | live F18 2026-09-15 | H | RECORD | Floor known. Delta of 1 on Confirmed / Partial / Needs attention on fixture 18 is noise. |
| B198 | **REWRITE PROMPT CEILING. SHIPPED 2026-09-15 `bf23217`.** The instruction rewrite prompt's only word-limit line was a hedge (`Keep output under ~N words where possible`). Replaced with the same firm ceiling as `api/generate.js` L764–765: "Word limit: N words maximum for the commentary, excluding any Methodology Note. Write to fit within it." plus the do-not-pad / do-not-truncate / best-complete-version lines. Length-only rewrite already stated a firm maximum and was left alone. Tag `b198-rewrite-ceiling`. Test: `tests/rewrite-word-limit.test.mjs`. | B188b leftover | S | RECORD | One ceiling, no hedge. |
| B200 | **HEADER BUILD IDENTITY. SHIPPED 2026-09-15 `dd81461`.** The header shows the short build commit instead of a hand-typed version. Docs `e5f1f3e`: three wrong tag citations corrected, duplicate B169 merged, B185 recorded as the same work as B177. Frontend tag `b200-header-build-identity`. Tests `tests/version.test.mjs`. | header identity 2026-09-15 | H | RECORD | Slot is the commit. |
| B201 | **BUILD LABEL. SHIPPED 2026-09-16 `7fd0d1e`.** Header slot is `(Build: <commit>)` (or `(Build: dev)`). Dropped dead `VITE_APP_VERSION` / `VITE_APP_COMMIT` from the `build` script; nothing reads them. Frontend tag `b201-build-label`. | B200 2026-09-16 | H | RECORD | Label on the commit. |
| B202 | **A CHECK THAT DID NOT RUN READS AS NOT CHECKED. SHIPPED 2026-09-17 `74d0134` tag `b202-not-checked-at-source`.** Compliance parse failure and a thrown editorial/compliance review write `not_reviewed`, not `null` or `"clean"`. Stage 7 now receives `reviewOptions`; a missing verdict for a review type that was ON becomes `not_reviewed`. `classifySignal` maps missing/null/empty ON to `notChecked`. Frontend fixtures copied `2c32c3e`. Log extractor counts stage6 crashes as `reviewCrashes` (`72704a8`). **Annotation 2026-09-17 (B202b `b53fc76`):** an unrecognised non-empty verdict is now `not_reviewed` / `notChecked` when that review type is ON. Frontend fixtures `a25d693`. **Regression (local, fixtures 18/24/10, `819e498`):** G1 PASS on all three (Not checked 0, no schema/parse/crash logs). G3 PASS: every other count sat inside the B195 range. | B195 2026-09-17; B194 | H | RECORD | Failed check is Not checked, never a silent green. |
| B203 | **ASSESSMENT DESCRIBES A CONFLICT AS A DISAGREEMENT. SHIPPED 2026-09-17 `8d541a4` tag `b203-conflict-wording`.** The synthesize-review prompt tells the model to describe a conflicting statement only as a disagreement between the sources and never to use any form of "support" about it. **Annotation 2026-09-17 (B203b `63cb4c9`):** the prompt now says to state the verdict in the first sentence, in the badge's exact words. Production wording check (three calls from cache-off-18-run01): W1 PASS/PASS/PASS; W2 PASS/PASS/PASS; W3 PASS/PASS/PASS. Each opened `Needs significant work.` | B193; live F18 wording | H | RECORD | Conflict is a disagreement. The paragraph opens on the verdict. |
| B204 | **A SAVED ACCEPTED FIX TAKES ITS CARD OUT OF NEEDS ATTENTION. SHIPPED 2026-09-17.** Frontend `5c20a02` tag `b204-accepted-fix-dealt-with`. Backend store write `25e1ea6` (no tag). Saved accept or modify marks the card `dealt_with` and drops it from Needs attention. Reject and unset do not. Other counts, badge, bullets, and assessment stay as the Review found them. Persist by `analysisRunId`. Tests `tests/b204-accepted-fix-leaves-needs-attention.test.mjs`. Live P1 to P5 PASS; P6 FAIL (see **B208**). | Ben ruling over keep-until-next-Review; B196 | H | RECORD | Saved accept is a decision. The to-do count shrinks. |
| B205 | **HEALTH PILL REPORTS WHETHER THE EDITORIAL CHECK IS ON. SHIPPED 2026-09-17.** Backend `faf1054` tag `b205-editorial-switch-visible`. Frontend `1d6dd01` tag `b205-editorial-switch-visible`. `/api/health` `editorialReview` is true only when `BRIGHTLINE_EDITORIAL_REVIEW` is `"1"`; `ok` requires it. Missing field is unknown, not off. Pill tooltip `Editorial review: on` or `off`; amber when off. Live S1: grey pill, tooltip on. | B189; editorial switch | H | RECORD | Off is visible in the header, not only as Not checked on cards. |
| B206 | **EDITORIAL AND COMPLIANCE CHECK EVERY SENTENCE. SHIPPED 2026-09-17 `7d20516` tag `b206-no-statement-cap`.** Removed `statements.slice(0, 20)` from `lib/qc/editorial-compliance-reviewer.mjs` (A7.14 `5b0a5bd`). Evidence-skipped batch path now checks every statement passed in, all at once. Tests `tests/b206-no-statement-cap.test.mjs`. | A7.14 cap; evidence-skipped path | H | RECORD | Long drafts with evidence off are checked in full. |
| B207 | **VERIFY:SHIP REFUSES BACKEND FILES THAT DO NOT PARSE. SHIPPED 2026-09-17 `ad0d5dc`.** `scripts/verify-ship.mjs` runs `node --check` on every `.js`, `.mjs`, `.cjs` under `api/` and `lib/` after git checks and before tests. Failure prints checker output and `SHIP NOT VERIFIED  syntax check failed <file>`. No tag. Scratch-copy gate fired on `lib/qc/review-summary.mjs`. Syntax check added about 2.3 s. | instrument | H | RECORD | Unparseable api/lib cannot ship as SHIP VERIFIED. |
| B209 | **REWRITE INVENTED A PLACEHOLDER AND THE WRONG FIRM. SHIPPED 2026-09-18.** Backend `d988e85` tag `b209-house-identity`; `504a909` tag `b209-placeholder-guard`. House name comes from the configured organisation; missing date/investment no longer interpolates bracket placeholders. A bracketed placeholder in model output is refused (422), draft unchanged. Live W1: opening `Halden Group has completed a controlling investment in Nordic SaaS Holdings...`. No `Partners Group`, no `[transaction date]`. | B196 S5 2026-09-18 | H | RECORD | House from config. Placeholder never reaches the draft. |
| B212 | **EXPORT LABELS THE CURRENT REVIEW V 1 WHILE HISTORY SHOWS V7. SHIPPED 2026-09-18.** Backend `b6cf81c`; frontend `a44246c`. Version taken from the same History list (`versionNumberForExport`). Live W2 on the restored 18 Sep session (History v1): checkbox `Reviewed draft (v 1)`, filename `Press_release_PressRelease_v1_20260918.pdf`, file `Version: v1`. | B196 S7 2026-09-18 | H | RECORD | Export names the version History shows. |
| B213 | **EXPORT QRS IS NOT THE ON-SCREEN QRS. SHIPPED 2026-09-18.** Same commits as B212. Export carries readiness and the same bullets the screen shows, from the backend review summary. Live W2 screen and file: `Ready` / `All claims are backed by sources and no editorial or compliance concerns were found.` / `The draft is 66 words over the 80 word limit.` | B196 S7 2026-09-18 | M | RECORD | One summary on screen and in the file. |
| B214 | **PLACEHOLDER COPY ON CARDS. SHIPPED 2026-09-18.** Backend `b6cf81c` (`lib/revise-actions/sort.mjs`; spec named this in the export commit). New string: `The review found a concern in this sentence. No wording change is proposed, so you can judge it yourself.` Stored cards keep the old string until the next Review. Live W3: the restored 18 Sep F10 session had Editorial 0, so the line was not on those cards. **Annotation 2026-09-18 (B218/B219 F18):** second production Review of fixture 18 also did not show that line. The two editorial cards had proposed directions, so the no-proposal copy had nowhere to appear. **Annotation 2026-09-18 (B214 close):** tests without a Review. Backend `tests/revise-actions-license.test.mjs` already asserted `NO_PROPOSAL.visible_signal`. Frontend `tests/b214-no-proposal-card-copy.test.mjs` (`560b082`) asserts `acknowledgeReasonFor` surfaces that string and that `StatementReviewCard.jsx` renders it. | B196 S2 2026-09-18 | M | RECORD | Copy tells the reader why the card is there. Tested. |
| B215 | **ASSESSED-AS LINE USES AN EM DASH. SHIPPED 2026-09-18.** Frontend `195f40b`. Live: `Assessed as: Press release, Public` then `Assessed as: Investor letter, Complete`. Comma, no em dash. | B196 S1 2026-09-18 | L | RECORD | Results header matches the rest of the vocabulary. |
| B216 | **NEW OUTPUT DID NOT START A NEW OUTPUT. SHIPPED 2026-09-18.** Frontend `faf04f6`. Confirm `Start a new output? This clears the current draft, sources, and review.` then reset, new review id, reload. Live W4: draft empty, sources gone, History empty, Review and Export disabled. Residue: word limit stayed at 150 after the reset. | B196 S9 2026-09-18 | M | RECORD | The control starts a new output. |
| B208 | **DEALT WITH SURVIVED A NEW REVIEW. COPY SHIPPED 2026-09-18 as B218.** Overlay was `sources_agreed_kept`, not a B204 leak. Frontend `8f1d7ac`. Live F18 after naming 18b and accepting figure fixes, second Review: Conflicting badge, chip `Matches the document you chose as governing.`, not in Needs attention. | B196 P6; B218 | H | RECORD | Overlay right. Phrase shipped. |
| B210 | **EDITORIAL FLAGS A THIRD-PERSON SENTENCE AS FIRST-PERSON. SHIPPED 2026-09-18 as B219.** `voice_consistency` now has a pronoun backstop on third-person output types and no longer carries first-person removal on first-person house-voice types. | B196 S2; B219 | H | RECORD | Same rule as B211 and B217. |
| B211 | **EDITORIAL CALLS `was attracted to` FIRST-PERSON PLURAL. SHIPPED 2026-09-18 as B219.** Worked examples in `FIRST_PERSON_ACTOR_INSTRUCTION` now label Fault and Corrected sides. Pronoun backstop on reporting commentary. | B196 S7; B219 | H | RECORD | Wrong person label on the verb. |
| B217 | **EDITORIAL VOICE RULE FLIPS BETWEEN RUNS. SHIPPED 2026-09-18 as B219.** Investor letter, press release, and LinkedIn no longer attach first-person removal to `voice_consistency`. | B196 S2/S6; B219 | H | RECORD | One rule, two directives. |
| B218 | **DEALT WITH SHOULD NAME THE GOVERNING-DOCUMENT RULING. SHIPPED 2026-09-18.** Frontend `8f1d7ac`. `sources_agreed_kept` is `matches_governing` in the data, chip `Matches the document you chose as governing.`; applied decisions still say `Dealt with`. Live F18 second Review: Conflicting + that chip on 412, EUR 35 million, and 2.6x cards; Needs attention 5. | B208 diagnosis; production F18 2026-09-18 | H | RECORD | The overlay is right. The phrase names the ruling. |
| B219 | **VOICE_CONSISTENCY STOPS ASKING FOR FIRST PERSON AND ITS REMOVAL AT ONCE. SHIPPED 2026-09-18.** Backend `4649916` tag `b219-voice-consistency`. House voice mapping in `first-person-actor.mjs`: third person is reporting commentary only. `FIRST_PERSON_ACTOR_INSTRUCTION` attaches to `voice_consistency` only there. Pronoun backstop on that type only, gated on finding text that claims first-person misuse. Live F18 investor letter: zero editorial notes mentioned first person; opening and "We have invested" were editorially clean. Covers **B210**, **B211**, **B217**. Does not close **B182**. **Annotation 2026-09-18 (B220):** press release moved to the third-person list. Removal instruction now attaches on press releases too. | B196 S2/S6/S7; diagnosis A3 | H | RECORD | Instruction and backstop scoped. B182 stays. |
| B197 | **B182 RECHECK. ANSWERED 2026-09-18.** Live F18 first-person hits were `voice_consistency`, not `first_person_plural`. Diagnosis A3 in `b208-b217-diagnosis.md`. Those misfires are **B219**. B182 remains its own reporting-commentary style-harness miss. | live F18 2026-09-15; B182; B219 | M | RECORD | Not the same rule. B182 stays open. |
| B220 | **ONE SOURCE OF TRUTH FOR HOUSE VOICE. SHIPPED 2026-09-18.** Backend `04c145f` tag `b220-house-voice`. Frontend `560b082`. Table in `lib/prompt-library/house-voice.mjs`; frontend mirror `src/constants/houseVoice.js`. Press release is third person in the body. `FIRST_PERSON_ACTOR_INSTRUCTION` now attaches to `voice_consistency` on press releases. `first_person_plural` `applies_to` not widened (**B223**). Quote carve-out is instruction only (**B224**). Tests `tests/b220-house-voice-single-source.test.mjs` in both repos. | B219 C4 leftover | H | AFTER | Review and writing prompts share one table. |
| B221 | **DOCS COMMITS STOP REDEPLOYING PRODUCTION. SHIPPED 2026-09-18.** Backend `vercel.json` `ignoreCommand` skips the build when `git diff HEAD^ HEAD` is empty outside `docs/` and `scripts/diagnostic`. Frontend `ignoreCommand` skips when the diff is empty outside `docs/`. Live check is the docs commit that records this bundle. | B220 instrument | M | AFTER | Docs-only push should not rebuild. |
| B222 | **STANDING AUTHORITY SHIPPED 2026-09-18.** `.cursorrules` on both repos. The rule text lives under Standing rules **B222**. | B220 instrument | -- | RECORD | Files shipped. Rule is standing. |
| B225 | **REVIEWER ASSESSMENT IS GIVEN THE CARD NOTES. SHIPPED 2026-09-19.** Frontend `f0bc71f`. `src/utils/reviewerSynthesisPayload.js` reads `evidenceSummary` / `reasoningParagraph` and `editorialConcerns[].note` / `complianceConcerns[].note`, one payload item per concern. Dead fields `editorialCommentary`, `assessmentExplanation`, `row.assessment` are no longer read. Readiness and counts unchanged (byte-identical on the Meridian fixture). Tests `tests/b225-assessment-finding-text.test.mjs`. Fixture `tests/fixtures/b225-meridian-review.json`. Prompt not touched. | P6 empty findings; 18 Sep Meridian | H | AFTER | Finding text only. |
| B226 | **CONSTRUCTIVE FEEDBACK IS A PIECE, NOT A FORM. SHIPPED 2026-09-19.** Backend `d41f3ea` tag `b226-constructive-feedback`; coverage follow-ups `5eba0bf`, `e2b28b3`. One call. Margin notes on the draft. Code writes readiness. Craft is a permission. Temperature 0. Tests `tests/b226-constructive-feedback-piece.test.mjs`. Fixtures `tests/fixtures/b226/`. Report `scripts/diagnostic/delivery-check/b225-b226-report.md`. | B225 proposal P1-P7 | H | AFTER | Author-facing piece. |
| B232 | **UNRECOGNISED EVIDENCE VERDICTS ARE UNVERIFIABLE. SHIPPED 2026-09-19.** Backend `9254b37` tag `b232-unverifiable-badge`. Frontend `0ca8bf7` tag `b232-unverifiable-badge`. Allowlist compared after trim and case fold. Unknown is `Unverifiable` on the badge and in the export, never Confirmed. Dead `evidenceVerdictLineFromCard` / `evidenceVerdictLineFromSupportState` deleted. Tests `tests/b232-export-verdict-label.test.mjs`, `tests/b232-display-verdict-label.test.mjs`. | dead-read census D1 2026-09-19 | H | RECORD | Screen and file agree. |
| B227 | **ASSESSMENT REFUSES A BLANK FINDING. SHIPPED 2026-09-19.** Backend `c48a464`. Frontend `1113dde`. Classified concern or evidence item with no text refuses the call. UI already shows `Reviewer assessment unavailable.` Tests `tests/b227-assessment-blank-finding.test.mjs` in both repos. | dead-read census 2026-09-19 | H | RECORD | No empty concern reaches the model. |
| B228 | **EXPORT MISSING FINDING IS A FAULT. SHIPPED 2026-09-19.** Backend `48d7518`. Empty `reasoningParagraph` / `reasoningHeadline` prints `Not recorded.` under Evidence finding. Distinct from omitted skip and from `Not checked.` Test `tests/b228-export-finding-hole.test.mjs`. | dead-read census 2026-09-19 | H | RECORD | Hole looks like a fault. |
| B229 | **FEEDBACK OMITS EMPTY NOTES. SHIPPED 2026-09-19.** Backend `2b71475`. `collectMarginNotes` no longer invents generic evidence / editorial / compliance sentences. Absence of a note. Test `tests/b229-feedback-omit-empty-notes.test.mjs`. | dead-read census 2026-09-19 | H | RECORD | No invented margin note. |
| B230 | **IMPLEMENT CHANGES SKIPS A BLANK FINDING. SHIPPED 2026-09-19.** Backend `243c998`. Empty `thing2` and `suggestedDirection` become ACKNOWLEDGE with `NO_PROPOSAL.visible_signal` instead of calling the model. Conflict path unchanged. Test `tests/b230-revise-blank-finding-acknowledge.test.mjs`. | dead-read census 2026-09-19 | M | RECORD | No `(none)` prompt to the reviser. |
| B240 | **THE COVERING NOTE NO LONGER APPENDS LEFTOVER CARD TEXT. SHIPPED 2026-09-19.** Backend `ddbebd4` tag `b240-feedback-covering-note`. API stops calling `assembleConstructiveFeedbackPiece`. One coverage retry names missed findings; a second miss ships the piece and records `constructive-feedback-coverage-miss` on the Langfuse trace plus `console.warn`. Grouping check at four or more findings. Craft referee drops a paragraph that names no finding and quotes no draft. Prompt: say it once, at the level it is true. Temperature stays 0. Tests extended in `tests/b226-constructive-feedback-piece.test.mjs`. Check `scripts/diagnostic/delivery-check/b240-check.mjs`. Report `scripts/diagnostic/delivery-check/b240-report.md`. Spec asked **B232**; that id was already the unverifiable badge. **B234**-**B239** were already filed by the claims inventory. | B226 remainder and grouping | H | AFTER | Covering note. Cards stay the record. |
| B241 | **CLEAN-DRAFT COPY DROPS THE EM DASH. SHIPPED 2026-09-19.** Backend `0995fe0`. `CLEAN_DRAFT_FEEDBACK_TEXT` is `No changes are needed. The draft is ready for signoff.` Test `tests/b241-clean-draft-dash.test.mjs`. | B240 D5; C5 | L | RECORD | User-facing copy, not a prompt. |
| B242 | **FEEDBACK LOADING COPY USES ASCII PERIODS. SHIPPED 2026-09-19.** Frontend `c846d84`. `Generating constructive feedback...` Test `tests/b242-feedback-loading-copy.test.mjs`. | B240 trivia | L | RECORD | Loading line only. |
| B244 | **A RECORDED ANALYSE-STATEMENTS PAYLOAD CAN DRIVE ASSESS WITHOUT CALLING THE API. SHIPPED 2026-09-19.** Frontend `f22e96c`. Dev-only Vite serve plugin and DEV intercept at `apiAnalyseStatements`. `meta.replayed` blocks `review_state` persistence and synthesize-review. Tests `tests/b244-replay-path.test.mjs`. Shared fixture remains `tests/fixtures/b226/1-meridian-reporting-live-2026-09-18.json` on the backend. | claims inventory Part 3; replay path | H | RECORD | Instrument. Not reachable from a production build. |
| B245 | **CLAIMS FROM THE RECORDED MERIDIAN REVIEW ARE ASSERTED END TO END. SHIPPED 2026-09-19.** Frontend `7302ece` tag `b245-claims-e2e`. Backend `64cc94d` tag `b245-claims-e2e`. One shared fixture. Results-screen strings, canonical export golden, assessment payload, margin notes, revise-action list. Three fixture corruptions. Report `scripts/diagnostic/delivery-check/b244-b245-report.md`. Inventory Part 3 is now 20 of 87. | claims inventory; B244 | H | RECORD | Instrument. No Review. No model call. |
| B246 | **THE TWO READINESS CALCULATIONS CANNOT DRIFT. SHIPPED 2026-09-19.** Frontend `03f539d`. Backend `794736c`. `tests/b246-readiness-drift.test.mjs` in each repo. Each imports the other `summariseReview` over `review-summary-cases.json` (both copies) and the recorded Meridian payload. Fails on different readiness or counts. Does not merge the modules. Shared-module recommendation stays on **B239**. | B239 remainder | H | RECORD | Drift test. Not a shared module. |
| B234 | **THE SUMMARY DESCRIBES THE REVIEW THAT RAN. SHIPPED 2026-09-19.** Frontend `78d725c`. Backend `ecbb7f2`. QRS is recomputed from cards and `reviewOptions` at the point of use. A later editorial-off flip no longer keeps `1 claim has editorial notes.` in the file or on screen. B245 D5.ii. Report `scripts/diagnostic/delivery-check/b233-b235-report.md`. **Annotation 2026-09-19:** R3 and R4 live recordings agree. R3 QRS `All claims are backed by sources.` R4 QRS `1 claim has editorial notes.` with no evidence verdict in the file. | claims inventory; B245 D5.ii; claim-disagreements 2026-09-19 | H | RECORD | Stamp kept for legacy snapshots with no cards. |
| B235 | **AN UNREADABLE EVIDENCE VERDICT CANNOT MAKE A DRAFT LOOK READY. SHIPPED 2026-09-19.** Frontend `7ff5716`. Backend `d877296`. `classifyEvidence` uses the badge allowlist. Unknown and `Not reviewed` while evidence is on count as `notChecked`. Case `unknown-display-verdict` readiness Ready became Not fully checked. B245 D5.i. | claims inventory; B232 remainder | H | RECORD | Dropped cards no longer look cleaner. |
| B233 | **EXPORT NAMES EVIDENCE CONCERN AS EVIDENCE. SHIPPED 2026-09-19.** Backend `4d10005`. Golden re-bless `4b32e34`. Mapper renamed `mapSupportStateToEvidenceConcernLevel`. File prints `(moderate evidence concern)` / `(high evidence concern)`. Card still stores `concernLevel`. Five Meridian golden verdict lines changed. Do not close **B231**. | B227-B232 D6; B245 golden | M | RECORD | Rename at print. Do not stuff editorial into it. |
| B269 | **CURSOR OPERATING DOCS MADE TRUE AND AUTO-READ. SHIPPED 2026-09-20 `b2e8c9c`.** Audit `docs/DOC_TRUTH_AUDIT.md`. Rewrote `ai/AI_OPERATING_MANUAL.md`, `ai/SPEC_TEMPLATE.md`, `docs/ARCHITECTURE.md`. Frontend `.cursorrules` `4e5f7c2`. Both repos now say: read the manual before any spec; **P31** binds at every action. `.cursor/rules/ce-core-rule.mdc` same trigger. Standing rule **P33**: commit the artefact. No product code. | this spec 2026-09-20 | -- | RECORD | Docs and rules. |
| B268 | **STAGE 6 POOL IS 4. SHIPPED 2026-09-20.** `badb55e` tag `b268-stage6-pool-4`. Editorial 186/186, compliance 186/186 on the Shopify memo. Report `scripts/diagnostic/delivery-check/b268-stage6-pool.md`. | this spec 2026-09-20 | H | RECORD | Missing checks closed. |
| B273 | **EDITORIAL SCOPE HONESTY. MEASURED AND NOT SHIPPED 2026-09-20.** Mean codesDiffer 24/40 vs floor 13/40. Line left alone. | this spec 2026-09-20 | H | RECORD | See Findings log. |
| B275 | **DOCUMENT-LEVEL EDITORIAL SLICE. MEASURED AND NOT SHIPPED 2026-09-20.** Quote-locate proven. Layer B recovered 0 of 7 materiality findings. Mean 28.5/40 vs floor 12/40. Control 9.5/17 vs floor 4/17. Production path unchanged. Report `scripts/diagnostic/delivery-check/b275-document-level-editorial.md`. | this spec 2026-09-20 | H | RECORD | See Findings log. |
| F24 | **NOT CHECKED FILTER INCLUDES EVIDENCE THAT WAS NOT REVIEWED. SHIPPED 2026-09-20.** Frontend `33c83c6`. Filter matches `summaryClass.evidence === notChecked`. **B294** dropped the extra `displayVerdict` `not reviewed` match: that pulled switched-off evidence into Not checked. R4 after is 0. Tests `tests/f24-not-checked-filter.test.mjs`. Do not close **B235**. | this spec; B294 | L | RECORD | Filter, not the QRS bullet cap. |
| B164 | **INGESTION WARNING REACHES THE MAIN PATH. SHIPPED 2026-09-20 `75318f9`.** `ingestionMetaFromPrep` copies `sourceIngestionWarning` / `totalTextLowWarning` onto analyse-statements meta. Does not call `computeGuardrailForSource`. Tests `tests/b164-ingestion-warning.test.mjs`. | this spec | M | RECORD | Computed, now shown. |
| B247 | **OFF CHECKS STORE not_reviewed. SHIPPED 2026-09-20 `75318f9`.** Stage 7 stamps `not_reviewed` when editorial or compliance is off. Screen and payload agree. Tests `tests/b247-stored-verdict-honesty.test.mjs`. Recorded r3 fixture kept as the before picture. | this spec | M | RECORD | Stored value matches the screen. |
| B249 | **UNCHECKED SENTENCES ARE DISCLOSED. SHIPPED 2026-09-20 `75318f9`.** Dropped not-a-claim rows carried forward. Coverage after the split names every non-whitespace uncovered run. Quality Review Summary lists the sentences. Split unchanged. Tests `tests/b249-unchecked-sentences.test.mjs`. Do not close **B248**. | this spec | H | RECORD | Disclose, do not override. |
| B250 | **FAILED MATCH IS NOT REVIEWED. SHIPPED 2026-09-20 `75318f9`.** Throw or malformed Stage 2 is `not_reviewed`, not `no_support`. Mixed confirmed plus failed stays confirmed. Tests `tests/b250-failed-match-not-supported.test.mjs`. | this spec | H | RECORD | Fake check closed. |
| B251 | **UNLOCATABLE MATCHER PASSAGE KEEPS THE CLASS AND DROPS THE QUOTE. SHIPPED 2026-09-20 `75318f9`.** `excerptNotLocatable` on the card. Classification unchanged. Tests in `tests/b259-unlocatable-excerpt.test.mjs`. **Annotation 2026-09-21 (B305 3.8):** "The matching passage could not be located in the named source." is this hole seen from the card. The remaining spanned B1 quotes are **B264**. No new row. | this spec | H | RECORD | Quote gone, class remains, now labelled. |
| B259 | **UNLOCATABLE EXCERPT IS NOT DISPLAYED. SHIPPED 2026-09-20 `75318f9`.** A card stores `primaryExcerpt` null unless a supportSpan locates the passage. r6 Oakfield quote is the exhibit. Tests `tests/b259-unlocatable-excerpt.test.mjs`. Related **B264** still open for spanned B1 quotes. **Annotation 2026-09-21 (B305 3.7):** the unrelated-document quote fell from 26 cards to 6. The six remaining with no locatable span show this honest hole rather than another document's words. The six spanned B1 quotes are **B264**. No new row. | this spec | M | RECORD | Visible hole. Not an unrelated document. |
| B274 | **PROGRESS BAR WHEN THE REVIEW WILL TAKE MORE THAN TEN SECONDS. SHIPPED 2026-09-20.** Frontend `33c83c6`. Estimate `8 + wordCount/25` seconds. Bar only if that exceeds 10. Eases to 90% at the estimate, then crawls toward 97%. **B279** replaced the caption with `About N of M statements checked`. Tests `tests/review-progress-estimate.test.mjs`. | this spec | H | RECORD | Generous estimate. Server unchanged. |
| B277 | **STAGE 5/6 SCHEDULER DERIVED FROM THE REAL DRAFT. SHIPPED 2026-09-20 `8f15563` tag `b277-b278-scheduler-and-wait`.** Concurrency is `N` when the stage fits the remaining TPM window, else `floor(remaining / tokensPerStatement)`. Live headers correct remaining. Production: 6/6, 25/25 (1 wave vs 7), 74/74, 187 editorial on the memo. Tests `tests/b277-stage-schedule.test.mjs`, `tests/b277-preflight-guard.test.mjs`. Report `scripts/diagnostic/delivery-check/b277-scheduler-and-wait.md`. | this spec | H | RECORD | A constant pool is not a schedule. |
| B278 | **WAIT UNTIL A REFUSED CALL FITS. SHIPPED 2026-09-20 `8f15563` tag `b277-b278-scheduler-and-wait`.** Same commit and tag as B277. No retry count. No 2s cap. Run 4 wait line: `waitMs=119762 boundMs=121610 marginMs=38887 requested=17329 attempt=2`. 102 previously lost checks came back except 1 compliance `not_reviewed` `rate_limit_window`. Tests `tests/b278-rate-limit-wait.test.mjs`. | this spec; B276 | H | RECORD | Completeness. Never clean. |
| B279 | **PROGRESS COUNTER DURING A REVIEW. SHIPPED 2026-09-20 `6df12bd` tag `b279-progress-count`.** Frontend only. `About N of M statements checked`. Shown only when the estimate exceeds ten seconds. Never reaches M until the result lands. Tests `tests/review-progress-estimate.test.mjs`. **Superseded by B287** (counter always shows; "About" removed). | this spec | H | RECORD | Estimate. Not progressive delivery. |
| B285 | **SHARED WAIT CANNOT STRETCH WITHOUT A REAL DEADLINE. SHIPPED 2026-09-20 `7cdb1c9`.** `requireStore` no longer fabricates a 300s bound. No ALS context: `NO_BUDGET_FALLBACK_BOUND_MS = 6000` (pre-B278 3x2s), logged once per request, total-waited accumulator so the bound cannot re-base. Attempt ceiling 1000 as arithmetic backstop. Cause: B278. Tests `tests/b278-rate-limit-wait.test.mjs`. Report `scripts/diagnostic/delivery-check/b285-shared-wait-regression.md`. | this spec; B278 | H | RECORD | Completeness. Never clean. |
| B286 | **EVERY MODEL-CALLING ENDPOINT OPENS A BUDGET. SHIPPED 2026-09-20 `7cdb1c9`.** The eleven `callLLM` handlers in `api/` now call `beginRequestBudget` with `FUNCTION_MAX_DURATION_MS` and that endpoint's `STAGE_MODELS` model. Enumeration test `tests/b286-endpoint-budget.test.mjs` fails when a new handler forgets. The Part 1 fallback stays as the loud backstop. | this spec; B285 | H | RECORD | Deadline. |
| B287 | **COUNTER ON EVERY REVIEW. SHIPPED 2026-09-20.** Frontend `deda15a`. Ben's ruling 20 September, superseding B279's ten-second rule. Caption `N of M statements reviewed`. Never reaches M before the result lands. Tests `tests/review-progress-estimate.test.mjs`. | this spec; B279 | H | RECORD | Estimate. |
| B288 | **CANCEL A RUNNING REVIEW. SHIPPED 2026-09-20.** Frontend `deda15a`. AbortController on `apiAnalyseStatements`. Restores pre-review draft and sources. Saves nothing. The Function still finishes and is still paid. Tests `tests/b288-b289-cancel-and-dead-buttons.test.mjs`. | this spec | H | RECORD | Control. |
| B289 | **DEAD BUTTONS GO FLAT. SHIPPED 2026-09-20.** Frontend `deda15a`. With no proposed changes, bulk accept, bulk reject, discard, and save are disabled. Save label is `Save` when the count is zero. Tests `tests/b288-b289-cancel-and-dead-buttons.test.mjs` (frontend). | this spec | H | RECORD | Honesty. |
| B290 | **SAY WHEN THE REVIEW IS WAITING FOR CAPACITY. SHIPPED 2026-09-20.** Backend `f02323c`. Frontend `7ea2db9`. The 13:40Z hang was `analyse-statements` itself on 429 retries (~280 s), not synthesize-review and not the B285 fallback. `meta.capacityWait` counts prelaunch holds and 429 sleeps. A stalled counter gains a second line after 8 s. After landing, one fact names the wait. `requireStore` no longer `enterWith`s the 6 s fallback. Tests `tests/b290-capacity-wait.test.mjs`. Report `scripts/diagnostic/delivery-check/b290-what-hung.md`. | this spec; B285 | H | RECORD | Honesty. Time is an input. |
| B291 | **RECORD THE PROVIDER REFUSAL. SHIPPED 2026-09-20 `3f08bd9`.** One `[PROVIDER_REFUSAL]` line per request: status, code, type, message, `x-ratelimit-*` and retry-after or explicit absence. Same object on `meta.providerRefusal`. The 20 Sep hang had none of this. Tests `tests/b291-refusal-recorded.test.mjs`. | this spec | H | RECORD | P31. |
| B292 | **STOP RETRYING A TERMINAL REFUSAL. SHIPPED 2026-09-20 `3f08bd9`.** `credit_balance_exhausted` / `insufficient_quota` fail immediately. Message text is not a rate limit. If nothing has succeeded, wait at most 8 s. User copy `The review did not run.` Tests `tests/b292-terminal-refusal.test.mjs`. Report `scripts/diagnostic/delivery-check/b291-why-no-capacity.md`. | this spec; B278 | H | RECORD | Completeness. Never clean. |
| B293 | **THE COUNTER STOPS CLAIMING WORK IT CANNOT SEE. SHIPPED 2026-09-20.** Frontend `aa1bff6`. Caption is `N of M statements`, not `reviewed`. After the estimate, freeze at 90% and say the review is still running, nothing has come back. `ok:false` is `The review did not run.` Interim until server-reported progress. Tests `tests/b293-counter-honest.test.mjs`. | this spec; B287 | H | RECORD | Honesty. |
| B294 | **NOT REQUESTED IS ITS OWN STATE. SHIPPED 2026-09-21 `74e75c9`.** `classifyCard` still returns `null` when a check is off and `notChecked` when it was requested and missed. `summaryClass.cardTone` is stamped from those classes only. Off does not vote. A miss cannot be green or clean. Tests `tests/b294-not-requested.test.mjs`. Report `scripts/diagnostic/delivery-check/b294-card-colour.md`. | this spec; B247; B245 | H | RECORD | Off is a choice. A miss is a hole. |
| B295 | **ONE VOCABULARY FOR THE THREE CHECK STATES. SHIPPED 2026-09-21 `74e75c9`.** Requested and clean: Clean. Requested and did not complete: Not checked (B194). **B298 amended Part 3.3:** not requested is no longer omitted. It is named on a footer line below Clean. "Needs manual review" and grey "Not reviewed" for an off check remain gone. No dedicated test file; same commit and `tests/b294-not-requested.test.mjs` as B294. Report `scripts/diagnostic/delivery-check/b294-card-colour.md`. | this spec; B194; B298 | H | RECORD | One wording per state. |
| B296 | **CARD COLOUR IS THE BACKEND STAMP. SHIPPED 2026-09-21.** Backend `74e75c9`. Frontend `fe906d4`. Left border reads `summaryClass.cardTone`. `deriveTintClass` maps that stamp. 150-cell grid `tests/b296-card-colour-grid.test.mjs`. Evidence-only confirmed is green. A requested miss is never green. | this spec; B245; B294 | H | RECORD | One calculation. |
| B297 | **A FAILED REVIEW OFFERS RETRY. SHIPPED 2026-09-21.** Frontend `fe906d4`. `The review did not run.` has Retry beside it. Same draft and sources. No clear. No loop. Tests `tests/b296-card-colour-retry.test.mjs`. | this spec; B292 | H | RECORD | Control. |
| B298 | **A TURNED-OFF CHECK SAYS SO ON THE CARD. SHIPPED 2026-09-21.** Frontend `142e6f5`. Backend record `0d62c8f`. Footer line below Clean. **B304 amended the joiner:** `Turned off for this run: Editorial and Compliance reviews.` Same wording on the results-screen helper and the export. Neutral `⊘`, not a tick. Off still does not vote. A requested miss stays a row. Tests `tests/b298-turned-off-line.test.mjs`. Report `scripts/diagnostic/delivery-check/b298-turned-off-and-reasons.md`. | this spec; B295; B194 | H | RECORD | Visible off. Not a miss. |
| B299 | **AN ABSENT SETTING NEVER MEANS THE CHECK RAN. SHIPPED 2026-09-21 `6bc97cf`.** `asOptions` and every check-ran reader use `=== true`. The writer `resolveReviewOptionsFromBody` logs an absent or non-boolean key and stamps false. Tests `tests/b299-absent-setting.test.mjs`. | this spec; B294; P31 | H | RECORD | Loud at the writer. |
| B304 | **ONE RULE FOR LISTING CHECK NAMES, ON BOTH FOOTER LINES. SHIPPED 2026-09-21.** Backend `78df276`. Frontend `1d3669a`. Amends B298 Part 1.3. `joinCheckNames` is the only joiner. Turned-off: `Editorial and Compliance reviews.` Clean: `Source recency and Framing.` Trailing period on both. Order: Evidence, Editorial, Compliance, Source recency, Framing. Tests `tests/b304-name-list.test.mjs`. Report `scripts/diagnostic/delivery-check/b298-turned-off-and-reasons.md`. | this spec; B298 | H | RECORD | One joiner. |
| B303 | **THE ASSESSMENT AREA DISTINGUISHES NOTHING-TO-SAY FROM COULD-NOT-BE-WRITTEN. SHIPPED 2026-09-21.** Frontend `d128ac5`. Frontend prints `assessmentCopyForReason(reason)`. (a) `Nothing for the assessment to add.` (b) `The assessment was not written for this run.` (c) keeps B285. Replay stamps (b) at the skip writer. (a) and (b) are slate-600, not an alarm. Tests `tests/b303-assessment-states.test.mjs`. | this spec; B302 | H | RECORD | Backend reason. Frontend print. |
| B302 | **THE ASSESSMENT SAYS WHICH OF THE THREE THINGS HAPPENED. SHIPPED 2026-09-21 `1d7e5b8`.** `synthesize-review` returns `reason` on every empty path (`lib/qc/assessment-reason.mjs`, same pattern as `not-reviewed-reason.mjs`). Extra routes map to `could_not_be_written` rather than the B285 sentence. Absent `reviewOptions` on `assembleCard` is `not_reviewed`, never `clean`, logged `[ASSEMBLE_CARD] reviewOptions missing`. Tests `tests/b301-assessment-states.test.mjs`, `tests/b302-absent-options-not-clean.test.mjs`. | this spec; B299 leftover | H | RECORD | Empty is a state. |
| B301 | **ONE BLANK FINDING DOES NOT SILENCE THE ASSESSMENT. KILLED 2026-09-21 `7095b03`.** Guard never fires on recorded reviews. Screenshot was a replay skip. B227 stands: a blank finding still never reaches the model. Mixed blanks return `blank_finding`; all-blank returns `nothing_to_say`. Report `scripts/diagnostic/delivery-check/b301-assessment-empty.md`. Tests `tests/b227-assessment-blank-finding.test.mjs` still prove the guard. | this spec Part 1 | H | RECORD | Kill. Do not drop-and-keep. |
| B300 | **A FAILED CHECK RECORDS WHY. SHIPPED 2026-09-21 `6bc97cf`.** Every not-completed path stamps `editorialNotReviewedReason` / `complianceNotReviewedReason`. Vocabulary: `rate_limit_window`, `schema_invalid`, `empty_completion`, `thrown_call`, `zero_token_failure`, `empty_result`, `missing_provider_key`. Logged `[CHECK_NOT_REVIEWED]` once per statement with request id. Not shown to the reviewer. Tests `tests/b300-not-reviewed-reasons.test.mjs`. | this spec; B291 | H | RECORD | Hole with a reason. |
| B305 | **ROADMAP AND BACKLOG MATCH WHAT ACTUALLY HAPPENED. SHIPPED 2026-09-21.** Docs only. Report `scripts/diagnostic/delivery-check/b305-doc-sync.md`. No product code. | this spec | -- | RECORD | Governance truth. |

---

## Rejected (do not re-propose)

| ID | Item | Why rejected | Date |
|----|------|--------------|------|
| — | **Relational linkage check** (LLM check on causal / ordering connectives) | REJECTED ON EVIDENCE 2026-08-19. After the word-boundary fix (`b81929b`), only **11** corpus sentences match the connective blocklist, and most are `up from` (plain comparison, not causation or ordering). Genuine relational risk is 4–5 sentences, under 2% of the corpus. Not worth an LLM call per sentence or the concern-noise it would add. | 2026-08-19 |

---

## How to update this file

This file is updated by Cursor on instruction from the user. The standard pattern:

**To add a new item**, the user gives Cursor a short instruction like:

> Add to BACKLOG.md under [section]: [one-line description]. Source: [where it came from]. Priority: [H/M/L].

Example:
> Add to BACKLOG.md under Frontend / UI: "Tooltip on Editorial signal explains the rule that fired." Source: R2.4 testing. Priority: L.

Cursor adds the row at the bottom of the relevant table with the next available ID (F7, B12, etc.) and writes the entry. No reformatting of existing rows.

**To close an item**, the user gives Cursor:

> Close BACKLOG.md item [ID]. Resolved by [tag/spec ID].

Cursor moves the row from its current table into the "Closed" table with the resolution tag noted, and adjusts the IDs only if necessary (gaps in numbering are fine — leave them).

**To re-prioritise**, the user gives Cursor:

> Update BACKLOG.md item [ID] priority to [H/M/L].

Cursor edits the priority column. No other changes.

**To set Launch**, the user gives Cursor:

> Update BACKLOG.md item [ID] Launch to [LAUNCH/AFTER/RECORD].

Cursor edits the Launch column. No other changes.

**Periodic review**: at the end of each sprint, the user reviews this file with Claude to identify items ready to schedule. Items moving into a sprint don't get closed until shipped — they stay in the backlog with a note like "(scheduled in R3.1)".

---

## Conventions

- **Priority**: H = should pick up in next sprint or two, M = should pick up within a few sprints, S = small / cheap, L = nice-to-have, no urgency. `--` = watch-only / no priority.
- **Launch**: LAUNCH = needed before a first client uses the product. AFTER = worth doing, not needed for a first client. RECORD = not a task (Findings log, Standing rules, Closed rows).
- **Source / context**: where the item was identified. Useful for tracing back to a chat or Langfuse trace if context is needed.
- **IDs**: prefixed by table — F (Frontend), B (Backend), P (Process), Pr (Product). Numbers are assigned sequentially within each table and never reused.
- **No detailed specs in this file**: each row is a one-liner. When an item is scheduled, a proper spec gets written.
