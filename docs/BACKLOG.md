# Brightline Content Engine — Backlog

This document is the single source of truth for known issues, deferred work, future improvements, and product enhancements that have been identified but not yet scheduled into a sprint.

It complements:
- `docs/ROADMAP.md` — the active sprint plan
- `docs/ARCHITECTURE.md` — the system design
- `ai/AI_OPERATING_MANUAL.md` — how Claude/Cursor work on this codebase

---

## How to use this file

**When something gets deferred** during a working session — a UI bug, a code-hygiene item, a future optimisation, a copy change, an architecture nuance — it gets a row in the right table below.

**When something is ready to schedule**, it moves from a backlog table into a spec (and eventually into the roadmap).

**When something is shipped**, the row is moved to the bottom of this file under "Closed" with the spec ID and tag that resolved it.

**Adding new items**: see "How to update this file" at the bottom.

---

## Tables

The backlog is split into four tables by character of work:

1. **Frontend / UI** — user-visible bugs, copy, layout, polish
2. **Backend / Pipeline** — non-blocking pipeline issues, optimisations, architecture nuances
3. **Process & governance** — operational items, eval discipline, doc maintenance
4. **Product** — net-new features, capability gaps

---

## 1. Frontend / UI

| ID  | Item | Source / context | Priority | MVP | Notes |
|-----|------|------------------|----------|-----|-------|
| F5  | UI display labels misaligned with architecture rubric | R2.3 user observation | M | MVP | "Supported" should be "Confirmed". Align all four labels: confirmed / partially confirmed / conflicting / no support. Decision: more definitive language preferred for an audit-safe product. |
| F6  | "1 claim have" grammar bug in Reviewer Assessment | userMemories | L | Post-MVP | Pre-existing item from product backlog. |
| F10 | Single-concern Editorial cards may render the lone concern as a bullet — check rendering consistency across single-concern Editorial cards (bulleted vs inline). | R2.7.2 Run 1 testing | L | Post-MVP | |
| F11 | **Author-feedback UI:** top-right controls (Hide assessment summary + View/Generate author feedback) look cluttered stacked together. Try ghost/text buttons or relocating the collapse toggle to the container edge. | B26.2.2 testing 2026-06-30 | L | Post-MVP | |
| F16 | **Accepted limitation (restore / Review).** After a refresh, a PDF or Office source must be re-uploaded before Review can run again. Autosave still restores draft, extracted text, version history, and cards (`v8.73.0-review-state-no-blobs`). Reason: the backend refuses inline text for PDFs (`PDF_INLINE_TEXT_NOT_ALLOWED`) so extraction always happens server-side from the original bytes. Pill copy: "Re-upload to run again" (`v8.74.0-review-state-tidy`). Blob storage (**Pr14**) would also remove this limitation; that work is deferred. | review-state 2026-08-21 | -- | Post-MVP | Accepted. Not a bug. |

**Suggested grouping**: F1–F5 share the Assess module surface and could be bundled into a single frontend polish sprint, e.g. `v8.43-assess-polish`.

---

## 2. Backend / Pipeline

| ID  | Item | Source / context | Priority | MVP | Notes |
|-----|------|------------------|----------|-----|-------|
| B1  | Stage 2 prompt nuance: "all X, therefore not Y" patterns | R1.2.5.3 reproducibility findings (P02, P25) | L | Post-MVP | When source says "all employees in Ottawa" and draft claims "offices in Berlin," current rubric drifts between `no_support` and `conflicting` at temp 1. Worth a prompt-tightening spec eventually, but not blocking. |
| B2  | "Excerpt could not be retrieved" on conflict cards | R1.2.3, R2.3 testing | L | Post-MVP | v3 Stage 4 artifact. Architecture says Stage 2 should already return the passage; v3's separate retrieval step is unnecessary. Should resolve when R2.5 (Stage 4 in v4) lands. Substantially mitigated by r2.5.3 for bracket-abridgement cases. Residual placeholder appears only on silent-splice cases — see new backlog item B-next on splice handling. |
| B3  | **Stage 2 prompt-prefix caching (un-parked, re-scoped).** Measured Langfuse on the B53a shadow (2026-08-19): 97% of API spend is Stage 2 **input** tokens (~4,000/call), almost all source text re-sent per statement. OpenAI discounts repeated prompt prefixes automatically above ~1k tokens for gpt-4o; this is not Anthropic-only. Caching only applies if the source sits at the **front** of the Stage 2 prompt. Today it does not: `stage2_v4.md` is the **system** rubric (no source); the **user** message is `Statement:` then `Source:` (`matchSingleSource` in `stage2-match-sources.mjs`). The varying statement sits ahead of the repeated source, so the source cannot join the cached prefix. Fix: put source (or source + rubric) first, statement last. Cross-ref parked **B38** (batch API remains parked). | R1.2.4; B53a Langfuse spend 2026-08-19 | M | Post-MVP | Un-parked. gpt-4o automatic prefix cache. |
| B4  | LLM call consolidation: merge editorial + compliance + style | userMemories | M | Post-MVP | Architecture-level optimisation. Three review calls per statement could potentially become one. Cost model projects ~19% saving on production runs. Defer until v4 is fully live. |
| B5  | Stage 2 chunking cost ceiling for long sources | userMemories | L | Post-MVP | Pre-existing. Long sources can blow up Stage 2 token cost. Defer. |
| B7  | Document "internal source contradiction" handling | R1.2.5.2 finding | L | Post-MVP | Stage 2 already handles this correctly (returns `conflicting` when a source contradicts itself), but the architecture document doesn't acknowledge this case. Add a paragraph to architecture section 5.2. |
| B8  | E2 deterministic reimplementation | userMemories | L | Post-MVP | Pre-existing. Defer. |
| B9  | Implement-changes sprint (writer applies QC suggestions) | userMemories | L | Post-MVP | Pre-existing. Defer. Interim lighter version: **Pr9** (shipped). **Unblocked-but-not-built (2026-08-21):** a store exists (`persist-review-state`), but `review_state` is an overwrite autosave buffer. B9 needs append-only finding-decision event rows in a separate table (ARCHITECTURE §9). **Reframe (2026-08-23):** acting on a finding and acting on an edit are two different workflows. B9 as scoped covers only the first. The chapter's first question is where in the flow the human makes each decision, not how accept and reject work on a card. Claude project docs (not in this repo): `claude/pr9-rewrite-correctness-arc.md`, `claude/pr9-finding-handling-rulebook.md`. |
| B10 | Spring clean / codebase hygiene | userMemories | L | Post-MVP | Pre-existing. Defer to after v4 is live and v3 is deletable. |
| B11 | Public version prompt | userMemories | L | Post-MVP | Pre-existing. Defer. |
| B12 | Add canary score for non-schema Stage 2 failures (network errors, timeouts) so they're tracked in Langfuse alongside schema failures. | R2.3 implementation review | L | Post-MVP | |
| B14 | Model non-compliance with explicit prompt rules: gpt-4o does not reliably honour negative constraints (e.g. 'do not abridge passages') on dense compound statements, even at temp 0. Future stages should design defensively — validation layers that handle expected non-compliance modes rather than rely on the model's adherence. | R2.5.2.1 diagnostic finding | M | Post-MVP | |
| B15 | Stage 2 occasionally returns silently spliced passages (multiple non-adjacent source spans presented as contiguous, with no ellipsis marker). Validator correctly rejects these, but the resulting placeholder appears on conflict cards. Possible mitigations: (a) two-call retry asking for a single contiguous span when first call's passage fails validation, (b) accept and surface explicitly as 'spliced excerpt - confirm against source'. To consider after pipeline-v4 fully live. | R2.5.3 testing | M | Post-MVP | |
| B17 | **R2.7.2.1 — Relative-source-period resolution.** Stage 2 period matching only works explicit-vs-explicit; fails when the source period is a relative reference requiring document-date inference. Proposed fix: deterministic date-resolution pass pre-Stage-2. See ROADMAP **R2.7.2.1**. **Note (2026-08-18):** the supersession work (`review-supersession`) added date-aware machinery at the VERDICT layer (`extractSourceAsOfDate` fallback in `resolveSupersession`), but B17's Stage-2 relative-period MATCHING remains unaddressed. Cross-ref `review-supersession`. | R2.7.2 testing | M | Post-MVP | |
| B18 | Stage 5 evidence commentary editorialises rather than describes (e.g. 'This is a significant discrepancy'). Calibrate Stage 5 prompt toward neutral description per QC Output Language Standard. **Watch item** — pending next diagnostic batch after `commentary-calibration` (2026-06-03); 0 editorialising tone markers in 2026-06-01 batch. Close if next batch also clean. | R2.7.2 Run 1 testing | M | Post-MVP | |
| B27 | **Named-individual suppression reliability on subtle attribution drift.** R6.6.3 proviso (suppress only when source uses the named person the same way the draft does) verified only on a clear-cut case — fabricated forward-looking quote. Unproven on subtler drift (same role, shifted emphasis; paraphrased vs invented quote). Fail-safe bias means subtle cases should fail toward KEEPING the concern, so risk is residual noise not silent over-suppression — but confirm on a future diagnostic batch that suppression never drops a genuine consent gap. | R6.6.3 testing | L | Post-MVP | Watch only |
| B30 | **Thorough regression calibration matrix (deferred).** R6.13.2 shipped light coverage (two guard fixtures). Full version: every regression fixture declares version/output-type/publication-state asserted against expected rule subsets. Build only if calibration drift starts showing up. Source: R6.13.2. | R6.13.2 | L | Post-MVP | Deferred |
| B31 | **Silent-default handler canary (deferred).** Scoped narrowly: emit a canary when a draft-review request reaches the API handler without review intent (always wrong for a real UI request); low-noise unlike per-default canaries. Source: R6.6-audit. | R6.13-audit | L | Post-MVP | Deferred |
| B38 | **API batching (parked).** Batch API (50% off, up to 24h latency) for offline diagnostic/regression sweeps only. Do not apply batch latency to interactive Review. Prompt-caching half moved to **B3** (OpenAI automatic prefix cache is live for gpt-4o; blocked by current Statement-then-Source order). | post-A10 scoping 2026-07-09 | L | Post-MVP | Parked (batch only) |
| B39 | **Conflict-with-confirmation card surfacing (review candidate).** Confirmed verdict + `hasConflict` true can render green border when editorial/compliance clean (`deriveTintClass` ignores `hasConflict`). Data preserves conflict; card surface may not. Post-demo review — no change now. See ROADMAP **Watch items → D1**. | post-A10 diagnostic 2026-07-09 | -- | Post-MVP | Watch / review candidate |
| B41 | **Stage-2 multi-passage widening (depth-of-support).** Matcher emits multiple passages per statement per source (currently one pair). Enables depth-of-backing signal beyond Build A's additive single widened list. **VERDICT-ADJACENT** — needs own neutrality diagnostic (shadow-diag pattern) before build. Independent of Build B; can run in parallel with drawer UI (**F12**). See ROADMAP **R7**. | R7 Option-2 depth 2026-08 | M | Post-MVP | Verdict-adjacent. Attention-Router throughline (ROADMAP). |
| B42 | **includeFiles bundle rework (real fix).** `excludeFiles` alone cannot reliably defeat load-bearing `includeFiles: "node_modules/**"` (see `docs/R7_BUNDLE_ANALYZE_FINDINGS.md`; esbuild/@napi-rs excludes are interim). Narrow/replace the blanket include; let NFT trace per-function; **explicitly re-include** OpenAI transitive deps + dynamic `import("officeparser")` that NFT historically missed (original reason for the blanket + 256-char limit). Fold **`tests/**` trim** into this pass (test files currently ship into every function via the same include string — do not edit include alone without that careful rework). **PREVIEW-deploy verify** before production. Currently **masked** by `VERCEL_SUPPORT_LARGE_FUNCTIONS` beta flag (crutch). | Vercel bundle / R7 2026-08 | M | Post-MVP | Drop beta flag after |
| B43 | **Compressed 50MB function limit — unmeasured.** Re-check after **B42** includeFiles rework (uncompressed 250MB and compressed 50MB are separate gates). | Vercel bundle 2026-08 | L | Post-MVP | After **B42** |
| B44 | **alp/crf authority-assertion harness quirk.** `qc:test` raw harness 7/9 vs 9/9 first-card evidence-pass: `alp_multisource` / `crf_multisource` fail only on `assertAuthority` (`meta.qcEvidenceAuthorities` not attached by v4 `analyse-statements`). Set `assertAuthority: false` on those fixtures or attach authorities. Cosmetic; keeps extractor/future baselines clean. | extractor baseline 2026-08 | L | Post-MVP | Harness only |
| B61 | **Stage 2 run-to-run non-determinism at temperature 0 (residual `hasConflict`).** Diagnosed 2026-08-19 (`48d8483`, `b81929b`). B53a removed **verdict** instability on the measured subset (1/38 → 0/38 with the flag on). Residual is confined to `hasConflict` on two F18 statements (5.26%), unchanged by B53a because the upgrade must not touch `hasConflict`. OpenAI `seed=1` does not pin it: three distinct `system_fingerprint` values appeared in one diagnostic. **B63 does not close this.** B63 is a process-local cost cache; it does not survive a cold start or an instance change. **Unblocked-but-not-built (2026-08-21):** Neon exists (`persist-review-state`), but that table is the frontend autosave blob, not a durable Stage 2 LLM-result cache. Cross-session `hasConflict` repeatability still needs a store of a different shape. **Annotation (2026-08-24):** do not treat B61 as a blanket prerequisite. See **B83**. Two Review runs on an identical draft (2026-08-23) held evidence verdicts 10 of 10; instability was concentrated in the editorial layer (4 of 10 differed). Verdict-level work can be measured without first solving instability. Editorial-rule work needs repeat runs. Residual `hasConflict` on F18 remains this item's original scope. **Boundary (Meridian re-run 2026-08-24):** that finding is "10 of 10 held on two runs of one draft", not "the evidence layer is stable". Do not quote the second version. On the re-run, a byte-identical statement moved from `not_supported` / high to `supported_partial` / moderate. The second answer is the better one, but it is a flip on unchanged text and it took a card carrying an unsupported eight-figure commitment from high concern to moderate. Editorial instability reproduced independently on the same run: a byte-identical sentence picked up a `sentence_length` concern in one pass and not the other. Planning consequence unchanged. | B53a shadows + Stage 2 determinism harness 2026-08-19; B63 gate 2026-08-20; persist-review-state 2026-08-21; **B83** 2026-08-24; Meridian re-run 2026-08-24 | M | Post-MVP | Still open. Cross-ref **B83**. Store exists; wrong shape. The 10 of 10 is bounded. |
| B53b | **Supersession no-ops on a compound.** **VERDICT-ADJACENT.** "14 facilities / three markets / 720 people" keeps `hasConflict=true` with the 2023 memo (12/640) instead of demoting to a `supersededSourceNote`. Atomic supersession fixture (headcount/money) passed; compound does not. Hybrid (**B53a**, shipped) helps via per-claim matching / passage pick, but "14 facilities" is not a `collectBackstopFigures` kind (percent / money / headcount only) — decomposition will not invent a facilities extractor. Split from **B53**. | Nordholt expanded-fixture / compound diagnose 2026-08-19 | M | Post-MVP | Verdict-adjacent. |
| B55 | **`first_person_plural` under-fire on "We expect".** First-person plural is a codified output-type rule (reporting commentary = third person). The deterministic backstop correctly kept all three first-person statements ("We now run", "our acquisition", "We expect"), but the LLM raised `first_person_plural` / `voice_consistency` on the first two and missed "We expect" in the same dirty-draft run. Genuine under-fire — the rule should fire on every first-person pronoun in reporting commentary. | Nordholt dirty-draft run 2026-08-19 | M | Post-MVP | |
| B56 | **Source-recency sensitivity gap: margin / contract-length (watch/decision).** Source-recency did not fire on a present-tense margin + contract-length claim (clean S4) supported only by the 3-year-old IC memo. Decide whether margin is recency-sensitive (a stale margin presented as current is arguably the target) or durable. May need `recencySensitiveReasons` expansion or a DURABLE_RE exclusion review. | Nordholt clean-draft run 2026-08-19 | -- | Post-MVP | Watch / decision |
| B57 | **Editorial embellishment slips (watch).** "surged" (overreach vs source "climbed") and "$155m" (currency symbol + USD vs sources' EUR) drew no editorial or style flag on the dirty run. Group with house-style / framing coverage watch. | Nordholt dirty-draft run 2026-08-19 | -- | Post-MVP | Watch only |
| B58 | **Stage 5 evidence commentary double-states the draft claim.** Stage 5 commentary restates the draft's own claim before describing the source evidence (e.g. "The statement says X. The source confirms X."). The first sentence adds no information — the reviewer already sees the statement on the card. Calibrate Stage 5 prompt to lead with the source finding, not a restatement of the claim. Cross-ref **B18** (editorialises) / **B22** (commentary register). | Nordholt expanded-fixture runs 2026-08-19 | M | Post-MVP | |
| B62 | **PARKED — Qualitative-claim validity rule.** **VERDICT-ADJACENT.** Separate from extractor fixes (**B64**, shipped). Claims like "almost entirely organic" or "the sector is fragmented" are verifiable but contain no number/date/name, so they revert as `anchorless_claim`. Fix would keep the anchor test **strict for the coverage guard** and relax **claim validity** to "contains an anchor OR is a complete predicative clause of at least N words". **Parked 2026-08-21:** widening the claim-span funnel was measured to produce no additional upgrades. `stage1b_v1.md` also tells the model to omit spans with no number, date, or proper noun, which duplicates the code-side anchor rule; if revived, relax the prompt as well. | `review-claim-spans` gate 2026-08-19; Stage 1b revert dump; funnel measurement 2026-08-21 | M | Post-MVP | Parked. No additional upgrades measured. |
| B66 | **PARKED — Claim-span connective blocklist is over-broad.** Would narrow to genuine causation and ordering (`driven by`, `following`, `because`, `resulting in`) and drop pure comparisons (`up from`, `down from`, `compared with`, `compared to`, `versus`). Seven of twelve original matches were `up from`. Word-boundary matching shipped in `b81929b` (killed "step-up from"). **Parked 2026-08-21:** widening the claim-span funnel was measured to produce no additional upgrades. | Sizing count 2A 2026-08-19; funnel measurement 2026-08-21 | S | Post-MVP | Parked. No additional upgrades measured. |
| B68 | **PARKED — Arithmetic self-consistency check.** L. Deterministic, no LLM cost. 14 corpus sentences pass the strict test (at least two of base / change / result stated, third derivable); all 14 are base-and-result pairs. Would build only that narrow case. Additive signal, never feeds the verdict. **Parked 2026-08-21: logged but not building.** | Sizing count 2B 2026-08-19 (strict recount) | L | Post-MVP | Logged but not building. |
| B74 | **Stage 1 statement text varies on bullet lines.** S. One run in five on F15 (identical input, cache off) dropped the leading `- ` from five list items. Words unchanged; statement text and highlight offsets changed, so the LLM cache key busts. Fix: strip leading list markers deterministically and adjust the offset so our layer absorbs the variance. **Supersedes** the earlier claim that unchanged drafts always produce byte-identical statements (measured on too small a sample; 4/5 identical is not always). | Stage 1 stability harness F15 2026-08-21 (5 repeats, cache off) | S | Post-MVP | Small. Deterministic strip, not a prompt change. |
| B75 | **Do not overstate B73.** B73 was insurance, not a live-path fix. The dash-as-sentence-boundary bug existed only in the fallback splitter (`splitDraftIntoCandidatesV2`, maxLen 240) and D-METRIC-LLM. Production Stage 1 uses the LLM extractor and reaches fallback only when that output is rejected. Shadow: LLM card texts and verdicts unchanged (100% cache). Recorded so later release notes do not treat B73 as a production-path correction. | B73 splitter gate 2026-08-21 | -- | Post-MVP | Record only. |
| B79 | **OPEN. GUARDED. Vercel 4.5 MB request-body ceiling limits source documents to about 3 MB.** Vercel rejects any function request body over 4.5 MB at the edge. Because the function never runs, it never sets CORS headers, so the browser reports a CORS failure and the app receives only `TypeError: Load failed`. The real 413 never reaches application code. Confirmed in production 2026-08-21 with an 11.3 MB PDF. After base64 inflation the practical ceiling is roughly 3 MB of source documents in total across all sources in one Review, not per file. Many real client documents will exceed this. The client-side guard (**F20**, **F21**) makes the failure honest; the tool still cannot review a document over about 3 MB. Pre-existing since first deployment. Unrelated to persistence. Found while discussing a different limit. Distinct from the 4 MB review-state autosave limit (raw bytes dropped, `v8.73.0-review-state-no-blobs`). Real fix is **Pr14** (deferred). Local checks are invalid (**P14**). | production 11.3 MB PDF 2026-08-21; `v8.75.0-source-size-guard`, `v8.76.0-size-message-wording` | H | Post-MVP | OPEN. GUARDED. Honest, not fixed. |
| B80 | **Pr9 residual: a clause-cut can be labelled CHANGED rather than CUT.** The marker wrapped the surviving sentence rather than a remnant (logged on F10 at `pr9-marker-intent`). Honesty passes either way. Intent does not reliably distinguish a rewrite from a removal if the UI wants to show those two differently. Do not change the honesty check to paper this over. | `pr9-marker-intent` 2026-08-23; `pr9-cut-punctuation` log | M | Post-MVP | Logged, not repaired. |
| B81 | **Pr9 residual: punctuation pass does not repair stranded leftovers.** `pr9-cut-punctuation` tidies joiners left behind by a clause cut (comma/semicolon/colon before terminal punct, doubled punct, space before punct, doubled spaces, dangling lowercase `and`/`or`/`but`/`nor`/`with`). It deliberately does not repair stranded prepositions, articles, verbs, or other connectives (`at.`, `the.`, `The company trades at.`, `The team now numbers.`, `including.`). A wrong repair is worse than a visible fault. | `pr9-cut-punctuation` 2026-08-23 | L | Post-MVP | Deliberate non-fix. |
| B82 | **Revision routing keys off a model-authored verb. OPEN. Latent; not currently biting.** For most editorial rules, `classifyEditorialKind` (`lib/build-revision-prompt.mjs`) reads the leading verb of the reviewer's suggested direction against `DELETION_VERB_RE` (`Remove`, `Cut`, `Delete`, `Drop`, `Strip`, `Omit`). `marketing_language_excess` is hardcoded to `"soften"` ahead of that check. A sentence written by a model therefore steers whether the revision edits or keep-and-flags. Trap: `review-hype-delete` now emits `Delete` as its deterministic prefix. If the marketing special case were ever removed, that word would match `DELETION_VERB_RE`, route to keep-and-flag, and silently stop the deletion the rule exists to perform. Durable fix: a structured action field (`delete` \| `keep`) set by the reviewer schema and read by `classifyEditorialKind`, so routing follows the finding rather than the wording. Do not fix now. **Second instance:** **B87** (restated resulting phrase). Same family. See **P20**. | review-layer chapter 2026-08-24; `review-hype-delete` (`63c359f`); Meridian re-run 2026-08-24 | M | Post-MVP | Latent. Do not fix now. Cross-ref **B87**. |
| B83 | **Instability is concentrated in the editorial layer. OPEN. Refines B61; do not duplicate.** Two full Review runs on an identical draft, 2026-08-23: evidence layer, 10 of 10 statements returned identical verdicts (conflicts, partials, and not-supporteds all stable); editorial layer, 4 of 10 statements differed. A 344-character sentence was flagged for length in one run and came back clean in the other. A filler phrase was flagged once. The same first-person problem was reported under `first_person_plural` in one run and `voice_consistency` in the other. An extra `underreach_hedging` appeared in one run only. Planning: verdict-level work can be measured without first solving instability, because the evidence layer holds. Editorial-rule work needs repeat runs to verify, because it does not. **B61** should be scoped to editorial instability specifically rather than treated as a blanket prerequisite. Residual Stage 2 `hasConflict` drift on F18 remains **B61**'s original scope. **Boundary (Meridian re-run 2026-08-24):** the finding is "10 of 10 held on two runs of one draft", not "the evidence layer is stable". See **B61**. | two Review runs 2026-08-23; refines **B61**; Meridian re-run 2026-08-24 | M | Post-MVP | Cross-ref **B61**. The 10 of 10 is bounded. |
| B84 | **False greens: a card can read fully supported while containing an unsupported claim. OPEN. Next substantive item. Verdict-adjacent; needs the full gate.** Causal exhibit (Meridian re-run 2026-08-24), stronger than the earlier "reproduced identically across two runs" wording. The clause "these returns have been generated without recourse to aggressive leverage" is identical in both Review passes. First pass, in its own sentence: `supported_partial`, concern moderate, with the evidence summary stating plainly that the source does not address leverage. Second pass, merged by the revision into the long track-record sentence: `supported_full`, concern NONE. Decomposition extracted three claims from that sentence, all confirmed. The leverage clause is not among them. Same clause, same source, opposite verdict. The only variable is what else was in the sentence. Mechanism: sentence composition determines whether a clause is examined at all. A card verdict is composed from the extracted claims, and a clause that was never extracted cannot lower it. Put an unsupported assertion beside material that checks out and it stops being looked at. "widely regarded as among the most disciplined operators" returned green for a third consecutive run on the same shape. **Before scoping this row, check `gatherConcerns` (hypothesis **B88**).** If whole-card `not_supported` never reaches the reviser, the false-greens work is about a smaller surface than it first appears. | Meridian re-run 2026-08-24 (replaces 2026-08-23 two-run wording) | H | Post-MVP | Next substantive. Verdict-adjacent. Full gate. Check **B88** first. |
| B85 | **Fixture 01 is a temporal hybrid, and B48's exhibit sits on it. OPEN. Check before treating that exhibit as settled.** Audit finding: `01_bvp_shopify_memo` is the real 2010 Bessemer memo with 2019 merchant and GMV material spliced in, almost certainly deliberately, since fixtures 91 and 92 are the temporal-gap and stale-present-tense cases built from it. That source is therefore internally inconsistent across two eras by construction. The **B48** review-calibration exhibit, where the same figure was judged differently depending on the length of the sentence containing it, lives on this fixture. Some of what reads as review inconsistency there may be the source genuinely carrying both a 2010 and a 2019 view of the same business. **Reconciliation note:** the instruction was "confirm before B48 is scoped", but **B48** already shipped 2026-08-16 (`review-B48`, `b4bc974`). This row is a check on that shipped exhibit's source, not a pre-scope gate. Confirm before treating B48 observations on this fixture as review inconsistency. | fixture audit 2026-08-24; Closed **B48** | M | Post-MVP | Check before scoping further B48 calibration on F01. |
| B86 | **The authoring organisation does not resolve in production. OPEN. Do this first. A lookup, not an investigation.** Four cards across the two Meridian Review passes report some version of "The authoring organisation could not be identified from the draft, so the actor cannot be named." The draft names the organisation in its first sentence and again in its sixth. Consequence: `review-first-person-actor` (`63de82f`) and `review-actor-config` (`cc69abc`) are UNVERIFIED IN PRODUCTION. What ran was the fallback path, which keeps the first person, flags it, and changes nothing. The fallback is well behaved and it is why the closing sentence kept its owner, but the actor-substitution path that the work built has never executed outside the harness, where fixtures override `AUTHORING_ORGANISATION` in-process. Two candidates: `AUTHORING_ORGANISATION` is unset in the deployed environment, or it is set and the resolver is not reading the draft where the name appears. **Lookup (2026-08-24):** `vercel env ls production` on `brightline-content-engine-backend` does not list `AUTHORING_ORGANISATION` among named env vars. Unset falls back in code to `DEFAULT_AUTHORING_ORGANISATION` (`Partners Group`). That does not by itself prove why the live draft failed the identify-from-draft test. See **P19**. | Meridian re-run 2026-08-24; `vercel env ls production` 2026-08-24 | H | Post-MVP | Do this first. Unverified in production. |
| B87 | **The restated resulting phrase in a deletion direction is model-authored and unchecked. OPEN. One mechanism, three symptoms, one fix.** `review-hype-repair` (`02a0212`) requires a deletion direction to state the phrase that results, so literal application does not leave stranded scaffolding. That restatement is written by the model and nothing verifies it against the instruction beside it. Symptom 1: it contradicted its own instruction. The direction read: Delete 'genuinely exceptional'. The phrase becomes 'a track record that is, in our view, exceptional: ...'. The instruction removes both words; the stated phrase keeps "exceptional". The reviser followed the phrase. A stronger word was replaced by a milder one, which is exactly the laundering `review-hype-delete` exists to prevent, arriving through the repair fitted on top of it. Symptom 2: the marker gate passes it. The span changed and the declared intent matches. Green gate, wrong output. Symptom 3: a lost sentence boundary. "It is worth noting that" opened a sentence. The direction restated the remainder as lowercase "these returns have been generated...", the reviser dropped it in, and the full stop went with it. The revised draft contains "...marked at 1.9x and 24% these returns have been generated without recourse to aggressive leverage". The segmenter confirms it: two statements in the first pass, one in the second. The punctuation pass (`pr9-cut-punctuation`) is the other candidate site for the boundary half of the fix. Cross-ref **B82**: same family, second instance. See **P20**. | Meridian re-run 2026-08-24; `review-hype-repair` (`02a0212`) | H | Post-MVP | Same family as **B82**. |
| B88 | **HYPOTHESIS, not a finding: a whole-card `not_supported` verdict may produce no edit.** Readable in one file, no run needed. Both `not_supported` / high cards survived the Meridian revision untouched. An unsupported EUR 150 million commitment and an unsupported use of "flagship" are still in the draft after a full review-and-revise cycle. Meanwhile an unsupported figure that arrived as a `partially_confirmed` claim INSIDE a decomposed card was cut properly. Hypothesis: claim-level verdicts reach `gatherConcerns` and drive edits; whole-card evidence verdicts do not. If it holds, the soften-or-cut rulebook is being applied only to claims that came through decomposition, which is a much smaller surface than intended, and it changes what the false-greens work in **B84** is actually about. Check `gatherConcerns` before scoping **B84**. Do not write this as established. | Meridian re-run 2026-08-24 | H | Post-MVP | Hypothesis. Check before scoping **B84**. |
| B89 | **`supportSpans[].classification` reads `conflicting` on spans the card treats as confirming, with `hasConflict` false, on two statements in both Meridian passes.** A stable mislabel rather than instability. Low priority on its own, but any UI colouring spans from that field will show conflicts that do not exist. **Check (2026-08-24):** the Sources drawer (`SourceReaderPanel.jsx`) does read `supportSpans.classification` for highlight colour and relation lines. Card tint in `StatementReviewCard.jsx` uses `displayMode`, not that field. Distinct from **B39** (confirmed verdict + `hasConflict` true). | Meridian re-run 2026-08-24 | L | Post-MVP | Drawer reads the mislabel. |
| B90 | **Framing fidelity false positive on "record".** It flagged that a statement uses "record" where the matched source does not, when the source line begins "Track record:". In the first Meridian pass the same rule correctly caught a 23 versus 21 percent discrepancy, so this is a precision problem, not a broken rule. Cross-ref Closed **B53c** (`review-superlative`). | Meridian re-run 2026-08-24 | L | Post-MVP | Precision, not a broken rule. |

---

## 3. Process & governance

| ID  | Item | Source / context | Priority | MVP | Notes |
|-----|------|------------------|----------|-----|-------|
| P2  | Reproducibility test pattern for future model evals | R1.2.5.3 finding | M | Post-MVP | Standard for any future eval involving reasoning models or temperature > 0: run 2–3 times, check for stability, average results. Add to `ai/AI_OPERATING_MANUAL.md` when next updated. |
| P3  | Re-eval trigger: new gpt-5-family model with materially better latency | R1.2.6 conclusion | -- | Post-MVP | Watch only. Re-run R1.2.5 if a candidate appears that addresses gpt-5's latency profile. |
| P4  | Re-eval trigger: new Anthropic Sonnet/Haiku with closed agreement gap | R1.2.6 conclusion | -- | Post-MVP | Watch only. |
| P5  | Single technical opinion risk | userMemories | M | Post-MVP | Pre-existing. Claude is the only technical sounding board. No current cross-validation. Worth flagging if anything goes wrong. |
| P6  | Business backlog gap | userMemories | M | MVP | Observability, cost modelling, pricing, second pilot user. Materially lighter than product backlog. Needs active attention. |
| P7  | Singapore government AI funding eligibility | userMemories | L | Post-MVP | Startup SG Tech POV primary, AI Accelerate parallel. Confirm prerequisites before applying. |
| P8  | **Grok 4.5 output-quality eval (parked).** Language layers only (Stage 5, editorial, compliance, B26); Stage 2 held on gpt-4o. Hold Stage 2 output constant across baseline and Grok arm for clean comparison. Full diagnostic batch ~$25–30; prefer `--only` subset first. Revisit when measured cost/quality problem points at model. | post-A10 scoping 2026-07-09 | L | Post-MVP | Parked |
| P9  | **R7 test assets + diagnostics (retain).** Synthetic corpus `tests/extraction-corpus/` (MANIFEST + multi-format ground-truth files); real ingestion samples `scripts/diagnostic/r7-samples/`; diagnostics retained: `docs/R7_STAGE2_SHADOW_DIAGNOSTIC.md`, `docs/R7_OFFSET_EXTRACTION_DIAGNOSTIC.md`, `docs/R7_EXTRACTOR_SURVEY.md`, `docs/R7_EXTRACTOR_CHECK2_ATTRIBUTION.md`, `docs/R7_BUNDLE_ANALYZE_FINDINGS.md` (+ related stage-2/build-a docs). Not a product feature — keep for revalidation. | R7 2026-08 | L | Post-MVP | Asset inventory |
| P10 | **Stage 2 concurrency cap 24 unproven under load.** Cap is `STAGE2_CONCURRENCY=24` (`e6e59a6`). No fixture case exceeds 24 pairs, so the pool never filled during measurement (mean 3619 ms/case on the subset). A real full-length draft will exceed it. Measure on the first full client draft, not on fixtures. | Stage 2 timing `--timing-once` 2026-08-19 | -- | Post-MVP | Watch |
| P13 | **`npm ci` install: dependency commits must include the lockfile.** Backend Vercel `installCommand` is `npm ci` (`vercel.json` since `9b5bd90`, 2026-01-25). `npm ci` fails if `package-lock.json` is missing or stale. Node 22 pin and Frankfurt region shipped the same day as persistence (see Closed **P11** / **P12**). | vercel.json / 2026-08-21 reconcile | H | MVP | Standing rule |
| P14 | **Payload-size behaviour cannot be tested locally.** `vercel dev` does not enforce the 4.5 MB request-body limit. An 11.3 MB file that fails in production runs successfully on localhost. Any local verification of a size-related fix is a false negative. Production is the only valid check for this class of bug. | production vs localhost 2026-08-21; `v8.75.0-source-size-guard` | H | MVP | Standing rule |
| P15 | **The Pr9 marker harness measures honesty, not correctness.** Three separate faults passed it clean with perfectly consistent notes: an invented figure, meaningless filler, and a malformed sentence. Any future Pr9 change must be judged by inspecting quoted output rather than the cross-tabulation. | `pr9-cut-punctuation` 2026-08-23 | H | MVP | Standing rule |
| P16 | **Planted faults belong only in invented fixtures.** A real document with an invented fault in it is no longer either thing, and cannot be cross-referenced against the original. Real published sources are kept verbatim. See `scripts/diagnostic/README.md` for the real-versus-invented list. | review-layer chapter 2026-08-24; `fixtures-real-vs-invented` (`2f5f403`) | H | MVP | Standing rule |
| P18 | **Telling a model what NOT to put in a gap makes it fill the gap.** A prompt line forbidding the revision from narrating its own cut produced invented filler instead. When an instruction about absence backfires, stop writing instructions and write a check. | review-layer chapter 2026-08-24 | H | MVP | Standing rule |
| P19 | **A fix verified only by its own harness is not verified.** The first-person actor harness returned 18 of 18 clean on logic that was unreachable in production. See **B86**. | Meridian re-run 2026-08-24 | H | MVP | Standing rule |
| P20 | **Where a deterministic mechanism consumes text a model wrote, the model becomes the specification.** Four instances so far: the change-log note (bit), the routing verb (latent, **B82**), the restated resulting phrase (bit twice, **B87**), and the extracted claim list that composes a card verdict (bit, and is the false greens, **B84**). Whenever a deterministic step reads model prose, ask what happens when that prose is wrong. | Meridian re-run 2026-08-24 | H | MVP | Standing rule |

---

## 4. Product

| ID  | Item | Source / context | Priority | MVP | Notes |
|-----|------|------------------|----------|-----|-------|
| Pr2 | Merge duplicate concerns | userMemories | L | Post-MVP | Merge duplicate concerns — R3.4 partial fix superseded by **R6.3** (shipped 2026-05-31, `r6.3-principle-based-suppression`). Evidence-vs-Editorial duplication on conflicting statements is now handled by per-instance LLM judgment via `editorial-duplication-judge.mjs` at card assembly (not rule-ID suppression). Canary: `editorial_concern_suppressed_by_judgment`. **Remaining open:** (a) Evidence-vs-Editorial duplication on `partially_confirmed` verdicts (R3.4/R6.3 deliberately scoped to conflicting only). Reactivate if the same noise pattern shows up on partials. (b) Editorial-vs-Compliance overlap on promotional language (e.g. `marketing_language_excess` + `regulatory_prohibited_language` firing on the same sentence). Reviewed and judged intentional — both signals serve distinct reviewer decisions (craft vs regulatory). No fix planned; revisit if reviewer feedback indicates the overlap is genuinely noisy rather than complementary. |
| Pr3 | Align Direction intensity — Evidence softer than others | userMemories | L | Post-MVP | Folded into **R6.1** (ROADMAP). |
| Pr4 | Reviewer comments follow house style | userMemories | M | Post-MVP | Pre-existing. |
| Pr5 | Hide Editorial on conflict | userMemories | L | Post-MVP | Pre-existing. |
| Pr6 | Fidelity log traceability (add draft identifier) | userMemories | L | Post-MVP | Pre-existing. |
| Pr10 | **A11.1 — Per-version QC storage in Assess.** Single flat `analysisResult` — loading earlier version shows "re-run Review" banner even when that version was reviewed. Prerequisite for tabbed base↔adaptation view. Post-Monday. | A10 sprint 2026-07-09 | M | Post-MVP | See ROADMAP **Parked → A11.1** |
| Pr11 | **A11.2 — Tabbed base↔adaptation view (deferred).** Depends on **Pr10** + multi-version model in Assess. Current: labelled timeline entries. | A10 sprint 2026-07-09 | L | Post-MVP | Deferred |
| Pr12 | **A11.3. Single-view consolidation.** Collapse Assess/Writing into unified Content Engine view. Deferred until A10 settles. Writing route off/dead. **Evidence 2026-08-22 supports deletion rather than repair:** `addUrlSource` is unreachable from any rendered control; its only caller is the URL field in `SourcesPanel.jsx`, which nothing imports; `/writing`, `/quality` and `/quality-review` all redirect to `/assess`; `FocusWorkspace` and `QualityPageLayout` remain in `App.jsx` but are on no live route. A miswiring in `addUrlSource` (it calls the URL-building helper `apiFetchUrl` rather than a client for `/api/fetch-url`) is therefore not user-visible. Deliberately not fixed: repairing a control nobody can reach would be building an unrequested feature. See ROADMAP **R4.2**. | A10 sprint 2026-07-09; reachability check 2026-08-22 | L | Post-MVP | Deferred. Evidence favours deletion. |
| Pr13 | **Approved-text library.** A library of past approved texts (list, picker, reuse). | persistence scoping 2026-08-21 | L | Post-MVP | **Unblocked-but-not-built:** a store exists, but `review_state` is one active review per browser (autosave/restore, `v8.71.0-review-state-restore`). Explicitly not a library of past reviews; no list, picker, or history UI shipped. |
| Pr14 | **DEFERRED. Blob storage for source files (removes the 3 MB ceiling).** The real fix for **B79**. The browser uploads directly to object storage and sends only a reference; the backend fetches the file server-side where the 4.5 MB rule does not apply. Also removes the re-upload-after-refresh limitation (**F16**). Scoped in full on 2026-08-21 and deliberately deferred the same day. Reasoning: the ceiling does not bite during development; building it means storing client source documents, which the persistence chapter deliberately avoided; the client deployment model is undecided, so it may be superseded by a client's own storage; **B9** is the higher-value next step. Full scoping (recommended provider, permanent choices, three decisions already taken) sits in the Claude project note `claude/blob-storage-scoping-note.md` (Claude project doc, not in this repo). Trigger to revisit: a real source document over 3 MB needs reviewing, or the client deployment model resolves in favour of the hosted app. | blob-storage scoping 2026-08-21 | L | Post-MVP | Deferred. Not scheduled. |
| WR2 | **Multi-client prompt libraries** — generalize WR1's PG-hardcoded prompt library into a client-keyed, data-driven layer: client id -> event types -> versions -> prompt blocks + client bindings (entity name, verb-by-event-type, house-style rules, exclusion lists) as DATA, not literals. Exactly one client library active at a time (single global config value, not per-user). Deterministic exclusion filter reads exclusions from the active library. Provisional storage: committed files, one per client, active client via one config value. Also folds in WR1 deferrals: blank-transaction-date modal guard; graceful fallback when an event type has no library entry. Defer true both-version output separately. | post-WR1 scoping | M | Post-MVP | Post-demo follow-on from **WR1**. Keep one active client globally; no per-user client switching. Attention-Router throughline (ROADMAP). |
| WR2.1 | **True both-version output** — single generate produces Complete and Public variants (post-WR1 demo). Currently one version per Generate. | WR1 ship 2026-07-06 | M | Post-MVP | Follow-on from **WR1**. Deferred separately from **WR2**. See ROADMAP **Recently shipped → WR1**. |
| B32 | **Event-type awareness** (writing matrix + review expectation handling) — see ROADMAP **R6.14**. Review-side SHAPE UNDECIDED (Option 1 filter dimension vs Option 2 expectation profiles). Sequenced after B21–B23. | roadmap-eventtype scoping 2026-06-25 | M | Post-MVP | Generate/Rewrite framing retained; QC path cleaned (**B28**). Demo scaffold shipped (**WR1**, 2026-07-06). Re-add to QC if R6.14 ships. |
| W2  | **R6.12 residual LinkedIn editorial noise** — F12 S6 `structural_integrity` and S8 hyperbole borderline on social formats after R6.12 ship. Monitor if more LinkedIn fixtures added. See ROADMAP **Watch items → R6.12 residual LinkedIn editorial noise**. | R6.12 verification 2026-07-05 | -- | MVP | Watch only |
| W1  | **internal_plausibility** — may still attempt to fire on statement-vs-source figure discrepancies (rounded vs exact). Scope wording in place (R6.2e); in testing suppression came via the fidelity gate (cited source figure absent from statement), not the model obeying the scope constraint — B14 pattern. User-visible result correct. Action: review next diagnostic batch; add targeted deterministic backstop only if it recurs in a form the fidelity gate misses. See ROADMAP **Watch items → internal_plausibility**. | R6.2e verification | -- | MVP | Watch only |
| B33 | **B21 residual (observable, not open):** a category mis-tag whose `ruleId` is in NEITHER rulebook cannot be salvaged by reclassification and would still fall back. Not observed across the B21-diag-confirm set (zero such drops). Now self-identifying in `pipeline.log` via `rejectReason` `ruleId_not_in_either_rulebook:<id>`. No action unless it appears. | B21 | L | Post-MVP | Watch only |

### B26 — Watch (signoff duplication; chapter closed, watch remains)

Readiness/signoff logic now exists in two places — `lib/qc/signoff-verdict.mjs` (backend, feeds Constructive Feedback) and the frontend hooks `useDraftState.jsx` / `useAssessState.jsx` (Reviewer Assessment). They are intentionally kept in lockstep to guarantee the two surfaces never disagree on whether a draft is ready. Any change to the frontend signoff thresholds must be mirrored in `signoff-verdict.mjs` (and vice versa). Alignment is verified live by the B26 Step 4 test (same run → same readiness on both surfaces). See ROADMAP **Watch items → B26 — Signoff logic duplication**. **B26 / B26.1 / B26.2 / B26.2.2 / B26.2.4** shipped — watch item remains open.

### Long-horizon ideas (below top 13)

- **Per-house / per-reviewer language profile** — capture preferred phrasings, tone, capitalisation, and term substitutions per reviewer or per house, included as prompt context for the language layer only (Stage 5 commentary, B26 register, future reviewer-facing prose). HARD BOUNDARY: this never touches Stage 2 classification, Stage 3/4/7 aggregation, or any verdict-layer logic. The deterministic LLM-last architecture is preserved. Origin: 'centurion vs learning' framing from Ren Education (Straits Times, June 2026).

---

## Closed

| ID  | Item | Resolved by |
|-----|------|-------------|
|     | Trace metadata for QC runs | r1.2-pre, r1.2-pre.1 |
|     | Stage 2 prompt v2 (conflict detection) | r1.2.2-prompt-eval, r1.2.3-prompt-v2-production |
|     | Multi-provider infrastructure (OpenAI + Anthropic) | r1.2.4-multi-provider-infra |
|     | Cross-provider Stage 2 eval | r1.2.5-eval, r1.2.6-eval-closed |
|     | gpt-5-mini reproducibility test | r1.2.5.3 (under r1.2.6-eval-closed) |
| P1  | Cost model spreadsheet update | R1.x |
| Pr1 | Reviewer Assessment synthesis (narrative, not dashboard) | R4.x |
|     | R1.2 gpt-4o-mini Stage 2 source-matching eval | R1.2, R1.2.4, R1.2.5 |
|     | Pipeline v4 scaffolding | r2.1-pipeline-scaffolding |
|     | Stage 1 (statement extraction) in v4 | r2.2-stage1-extraction |
|     | Stage 2 (source matching) in v4 | r2.3-stage2-matching |
| B16 | **R2.7.2 — Stage 2 semantic frame matching** | `r2.7.2-frame-matching` — period scope explicit-vs-explicit only; relative-source-period gap → **R2.7.2.1** (B17) |
| Pr2.7.2 | **R2.7.2 — Stage 2 semantic frame matching** (Product duplicate ref) | `r2.7.2-frame-matching` — see ROADMAP **R2.7.2.1** |
|     | qcCard.pipelineVersion hardcoded `"v3"` in shared `stage7-assemble-card.mjs`, now stamped from actual route | `fix-pipelineversion-label` |
| F3  | `<>` JSON copy button missing/broken in Assess module | shipped (small frontend change, tag not recorded) |
| F4  | Enter-to-execute on Assess module's primary action button | shipped (small frontend change, tag not recorded) |
| B19 | **RESOLVED** — absorbed into and closed by **B22** (anchor eliminated; excerpt meta-phrasing count 0). | Was `commentary-calibration` — v4 `stage5_v2.md` |
| B22 | **RESOLVED** — closed by B22 / B22.1 / B22.2. Prompt-only register calibration of evidence (Stage 5) and editorial/compliance concern prose. **ANCHOR (B19):** 'excerpt' meta-phrasing eliminated — 53 instances in batch 2026-06-01 → 0 across all commentary fields (Stage 5 LLM prose + Stage 5 deterministic fallback [B22.1] + editorial/compliance concern prose [B22.2]). **REGISTER:** plainer terms (active/passive, 'hedging language', 'immediate context'), preachy framing removed, Directions concise. **SPECIFICITY (B22.1):** evidence prose must name the source's own verb/direction/figures on directional/quantitative claims — vague frames ('the same growth','this aligns') banned except where a non-quantitative source genuinely adds nothing beyond confirmation (verified acceptable, F01 S5). **MATERIALITY (B22.2):** source confirmation is no longer treated as redundancy; materiality judged against surrounding DRAFT sentences. **GUARDRAIL held:** substantive observation (conflict specifics, narrative_coherence, materiality reasoning) preserved; F18 conflict prose retains both sources' figures. **VERIFICATION CAVEATS (recorded, not papered over):** (1) B22 firing-regression was compared against the 4-week-old 2026-06-01 batch with intervening pipeline work (R2.7.2, R6.6, R6.2e/f, R6.11a), so prose-vs-logic effects were confounded rather than cleanly A/B-isolated; firing-behaviour unchanged is supported by mechanism + spot-check, not by clean isolation. (2) B22.2 materiality reasoning fix verified on diagnostic fixture 01 only; materiality on fixtures 04/08/11 not re-checked under the new principle — review on next full diagnostic sweep. Tags: `b22-commentary-calibration` (orig ship: `commentary-calibration`), `b22.1-commentary-specificity`, `b22.2-editorial-excerpt-removal`. | See ROADMAP **Near-term — Review output** |
| B21 | **RESOLVED** — Editorial schema-fallback (reliability + observability chapter). | Diagnosis (**B21-diag**) found the 5 batch fallbacks (`2026-06-01-122541`) were not malformed output or over-strict schema — they were valid JSON with real concerns discarded by a pre-R6.11a fail-fast normalizer that rejected the whole payload on the first category mis-tag (model tagged `first_person_plural` as `editorial`; it is a `style_guide` rule), with retries resending the same prompt. **RELIABILITY:** already closed by R6.11a (per-concern salvage + cross-book reclassification + retry correction note); verified across the full set (**B21-diag-confirm:** F01/F06/F14×2/F20 — zero fallbacks, zero non-salvageable drops). **OBSERVABILITY:** card already distinct via R6.11b ("Needs manual review", amber, explicit copy); raw failed output now persisted to `pipeline.log` with per-attempt `rejectReason` (**B21**, this work) — closes the gap that forced Langfuse recovery during diagnosis. **Closed decision:** F06's batch `internal_plausibility` second-concern was not reproduced (model variance); fail-fast removal evidenced indirectly by two concerns surviving on one card — not chased; re-creating a historical model output proves nothing the per-concern salvage mechanism doesn't. Tags: `r6.11a-editorial-schema-salvage`, `r6.11b-not-reviewed-state` / `v8.54.0-r6.11b-not-reviewed-state`, **B21**. See ROADMAP **R6.11**. |
| B23 | **RESOLVED** — Editorial rule bug-fix pass shipped **2026-06-03** (`r6.2e-editorial-rule-bugfix`, `r6.2f-compliance-rule-bugfix`). Per-rule outcomes: ROADMAP **Near-term — Review output → EDITORIAL RULE BUG-FIX PASS** (line ~431) — `date_format`, `percentage_notation` + `number_spelling` percentage guard, `internal_plausibility` scope, `passive_voice_overuse`, `comparative_claim_without_basis`, `forward_looking_statement_without_qualifier`. | See ROADMAP line ~431 |
| B24 | Source-public-state awareness — R6.6 chapter (R6.6.1–R6.6.4). | R6.6 closed 2026-06-25 — see ROADMAP **Recently shipped → R6.6** |
| B20 | Writing-module QC (`useDraftState.jsx` `runStatementAnalysis`) did not send the user's required version or output type, so the pipeline silently ran every Writing review as Complete / Reporting Commentary. Complete was accidentally correct (default matched); Public was dropped and never engaged public calibration. The `visibility:null` log was a separate stale-field-name defect (`editorial-compliance-reviewer.mjs` logged `visibility` while the pipeline carries `requiredVersion`). Both fixed in R6.13.1; verified by 7→8 compliance rule-subset flip on Public + press_release. Regression guard added in R6.13.2. | R6.13.1 |
| Pr8 | **RESOLVED (B26, 2026-06-30)** — Reviewer Assessment reframe. Closed by **not** reframing Reviewer Assessment: **B26 / B26.1** takes detailed author-facing constructive feedback; Reviewer Assessment (`api/synthesize-review.js`) unchanged as short reviewer-facing overview. ROADMAP open list **#19**. | **B26** |
| B26 | **SHIPPED 2026-06-30** — Constructive Feedback chapter (**B26** base + **B26.1** consolidation). Tags: `b26.1-feedback-consolidation` (backend), `v8.56.0-b26.1-feedback-consolidation` (frontend). Card-derived feedback consolidated group-by-statement — one point per flagged statement, bundled by **`cardIndex`** (not `statementText`); statements ordered worst-first (conflicting/not_supported evidence → compliance-bearing → editorial-only, `cardIndex` ascending tiebreak); within bundle Evidence → Compliance → Editorial. **Supersedes** B26 global signal-tier point ordering. Register: `CONSTRUCTIVE_FEEDBACK_EDITOR_REGISTER` (editor-to-writer, warm-through-specificity, sparing earned praise, no schoolroom/system language). UI: scrollable modal, per-run cache keyed to `activeAnalysisRunId`, Regenerate, Escape/backdrop close, focus trap (**B26.1.1 / B26.1.2** folded). Resolves **Pr8** and ROADMAP **#19**. **Watch:** signoff duplication — subsection above. | See ROADMAP **Recently shipped → B26 / B26.1** |
| B26.2 | **SHIPPED 2026-06-30** — Document-level craft pass added to constructive feedback. Tags: `b26.2.1-craft-assembly` (backend), `v8.57.0-b26.2-craft-pass` (frontend). Second temp-0 LLM call reads the full reviewed-draft snapshot (`analysedDraftText`, snapshotted at QC-run time in `useDraftState` + `useAssessState`, keyed to analysis run id) across six FT-shaped dimensions (structure/flow, core-message clarity, conciseness & precision, register & tone, opening/closing, internal coherence). Draft-anchored document-level patterns; craft section first then card-derived points; one editor voice (`CONSTRUCTIVE_FEEDBACK_EDITOR_REGISTER`); single opening + continuous numbering (**B26.2.1** folded: preamble strip, renumber, strengthened dimension-6 figure-clash scan). Scoping decision #3 relaxed to draft-anchoring for craft section only; card points remain card-anchored. Internal coherence text-internal only. Independent of assess-path `lastAssessedDraftText`. Follow-up **B26.2.2** + **B26.2.4** shipped. | See ROADMAP **Recently shipped → B26.2** |
| B26.2.2 | **SHIPPED 2026-06-30** — Constructive feedback readability + UI polish. Tags: `b26.2.2-feedback-readability` (backend), `v8.58.0-b26.2.2-feedback-readability` (frontend). **Quote-length discipline:** short fragment + opening…closing ellipsis for long sentences in both craft and card prompts (prompt-only). **Figure-overlap dedupe:** craft owns internal-contradiction flag; card point on same figure addresses source reconciliation only — craft section passed to card pass + prompt instructions; neither signal suppressed. **Frontend:** Reviewer Assessment + Quality Review Summary + generate control in one bordered collapse container; generate button inline on REVIEWER ASSESSMENT header (caption removed); **Generate/View author feedback** button; modal title **Author feedback**. Resolves queued quote-length + figure-dedupe items from B26.2 testing. | See ROADMAP **Recently shipped → B26.2.2** |
| B26.2.4 | **SHIPPED 2026-07-05** — Output-type-aware craft pass. Tags: `b26.2.4-craft-output-type` (backend), `v8.59.0-b26.2.4-craft-output-type` (frontend). Frontend sends `outputType` on author-feedback request; backend normalizes (`normalizeOutputType` / `resolveConstructiveFeedbackCraftOutputType`; absent/invalid → null → generic craft). Normalized type threaded into craft call only — per-type calibration on the six existing dimensions (LinkedIn: conversational/first-person, hook opening OK; press release: factual lede, formal register; investor letter: salutation/narrative-arc, strict structure/register; reporting commentary: default). Card pass, selection, bundling, ordering, snapshot, `{ ok, feedbackText, isReady }` contract unchanged. Resolves **B35**. Editorial voice/register closed by **R6.12** (2026-07-05). | See ROADMAP **Recently shipped → B26.2.4** |
| R6.12 | **SHIPPED 2026-07-05** — Editorial output-type voice/register calibration. Tag: `r6.12-editorial-output-type` (backend). Prompt: `buildEditorialOutputTypeCalibration` in v4-combined + legacy editorial system prompts; `reviewerNoteByOutput.linkedin_post` on `voice_consistency`, `register_mismatch`, `structural_integrity`; `formatRulesForPrompt` emits editorial `reviewerNote`. Verified F12 (voice/register over-fire cleared) + F09 control. Closes editorial half of document-type-awareness gap (craft: **B26.2.4**). Residual watch: **W2** (F12 S6/S8). Resolves **Pr7**. | See ROADMAP **Recently shipped → R6.12** |
| SRC1 | **SHIPPED 2026-07-05** — Source publication-state pill alignment + override guard. Tag: `v8.60.0-src1-source-status-override` (frontend). Pill right-aligned in Assess and Drafting source rows (name → spacer → pill → remove). Manual override guarded against late-returning summarize-source inference via `publicationStateSource: "auto" \| "manual"`; shared `applySourceSummaryPatch.mjs` used by both hooks (skips `publicationState` write when `"manual"`, still applies `description`); covered by `tests/source-publication-state-patch.mjs`. No backend/payload/Compliance-calibration change — `publicationState` already consumed by Compliance (R6.4a). Partially resolves ROADMAP Active Backlog **#18(b)**. | See ROADMAP **Recently shipped → SRC1** |
| B34 | **SHIPPED 2026-07-05** — Remove Assess Review Settings auto-detect. Tags: `b34-assess-auto-detect-removal` (backend), `v8.61.0-b34-assess-auto-detect-removal` (frontend). Removed `trySessionAutoDetect`, `setDraftTextTracked`, override lock refs, and `isAutoDetectedType` badge from `useAssessState.jsx` / `AssessModule.jsx`; dropped `apiDetectOutputType`. Review Settings manual-only — default **Reporting commentary** / **Complete**. Backend `api/detect-output-type.js` and `detect-output-type` model-config stage entry deleted as dead code. No change to Drafting, generate/rewrite/analyse/export, or QC. | See ROADMAP **Recently shipped → B34** |
| B35 | **RESOLVED (B26.2.4 + R6.12, 2026-07-05)** — Constructive Feedback craft pass was output-type-blind. Craft layer closed by **B26.2.4**; editorial voice/register layer closed by **R6.12**. Document-type-awareness gap fully addressed across both layers. | **B26.2.4**, **R6.12** |
| Pr7 | **RESOLVED (R6.12, 2026-07-05)** — Editorial voice/register over-fire on LinkedIn posts (fixture 12). Closed by **R6.12** editorial output-type calibration. Residual watch: **W2**. | **R6.12** |
| B36 | **CLOSED 2026-07-05** — Vitest standardisation. `tests/constructive-feedback.test.mjs` (backend) and `tests/source-publication-state-patch.test.mjs` (frontend) converted to real Vitest suites (`describe`/`test`; assertions unchanged as `node:assert/strict`). `npm test` runs `vitest run` in both repos, scoped to `tests/**/*.test.mjs`. Other node:assert scripts unchanged — run via `node`. Future unit tests named `*.test.mjs` are auto-picked up by `npm test`. | Vitest standardisation 2026-07-05 |
| B25 | **SHIPPED 2026-07-05** — Verdict-label consistency across surfaces. Export (`api/export.js` `normalizeVerdict`) verdict strings aligned to card vocabulary — Confirmed / Partially confirmed / Conflicting / No support — with `supported_partial` split from `supported_full` (was collapsed to "Supported"). "not reviewed" → null and "Unverifiable" default unchanged. Conflict long-line in `displayVerdictLabels.js` (`evidenceVerdictLineFromCard` + `evidenceVerdictLineFromSupportState`) corrected "Conflicting sources" → "Conflicts with sources" (short badge "Conflicting" unchanged). Display strings only; no verdict enum/logic change. Tags: `b25-verdict-label-consistency` (backend), `v8.63.0-b25-verdict-label-consistency` (frontend). | See ROADMAP **Recently shipped → B25** |
| B29 | **SHIPPED 2026-07-05** — v4 review-toggle honouring + skipped-signal card rows (**B29** backend + **B29.1** frontend). Tags: `b29-v4-review-toggles` (backend), `v8.65.0-b29.1-not-reviewed-rows` (frontend). v4 now honours `editorialEnabled` / `complianceEnabled` / `evidenceEnabled` — each stage skipped when its toggle is off (evidence via existing skipped fast path; editorial/compliance halves gated in `runEditorialComplianceReview`). Deterministic aggregation unchanged. Frontend: evidence **Not reviewed** label + grey dot when skipped (fixed misleading **Confirmed**); **B29.1** adds **Not reviewed** grey rows for skipped editorial/compliance (keyed on `meta.reviewOptions`, not card verdict). Export already omits skipped-evidence line. All-off Review button already disabled. Resolves ROADMAP Active Backlog **#17**. | See ROADMAP **Recently shipped → B29 / B29.1** |
| B28 | **SHIPPED 2026-07-05** — Remove unused `eventType` from QC path (lean decision). Tag: `b28-remove-eventtype` (backend). Removed editorial DOCUMENT CONTEXT line (v3 split + v4 combined), `buildEditorialReviewContext` field, `evidence-skipped-fast-path` read, and `normalizeEventType` / `getEventTypeLabel` imports from reviewer. Compliance prompts already excluded it; no rule/verdict change. Generate/Rewrite event-type framing retained (**B32** / **R6.14**). Re-add to QC if R6.14 ships. Resolves ROADMAP Active Backlog **#3**. | See ROADMAP **Recently shipped → B28** |
| WR1 | **SHIPPED 2026-07-06** — Writing scaffold (PG demo). Tags: `wr1-writing-scaffold` (backend), `v8.66.0-wr1-writing-scaffold` (frontend). **Backend:** PG prompt library (`lib/prompt-library/pg-writing-prompts.mjs`) keyed `eventType` × visibility for `NEW_DIRECT_INVESTMENT` and `NEW_FUND_COMMITMENT`; consumed by `buildBasePrompt`; binding-precedence constraints; mandatory Methodology Note (`---METHODOLOGY---`); deterministic backstops (`pg-commentary-cleanup.mjs`, `normalizePgInvestmentName`, `applyPgFundCommitmentPostFilter`, sentence-boundary word-limit enforcement); canaries `pg_fund_exclusion_filtered`, `pg_word_limit_exceeded`. **Frontend:** `WritingInputModal`; Assess generate wiring (`useAssessState`); one version per Generate; Methodology Note panel (default collapsed, excluded from word count); Drafting **Configure text inputs** secondary button. **Demo sources:** `Shopify (text).txt`, `CVC VIII.txt`; Armitage IC memo as fund-filter stress-test fixture. **Learning:** source shape drives output — track-record-dense sources fight exclusions; close-memo/strategy-led sources generate clean. Generation path only; no QC change. Resolves **ROADMAP WSC1** / **Near-term — Writing scaffold**. Follow-on: **WR2**, **WR2.1**, **B32**. | See ROADMAP **Recently shipped → WR1** |
| A10 | **SHIPPED 2026-07-09** — Adapt into Assess sprint. Tag: `v8.51.0-rA10-adapt-into-assess` (frontend). **A10** Adapt modal + `handleAdapt`; labelled timeline entries with `meta.derivation`; full QC on adapted drafts. **A10.1** button/Beta/labels. **A10.2** / **A10.2.1** / **A10.2.2** derivation carry-forward, chip label, panel fill, scroll-to-top, resize-none. Backend `/api/adapt` unchanged. | See ROADMAP **Recently shipped → A10** |
| B40 | **SHIPPED 2026-08-09** — R7 Build B: offset population on `supportSpans`. Deterministic locate (exact `indexOf`, then repair-normalised with index map); authoritative-span-or-drop; offsets relative to stored source text; verdict path untouched. Tag: `r7-build-b`. | See ROADMAP **Recently shipped → R7 Build B** |
| B46 | **SHIPPED 2026-08-09** — R7 Build C: emit aligned source text + excludedSources in analyse-statements response (F12 data prerequisite). Deterministic; verdict path untouched. Tag: `r7-build-c`. | See ROADMAP **Recently shipped → R7 Build C** |
| B47 | **SHIPPED 2026-08-09** — Office-source ingestion fix: base64-encode docx/pptx/xlsx uploads so officeparser runs; backend rejects inline office-zip text (`OFFICE_INLINE_TEXT_NOT_ALLOWED`). Fixes Assess Office sources previously analysed as raw zip bytes. Tags: `b47-office-inline-guard` (backend), `v8.67.0-b47-office-upload-fix` (frontend). | See ROADMAP **Recently shipped → R7 F12** |
| F12 | **SHIPPED 2026-08-09** — R7.F12 Sources Drawer v1: reader (left source list + highlighted text pane), three openers (Sources button / row magnifier / card finding icon), verdict-coloured highlights (green/amber/red), per-span hover naming the statement. Tag: `v8.68.0-f12-sources-drawer`. | See ROADMAP **Recently shipped → R7 F12** |
| B6  | Wire Visibility (Complete vs Public) into rebuild — visibility now affects QC behaviour via R4.3 rule filtering and prompt calibration; Writing-path review intent wired in R6.13.1 (`versionType` / `selectedTypes` → `requiredVersion` / `outputType`). Residual Tier B `eventType` decision closed **B28** (2026-07-05). | R4.3 (`r4.3-visibility-wiring`), R6.13.1 (`r6.13.1-visibility-log-fix`), **B28** |
| F1  | **"Highlight in draft" link broken in Assess module.** **VERIFIED WORKING 2026-08-09** — F1 (Highlight in draft) + F2 (statement colour-coding) function in Assess via the in-place DraftOverlay path (R5.3a/b + R5.4), not a Draft Context pane. No dedicated fix tag; confirmed by code-path diagnostic + visual check. | Verified working 2026-08-09 |
| F2  | **Statement-level colour-coding broken in Assess module.** **VERIFIED WORKING 2026-08-09** — F1 (Highlight in draft) + F2 (statement colour-coding) function in Assess via the in-place DraftOverlay path (R5.3a/b + R5.4), not a Draft Context pane. No dedicated fix tag; confirmed by code-path diagnostic + visual check. | Verified working 2026-08-09 |
| F7  | **Reinstate colour-coding / Highlight in Draft in Assess (Draft Context limiter).** **RESOLVED 2026-08-09** — highlight + verdict colour-coding render in-place in Assess via DraftOverlay; the 'no Draft Context pane' limiter is outdated. If a fuller Draft Context pane is wanted later, log fresh. | Resolved 2026-08-09 |
| B45 | **SHIPPED 2026-08-11** — B45 AI-provenance URL scan: deterministic pre-Stage-1 scan flags AI-tool tracking params (utm_source=chatgpt.com etc.) in draft URLs; surfaced in the review summary. Additive, no verdict change. Tags: `b45-url-provenance-scan` (backend), `v8.69.0-b45-provenance-display` (frontend). | Sprint 1 (Attention-Router) |
| F13 | **SHIPPED 2026-08-11** — F13 excluded-sources display: the Sources drawer shows unreadable sources under 'Excluded · Not reviewed' with a plain reason; exclusion now catches scanned/image-only PDFs (extraction status `unsupported_scanned`), not just empty text. Tags: `f13-scanned-source-exclusion` (backend), `v8.70.0-f13-excluded-sources` (frontend). | Sprint 1 (Attention-Router) |
| Pr9 | **SHIPPED 2026-08-14** — Suggest revised draft: deterministic gather of all Review/Assess card concerns → single temp-0 LLM call → whole-draft holistic rewrite; finding-handling rulebook + note-wording pass. Tags: `Pr9-BE`, `Pr9-handling-BE`, `Pr9-notes-BE` (backend); `Pr9-FE`, `Pr9-handling-FE` (frontend). See ROADMAP **Recently shipped → Pr9**. | `Pr9-BE`, `Pr9-handling-BE`, `Pr9-notes-BE`, `Pr9-FE`, `Pr9-handling-FE` |
| B48 | **SHIPPED 2026-08-16** — Evidence conflict-vs-partial calibration. **VERDICT-ADJACENT** — shipped via diagnose-first + read-only shadow gate. Tag: `review-B48`. | `review-B48` |
| B13 | **SHIPPED 2026-08-16** — Stage 5 material-vs-pedantic partial distinction / materiality signal. Tag: `review-B13`. | `review-B13` |
| F8  | **SHIPPED 2026-08-18** — `number_spelling` quarter-notation deterministic backstop (`isQuarterNotationSpan`). Tag: `review-F8`. | `review-F8` |
| F9  | **SHIPPED 2026-08-18** — Duplicated suggestion-text de-noiser in editorial concern rendering. Tag: `review-F9` (frontend). | `review-F9` |
| B37 | **SHIPPED 2026-08-18** — Framing-goes-beyond-source flag collapsed to framing-escalation; material-only. Tags: `review-framing-BE` (backend), `review-framing-FE` (frontend). | `review-framing-BE`, `review-framing-FE` |
| B49 | **SHIPPED 2026-08-18** — Source-recency flag: structural source as-of-date extraction (`extractSourceAsOfDate`) + 18-month, claim-aware present-tense recency signal; additive, verdict-safe. Tags: `review-recency-BE` (backend), `review-recency-FE` (frontend). | `review-recency-BE`, `review-recency-FE` |
| B50 | **SHIPPED 2026-08-18** — Card density UX (Option A compact clean-line): clean dimensions collapse to one quiet line. Tag: `review-card-density` (frontend). | `review-card-density` |
| B51 | **SHIPPED 2026-08-18** — Source supersession (verdict-layer): period-aware; newer dated source's figure supersedes an older different-period source; draft-matches-current → supported + historical note (`qcCard.supersededSourceNotes`); same-period restatement and draft-behind stay conflict; confident-dates-only; reconciled with B48 magnitude backstop. New module `lib/qc/supersession.mjs`. Fixture `scripts/diagnostic/supersession/`. **VERDICT-ADJACENT** — shipped through diagnose-first + read-only shadow gate. Tag: `review-supersession`. | `review-supersession` |
| B52 | **SHIPPED 2026-08-19** — Recency anchoring + first-person-plural FP fixes: `recencySourceIndices()` anchors to supporting source (confirmed/partially_confirmed); `STYLE_RULE_DETERMINISTIC_FILTERS.first_person_plural` requires a real pronoun; `suppressNoOpSuggestions()` drops any house-style concern whose `suggestedRewrite` equals the statement text. Tag: `review-recency-anchor-firstperson`. | `review-recency-anchor-firstperson` |
|     | **SHIPPED 2026-08-24.** First-person removal names the actor. `first_person_plural` and `voice_consistency` share a substitution contract: replace the pronoun with the named authoring organisation as the grammatical subject; never recast agentless or passive; preserve hedges. Fixes a live defect where a style rule could strip the owner from a forward-looking return statement, turning a hedged opinion into an unattributed institutional prediction. Observed identically across two production runs on the same draft. Actor identified only when a known house name already appears in the draft; otherwise flag and leave first person in place. Harness: `scripts/diagnostic/first-person-actor-harness.mjs`. Tag: `review-first-person-actor` (`63de82f`). **UNVERIFIED IN PRODUCTION** (Meridian re-run 2026-08-24): live cards took the fallback path. See open **B86**. | `review-first-person-actor` (`63de82f`) |
|     | **SHIPPED 2026-08-24.** Authoring organisation is configuration (`AUTHORING_ORGANISATION`) rather than a hardcoded one-entry list. Production default unchanged. Synthetic fixtures de-identified to the fictional house Halden Group. Tag: `review-actor-config` (`cc69abc`). **UNVERIFIED IN PRODUCTION** (Meridian re-run 2026-08-24): see open **B86**. | `review-actor-config` (`cc69abc`) |
|     | **SHIPPED 2026-08-24.** Real published press releases restored to the real house name. Real-versus-invented principle recorded in `scripts/diagnostic/README.md`. Tag: `fixtures-real-vs-invented` (`2f5f403`). | `fixtures-real-vs-invented` (`2f5f403`) |
|     | **SHIPPED 2026-08-24.** A parenthetical view-marker is deleted when the sentence subject is already the named actor, and converted when it is not. Tag: `review-view-marker` (`bd45d08`). | `review-view-marker` (`bd45d08`) |
|     | **SHIPPED 2026-08-24.** Unsupported evaluative language is deleted rather than substituted for a milder word. Same laundering shape as approximating an unsupported figure. Tag: `review-hype-delete` (`63c359f`). | `review-hype-delete` (`63c359f`) |
|     | **SHIPPED 2026-08-24.** Evaluative-deletion directions state the resulting phrase so literal application does not leave stranded scaffolding. Tag: `review-hype-repair` (`02a0212`). | `review-hype-repair` (`02a0212`) |
| P17 | **SHIPPED 2026-08-24.** Invented material must not carry real names, of firms or of people. The harm is fabricated claims wearing a real name, not the name itself. Real published documents keep their real names, because they contain only what was actually said in public. Fixture `04_synth_vc_pinterest_style_memo` From-line de-identified to Nathan Calder / Helen Rusk. Same principle as the house-name rule, applied to individuals. Commit: `acdfcb8`. | `acdfcb8` |
| Pr15 | **CLOSED 2026-08-24.** Pr9 narrative-flow question, the original complaint. The end-to-end Meridian re-run was done and the revised draft was read. The flow holds. The unsupported equity cheque clause was cut rather than softened, a conflicting IRR figure was corrected, an unsupported evaluative word was deleted correctly, a compliance qualifier was added, an overlong sentence split into two that read properly, and the closing recommendation kept its owner rather than becoming an unattributed institutional prediction. The two contributing rules were `first_person_plural` and `marketing_language_excess`, both addressed in the review-layer chapter (chapter two). One new prose defect appeared on the same run; it is mechanical rather than a flow problem and is open **B87**, not a reason to keep Pr15 open. Tags already shipped: `review-first-person-actor` (`63de82f`), `review-hype-delete` (`63c359f`), `review-hype-repair` (`02a0212`). | Meridian re-run 2026-08-24 |
| F14 | **SHIPPED 2026-08-19** — `number_spelling` false positive on spelled-out 0–12 (e.g. "twelve" / "twelve months"). Deterministic filter drops cited spans that contain a spelled-out 0–12 word; durations treated as prose counts; units carve-out = measured physical units only. Tag: `review-percent-number-style`. | `review-percent-number-style` |
| B54 | **SHIPPED 2026-08-19** — `percentage_notation` under-fire on British "per cent". Rule text now names "per cent" alongside "percent"; drop-filter uses `\bper\s?cent\b` (still drop-only). Tag: `review-percent-number-style`. | `review-percent-number-style` |
| B59 | **SHIPPED 2026-08-19** — `extractPercents` "per cent" visibility + same-metric magnitude guard. `extractPercents` matches `per\s?cent` so supersession and the B48 magnitude backstop see per-cent figures; `hasEgregiousMagnitudeGap` requires a non-empty metric-key intersection before forcing a **percent** conflict (fixed cross-metric false pair: 19% margin vs 70% contracted). Money/count later guarded by **B60** / **B60.1** (shipped). **VERDICT-ADJACENT** — shipped via diagnose-first + read-only shadow gate. Tag: `review-percent-extract`. | `review-percent-extract` |
| B53a | **SHIPPED 2026-08-19** — Internal claim spans with upgrade-only rollup. One card per sentence retained; whole-sentence match stays authoritative; claims may only upgrade `partially_confirmed` → `confirmed`. Full-corpus shadow: exactly one verdict change (**F06 S5**, signed off) and zero regressions. Determinism harness: verdict-unstable statements 1/38 flag OFF → 0/38 flag ON. Flag `QC_CLAIM_SPANS` default ON; set `0`/`false`/`off` to disable without a deploy. Tags: `review-claim-spans` (`c290cee`), `review-claim-spans-on` (`e6e59a6`). Untagged infra/diag in the same arc: `48d8483` (harness, sizing, shadow baseline cache), `b81929b` (seed, `systemFingerprint`, concurrency cap, two-arm harness, word-boundary connectives). Residuals: **B61** (hasConflict only; B63 does not close it), **B53b**. **B62** and **B66** parked 2026-08-21 (funnel widening measured, no additional upgrades). | `review-claim-spans`, `review-claim-spans-on` |
| B67 | **CLOSED 2026-08-19** — Draft-achieved vs source-projected figure. Prompt does not name this direction; planted probe did not confirm it. Draft: "ARR reached EUR 95 million." Sole source occurrence: IC memo "Our base case envisages ARR growth to approximately EUR 95 million." Stage 2 on that pair: **`conflicting`** (completed action vs future projection). Card verdict: **`conflicting`**. No spec. **Preserve:** the IC memo catch is behaviour to keep, not to change — any **B60** fix must still return `conflicting` on that pair. Press-release / fact-sheet false conflicts on EUR 155 million revenue are **B60**, fixture `scripts/diagnostic/b67-probe/`. | `scripts/diagnostic/b67-probe/` |
| B63 | **SHIPPED 2026-08-20 as a COST OPTIMISATION only.** LLM result cache on the verdict path (Stages 1, 1b, 2). Flag `QC_LLM_CACHE` default ON; set `0`/`false`/`off` to disable without a deploy. Process-local in-memory LRU (`qc_llm_cache`; 1024 entries / 16 MiB). Does not survive a cold start or an instance change, so cross-session repeatability is not delivered. **Does NOT close B61.** Tags: `review-llm-cache` (`e6fd99c`), `review-llm-cache-on`. | `review-llm-cache`, `review-llm-cache-on` |
| B69 | **SHIPPED 2026-08-20.** Disk-backed LLM cache for local diagnostics. `createDiskStore` in `lib/qc/llm-cache.mjs`; enabled only when `QC_LLM_CACHE_DISK` is a path. Diagnostic scripts default that path to gitignored `scripts/diagnostic/.llm-cache.json`. Live-measurement scripts (`run-gate`, `run-stage1-stability`, `stage2-determinism`) force the cache off. Does not close B61. Tag: `review-cache-disk`. | `review-cache-disk` |
| B64 | **SHIPPED 2026-08-20.** Claim-spans anchor test also sees spelled-out numbers (one to twenty, tens, hundred/thousand/million/billion), all-caps acronyms of two or more letters, and capitalised tokens that are not the first token of the parent sentence. Local to `extractClaimSpanAnchors` in `lib/qc/claim-spans.mjs`; used by claim validity and `residualHasUnclaimedAnchor`. Pre-filter, `collectBackstopFigures`, and materiality unchanged. Shadow: `anchorless_claim` 14 to 8, decomposed 20 to 26, zero verdict/`hasConflict` changes. Tag: `review-claim-anchors`. | `review-claim-anchors` |
| B70 | **SHIPPED 2026-08-20.** `extractMoney` treats plain `m` as million (case-insensitive, word boundary) only in the currency-prefix alternative. `$155m` / `$155M` / `EUR 155m` scale to 155 million; `155m` / `155 m` with no currency stay non-money. Fixes four corpus magnitude-backstop false conflicts against `EUR 155 million`. Tag: `review-money-scale`. Shadow shared with **B60**. | `review-money-scale` |
| B60 | **SHIPPED 2026-08-20.** Money figures carry a single canonical `metric` id from a longest-first word-boundary phrase list. The magnitude backstop suppresses a forced money conflict only when both sides resolved and the ids differ (fail closed). Count and percent untouched. Tag: `review-money-metric-keys`. Shadow: `scripts/diagnostic/b60-money/run-shadow.mjs`. Character-window lookup replaced by **B60.1**. | `review-money-metric-keys` |
| B60.1 | **SHIPPED 2026-08-20.** Money metric resolution uses the sentence containing the figure (Stage 1 fallback splitter), nearest phrase by character distance, whole-passage fallback if no boundary. Replaces the 48-character window that both missed a 71-character phrase and truncated `annual recurring revenue` to `revenue`. Percent untouched. Tag: `review-money-metric-scope`. | `review-money-metric-scope` |
| B72 | **SHIPPED 2026-08-20.** Percent figures carry one canonical `metric` id, sentence-scoped like money (shared `metric-scope.mjs`). Longest-first word-boundary phrases; nearest match. `margin_unspecified` does not match `gross_margin` / `ebitda_margin`. Guard forces a percent conflict only when both sides resolved and the ids match. Probe: gross 45 vs EBITDA 19 no longer forces. Tag: `review-percent-metric-scope`. | `review-percent-metric-scope` |
| B71 | **SHIPPED 2026-08-21.** Money figures record a recognised currency (USD/EUR/GBP/AUD/CAD from symbol or ISO code). The magnitude backstop does not force when both sides have a currency and those currencies differ; unknown currency fails closed. Tag: `review-money-currency`. | `review-money-currency` |
| period-overlap | **SHIPPED 2026-08-21.** No B-id assigned. Two figures whose parseable periods do not overlap cannot contradict. Magnitude backstop no longer forces; period gate rewrites `confirmed`/`conflicting` to `no_support` and names both periods. Unparseable fails closed. Tag: `review-period-overlap` (`8f8eb67`). | `review-period-overlap` |
| B65 | **SHIPPED 2026-08-21.** Stage 1b prompt requires character-for-character copies; forbids inserting entity names from earlier in the sentence; a pronoun or bare verb phrase is a valid claim start. Gestcompost / Partners Group negative examples. Tag: `review-claim-fidelity` (`fe57d1d`; first commit `7d6cf6b` untagged, tag moved to the F10 follow-up). | `review-claim-fidelity` |
| B53c | **SHIPPED 2026-08-21.** Superlative and record check in framing fidelity. Vocabulary: record, highest, lowest, best, strongest, unprecedented, first ever, all-time, never before. Fires only when the phrase is absent from matched source passages. Editorial only; evidence verdict and hasConflict unchanged. Tag: `review-superlative`. | `review-superlative` |
| B73 | **SHIPPED 2026-08-21 as INSURANCE, not a live-path fix.** `splitDraftIntoCandidatesV2` no longer treats a dash as a sentence boundary. Production Stage 1 uses the LLM extractor and already kept F19 S2 whole; fallback (maxLen 240) and D-METRIC-LLM had cut the 257-character sentence at dashes. Colons and semicolons unchanged. LLM card texts and verdicts unchanged on the gate (100% cache). See open **B75**. Tag: `review-splitter-dash`. | `review-splitter-dash` |
| B76 | **SHIPPED 2026-08-21** — Durable review-state store on Neon Postgres: `GET` / `POST` / `DELETE` `/api/review-state`. Autosave buffer (one row per review, overwrite is correct), not an audit record. Tag: `persist-review-state` (`8af163d`; store commit `efccb11`). | `persist-review-state` |
| B77 | **SHIPPED 2026-08-21** — Localhost CORS on the review-state route only (production frontend origin plus echoed `http://localhost:<port>` / `http://127.0.0.1:<port>`). Other routes unchanged. Tag: `review-state-cors-local` (`c5ff204`). | `review-state-cors-local` |
| F15 | **SHIPPED 2026-08-21** — Autosave and restore the in-progress review so a refresh no longer loses work. One active review per browser; no list, picker, or history UI. **Accepted limitation (see open F16):** a restored PDF or Office source needs re-uploading before Review can run again. Follow-on: **F17**, **F18**, **F19**. Tag: `v8.71.0-review-state-restore`. | `v8.71.0-review-state-restore` |
| F17 | **SHIPPED 2026-08-21** — Delete the abandoned `review_state` row on New output / `startNewReview` (best-effort `apiDeleteReviewState` of the outgoing id). Temporary snapshot-size log (removed in **F19**). Tag: `v8.72.0-review-state-cleanup`. | `v8.72.0-review-state-cleanup` |
| F18 | **SHIPPED 2026-08-21** — Stop storing raw source file bytes (`contentBase64`) in the saved review state. Measured: two PDFs 3.1 MB + 5.3 MB were 8,762,415B (8,761,484B base64) against a 4,194,304B save limit; a later post-Review session with one large PDF serialised to 47,182B. **Accepted limitation:** restored PDF/Office sources cannot run Review again until re-uploaded, because the backend refuses inline text for PDFs so extraction always happens server-side from the original. Tag: `v8.73.0-review-state-no-blobs`. | `v8.73.0-review-state-no-blobs` |
| F19 | **SHIPPED 2026-08-21** — Pill "Re-upload to run again"; Review `title` explains the disabled control; snapshot-size log removed; `[API BASE DEBUG]` gated to Vite DEV. Tag: `v8.74.0-review-state-tidy`. | `v8.74.0-review-state-tidy` |
| F20 | **SHIPPED 2026-08-22.** Client-side size guard. Refuses oversized sources and drafts before the request is sent, with a clear message naming the size and the remedy. Shared budget module (`sourceRequestBudget.js`). Untagged follow-up `3fce4fb` brought the Assess draft-upload path onto the same budget and removed a stale 10 MB PDF cap, so there is exactly one size constant in the frontend. Tag: `v8.75.0-source-size-guard` (`d0bbcc5`). Follow-up: `3fce4fb`. | `v8.75.0-source-size-guard`, `3fce4fb` |
| F21 | **SHIPPED 2026-08-22.** Size-limit messages no longer claim the limit is specific to Review, because the same upload feeds Drafting. Tag: `v8.76.0-size-message-wording` (`16a8cd1`). | `v8.76.0-size-message-wording` |
| B78 | **CLOSED 2026-08-22 as PROVEN, not as fixed.** The 2026-08-21 probe (upload a 5+ MB PDF) completed in production with an 11.3 MB PDF: Vercel 413 at the edge, CORS-masked as `TypeError: Load failed`. Standing limitation is open **B79** (GUARDED). Guard shipped as **F20** / **F21**. | production 11.3 MB PDF 2026-08-21 |
| P11 | **SHIPPED 2026-08-21** — Node `engines` pinned to **22.x** (Vercel production Node 22 LTS, not 24). Backend commit `86ada5d` (untagged itself; contained in `persist-review-state` / `review-state-cors-local`). Frontend commit `888f6a8`. | `86ada5d`, `888f6a8` |
| P12 | **SHIPPED 2026-08-21** — Vercel functions pinned to Frankfurt (`regions: ["fra1"]`). Commit `060d1ce` (untagged itself; contained in `persist-review-state` / `review-state-cors-local`). | `060d1ce` |
| Pr9-correctness | **SHIPPED 2026-08-23.** Rewrite correctness arc. Together these four tags fixed a revision that could attach a note claiming a removal to text it left untouched, reproduced 3/3 on the production sentence. `pr9-claim-spans` (`d8ab2df`) feeds per-claim evidence spans into the rewrite prompt. `pr9-soften-or-cut` (`0cd76a5`) applies one test: soften, cut the clause, or keep-and-flag; never approximate an unsupported figure. `pr9-marker-intent` (`00cce35`) declares CHANGED / KEPT / CUT with a deterministic honesty check. `pr9-cut-punctuation` (`71500c4`) tidies punctuation left behind by a clause cut. Residuals: **B80**, **B81**. **Pr15** closed 2026-08-24 (Meridian re-run; flow holds). Process: **P15**. Claude project docs (not in this repo): `claude/pr9-rewrite-correctness-arc.md`, `claude/pr9-finding-handling-rulebook.md`. | `pr9-claim-spans` (`d8ab2df`), `pr9-soften-or-cut` (`0cd76a5`), `pr9-marker-intent` (`00cce35`), `pr9-cut-punctuation` (`71500c4`) |

---

## Rejected (do not re-propose)

| ID | Item | Why rejected | Date |
|----|------|--------------|------|
| — | **Relational linkage check** (LLM check on causal / ordering connectives) | REJECTED ON EVIDENCE 2026-08-19. After the word-boundary fix (`b81929b`), only **11** corpus sentences match the connective blocklist, and most are `up from` (plain comparison, not causation or ordering). Genuine relational risk is 4–5 sentences, under 2% of the corpus. Not worth an LLM call per sentence or the concern-noise it would add. | 2026-08-19 |

---

## How to update this file

This file is updated by Cursor on instruction from the user. The standard pattern:

**To add a new item**, the user gives Cursor a short instruction like:

> Add to BACKLOG.md under [section]: [one-line description]. Source: [where it came from]. Priority: [H/M/L].

Example:
> Add to BACKLOG.md under Frontend / UI: "Tooltip on Editorial signal explains the rule that fired." Source: R2.4 testing. Priority: L.

Cursor adds the row at the bottom of the relevant table with the next available ID (F7, B12, etc.) and writes the entry. No reformatting of existing rows.

**To close an item**, the user gives Cursor:

> Close BACKLOG.md item [ID]. Resolved by [tag/spec ID].

Cursor moves the row from its current table into the "Closed" table with the resolution tag noted, and adjusts the IDs only if necessary (gaps in numbering are fine — leave them).

**To re-prioritise**, the user gives Cursor:

> Update BACKLOG.md item [ID] priority to [H/M/L].

Cursor edits the priority column. No other changes.

**Periodic review**: at the end of each sprint, the user reviews this file with Claude to identify items ready to schedule. Items moving into a sprint don't get closed until shipped — they stay in the backlog with a note like "(scheduled in R3.1)".

---

## Conventions

- **Priority**: H = should pick up in next sprint or two, M = should pick up within a few sprints, S = small / cheap, L = nice-to-have, no urgency. `--` = watch-only / no priority.
- **Source / context**: where the item was identified. Useful for tracing back to a chat or Langfuse trace if context is needed.
- **IDs**: prefixed by table — F (Frontend), B (Backend), P (Process), Pr (Product). Numbers are assigned sequentially within each table and never reused.
- **No detailed specs in this file**: each row is a one-liner. When an item is scheduled, a proper spec gets written.
