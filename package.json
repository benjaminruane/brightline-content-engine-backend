{
  "name": "brightline-content-engine-backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "api/generate.js",
  "scripts": {
    "test": "echo \"no tests\"",
    "prebuild": "NODE_OPTIONS=\"--trace-uncaught --trace-warnings --enable-source-maps\" node scripts/import-isolator.mjs",
    "build": "echo \"NODE_OPTIONS=$NODE_OPTIONS\" && NODE_OPTIONS=\"--trace-uncaught --trace-warnings --enable-source-maps\" echo \"Build complete (no compilation needed for JS)\""
  },
  "dependencies": {
    "openai": "^4.0.0",
    "@mozilla/readability": "^0.5.0",
    "jsdom": "^24.0.0",
    "node-fetch": "2.6.12"
  }
}
