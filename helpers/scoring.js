// helpers/scoring.js
// Currently unused by generate/rewrite, but kept for future use.

export async function scoreOutput() {
  return {
    overall: 85,
    clarity: 0.8,
    accuracy: 0.75,
    tone: 0.8,
    structure: 0.78
  };
}
